name: CI - App - Hybrid - Entertainment - Nikoli

on:
  push:
    branches:
      - 'master'
      - 'develop'
      - 'hotfix/**'
      - 'feature/**'
      - 'renovate/**'
      - 'dependabot/**'
    paths:
      - 'packages/app/hybrid/entertainment/nikoli/**'
      - '.github/workflows/ci-app-hybrid-entertainment-nikoli.yaml'
      - '.github/workflows/ci-app-hybrid-template.yaml'

permissions:
  contents: write

concurrency:
  group: ci-app-hybrid-entertainment-nikoli-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: ./.github/workflows/ci-app-hybrid-template.yaml
    with:
      package: packages/app/hybrid/entertainment/nikoli
      filter: '@hieudoanm.github.io/nikoli'
      releaseName: app-hybrid-entertainment-nikoli-latest
      releaseTitle: App - Hybrid - Entertainment - Nikoli (latest)
      artifactPrefix: entertainment-nikoli
