name: CI - App - Native - Android - AndroidX

on:
  push:
    branches:
      - 'master'
      - 'develop'
      - 'hotfix/**'
      - 'feature/**'
      - 'renovate/**'
      - 'dependabot/**'
    paths:
      - 'packages/app/native/androidx/android/**/*'
      - '.github/workflows/ci-app-native-android-androidx.yaml'
      - '.github/workflows/ci-app-native-android-template.yaml'

permissions:
  contents: write

concurrency:
  group: ci-app-native-android-androidx-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: ./.github/workflows/ci-app-native-android-template.yaml
    with:
      package: packages/app/native/androidx/android
      releaseName: app-native-android-androidx-latest
      releaseTitle: App - Native - Android - AndroidX (latest)
      artifactPrefix: android-androidx
