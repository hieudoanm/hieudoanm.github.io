name: CI - App - Hybrid - PDF

on:
  push:
    branches:
      - 'master'
      - 'develop'
      - 'hotfix/**'
      - 'feature/**'
      - 'renovate/**'
      - 'dependabot/**'
    paths:
      - 'packages/app/hybrid/pdf/**'
      - '.github/workflows/ci-app-hybrid-pdf.yaml'
      - '.github/workflows/ci-app-hybrid-template.yaml'

permissions:
  contents: write

concurrency:
  group: ci-app-hybrid-pdf-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: ./.github/workflows/ci-app-hybrid-template.yaml
    with:
      package: packages/app/hybrid/pdf
      filter: '@hieudoanm.github.io/pdf'
      releaseName: app-hybrid-pdf-latest
      releaseTitle: App - Hybrid - PDF (latest)
      artifactPrefix: pdf
