name: CI - App - Native - macOS - Brewery

on:
  push:
    branches:
      - 'master'
      - 'develop'
      - 'hotfix/**'
      - 'feature/**'
      - 'renovate/**'
      - 'dependabot/**'
    paths:
      - 'packages/app/native/brewery/apple/macos/**/*'
      - '.github/workflows/ci-app-native-macos-brewery.yaml'
      - '.github/workflows/ci-app-native-macos-template.yaml'

permissions:
  contents: write

concurrency:
  group: ci-app-native-macos-brewery-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: ./.github/workflows/ci-app-native-macos-template.yaml
    with:
      package: packages/app/native/brewery/apple/macos
      releaseName: app-native-macos-brewery-latest
      releaseTitle: App - Native - macOS - Brewery (latest)
      artifactPrefix: macos-brewery
