name: CI - App - Native - macOS - Top

on:
  push:
    branches:
      - 'master'
      - 'develop'
      - 'hotfix/**'
      - 'feature/**'
      - 'renovate/**'
      - 'dependabot/**'
    paths:
      - 'packages/app/native/top/apple/macos/**/*'
      - '.github/workflows/ci-app-native-macos-top.yaml'
      - '.github/workflows/ci-app-native-macos-template.yaml'

permissions:
  contents: write

concurrency:
  group: ci-app-native-macos-top-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: ./.github/workflows/ci-app-native-macos-template.yaml
    with:
      package: packages/app/native/top/apple/macos
      releaseName: app-native-macos-top-latest
      releaseTitle: App - Native - macOS - Top (latest)
      artifactPrefix: macos-top
