name: CI - App - Hybrid - Password

on:
  push:
    branches:
      - 'master'
      - 'develop'
      - 'hotfix/**'
      - 'feature/**'
      - 'renovate/**'
      - 'dependabot/**'
    paths:
      - 'packages/app/hybrid/password/**'
      - '.github/workflows/ci-app-hybrid-password.yaml'
      - '.github/workflows/ci-app-hybrid-template.yaml'

permissions:
  contents: read

concurrency:
  group: ci-app-hybrid-password-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: ./.github/workflows/ci-app-hybrid-template.yaml
    with:
      package: packages/app/hybrid/password
      filter: '@hieudoanm.github.io/password'
      releaseName: app-hybrid-password-latest
      releaseTitle: App - Hybrid - Password (latest)
      artifactPrefix: password
