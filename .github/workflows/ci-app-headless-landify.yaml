name: CI - App - Headless - Landify

on:
  push:
    branches:
      - 'master'
      - 'develop'
      - 'hotfix/**'
      - 'feature/**'
      - 'renovate/**'
      - 'dependabot/**'
    paths:
      - 'packages/app/headless/landify/go/**'
      - '.github/workflows/ci-app-headless-landify.yaml'
      - '.github/workflows/ci-app-headless-go-template.yaml'

permissions:
  contents: write

concurrency:
  group: ci-app-headless-landify-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: ./.github/workflows/ci-app-headless-go-template.yaml
    with:
      package: packages/app/headless/landify/go
      installZig: false
      releaseName: app-headless-landify-latest
      releaseTitle: App - Headless - Landify (latest)
      artifactName: app-headless-landify
      artifactPath: packages/app/headless/landify/go/bin/*
