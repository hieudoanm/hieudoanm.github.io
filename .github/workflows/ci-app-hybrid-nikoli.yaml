name: CI - App - Hybrid - Nikoli

on:
  push:
    branches:
      - 'master'
      - 'develop'
      - 'hotfix/**'
      - 'feature/**'
      - 'renovate/**'
      - 'dependabot/**'
    paths:
      - 'packages/app/hybrid/nikoli/**'
      - '.github/workflows/ci-app-hybrid-nikoli.yaml'
      - '.github/workflows/ci-app-hybrid-template.yaml'

permissions:
  contents: write

concurrency:
  group: ci-app-hybrid-nikoli-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: ./.github/workflows/ci-app-hybrid-template.yaml
    with:
      package: packages/app/hybrid/nikoli
      filter: '@hieudoanm.github.io/nikoli'
      releaseName: app-hybrid-nikoli-latest
      releaseTitle: App - Hybrid - Nikoli (latest)
      artifactPrefix: nikoli
