name: CI - App - Native - Android - Block

on:
  push:
    branches:
      - 'master'
      - 'develop'
      - 'hotfix/**'
      - 'feature/**'
      - 'renovate/**'
      - 'dependabot/**'
    paths:
      - 'packages/app/native/block/android/**/*'
      - '.github/workflows/ci-app-native-android-block.yaml'
      - '.github/workflows/ci-app-native-android-template.yaml'

permissions:
  contents: write

concurrency:
  group: ci-app-native-android-block-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: ./.github/workflows/ci-app-native-android-template.yaml
    with:
      package: packages/app/native/block/android
      releaseName: app-native-android-block-latest
      releaseTitle: App - Native - Android - Block (latest)
      artifactPrefix: android-block
