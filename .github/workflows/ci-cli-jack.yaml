name: CI - CLI - Jack

on:
  push:
    branches:
      - 'master'
      - 'develop'
      - 'hotfix/**'
      - 'feature/**'
      - 'renovate/**'
      - 'dependabot/**'
    paths:
      - 'packages/cli/jack/clap.rs/**'
      - 'packages/cli/jack/cobra.go/**'
      - 'packages/cli/jack/swift/**'
      - 'packages/cli/jack/cli.kt/**'
      - '.github/workflows/ci-cli-jack.yaml'

permissions:
  contents: write

jobs:
  rust:
    name: Rust
    runs-on: macos-latest
    defaults:
      run:
        working-directory: packages/cli/jack/clap.rs
    steps:
      - name: Checkout
        uses: actions/checkout@v6.1.0
      - name: Install Rust
        uses: actions-rust-lang/setup-rust-toolchain@v1.17.0
        with:
          components: clippy
      - name: Install zig
        run: |
          case "$RUNNER_OS" in
            Linux)
              url="https://ziglang.org/download/0.14.0/zig-linux-x86_64-0.14.0.tar.xz"
              dir="zig-linux-x86_64-0.14.0"
              ;;
            macOS)
              url="https://ziglang.org/download/0.14.0/zig-macos-aarch64-0.14.0.tar.xz"
              dir="zig-macos-aarch64-0.14.0"
              ;;
          esac
          curl -fsSL "$url" | tar xJ -C /tmp
          echo "/tmp/$dir" >> "$GITHUB_PATH"
      - name: Lint
        run: make lint
      - name: Test
        run: make test
      - name: Build (cross-compile)
        run: make build-all
      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: cli-rust
          path: packages/cli/jack/clap.rs/bin/*

  go:
    name: Go
    runs-on: macos-latest
    defaults:
      run:
        working-directory: packages/cli/jack/cobra.go
    steps:
      - name: Checkout
        uses: actions/checkout@v6.1.0
      - name: Install Go
        uses: actions/setup-go@v6.4.0
        with:
          go-version: '1.26.5'
          cache-dependency-path: packages/cli/jack/cobra.go/go.sum
      - name: Install Dependencies
        run: go mod download
      - name: Install zig
        run: |
          case "$RUNNER_OS" in
            Linux)
              url="https://ziglang.org/download/0.14.0/zig-linux-x86_64-0.14.0.tar.xz"
              dir="zig-linux-x86_64-0.14.0"
              ;;
            macOS)
              url="https://ziglang.org/download/0.14.0/zig-macos-aarch64-0.14.0.tar.xz"
              dir="zig-macos-aarch64-0.14.0"
              ;;
          esac
          curl -fsSL "$url" | tar xJ -C /tmp
          echo "/tmp/$dir" >> "$GITHUB_PATH"
      - name: Lint
        run: make lint
      - name: Test
        run: make test
      - name: Build (cross-compile)
        run: make build-all
      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: cli-go
          path: packages/cli/jack/cobra.go/bin/*

  swift:
    name: Swift
    runs-on: macos-latest
    defaults:
      run:
        working-directory: packages/cli/jack/swift
    steps:
      - name: Checkout
        uses: actions/checkout@v6.1.0
      - name: Build
        run: make build
      - name: Test
        run: swift test
      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: cli-swift
          path: packages/cli/jack/swift/.build/release/jack

  kotlin:
    name: Kotlin
    runs-on: macos-latest
    defaults:
      run:
        working-directory: packages/cli/jack/cli.kt
    steps:
      - name: Checkout
        uses: actions/checkout@v6.1.0
      - name: Install JDK
        uses: actions/setup-java@v6.4.0
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle
      - name: Test
        run: ./gradlew test

  publish:
    name: Publish to GitHub Release
    needs: [rust, go, swift]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6.1.0
        with:
          fetch-depth: 0
      - name: Download Build Artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
      - name: Collect Release Artifacts
        run: |
          mkdir -p release
          for dir in artifacts/*/; do
            artifact_name=$(basename "$dir")
            for file in "$dir"*; do
              cp "$file" "release/${artifact_name}-$(basename "$file")"
            done
          done
      - name: Publish Release
        run: |
          gh release delete cli-jack-latest --yes --cleanup-tag 2>/dev/null || true
          gh release create cli-jack-latest \
            --title "CLI - Jack (latest)" \
            --notes "Auto-built CLI tools from commit ${{ github.sha }}" \
            release/*
        env:
          GH_TOKEN: ${{ github.token }}
