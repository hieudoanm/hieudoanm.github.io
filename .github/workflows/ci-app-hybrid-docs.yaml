name: CI - App - Hybrid - Docs

on:
  push:
    branches:
      - 'master'
      - 'develop'
      - 'hotfix/**'
      - 'feature/**'
      - 'renovate/**'
      - 'dependabot/**'
    paths:
      - 'packages/app/hybrid/docs/**'
      - '.github/workflows/ci-app-hybrid-docs.yaml'
      - '.github/workflows/ci-app-hybrid-template.yaml'

permissions:
  contents: write

concurrency:
  group: ci-app-hybrid-docs-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: ./.github/workflows/ci-app-hybrid-template.yaml
    with:
      package: packages/app/hybrid/docs
      filter: '@hieudoanm.github.io/docs'
      releaseName: app-hybrid-docs-latest
      releaseTitle: App - Hybrid - Docs (latest)
      artifactPrefix: docs
      needsEnv: true
