name: CI - App - Native - macOS - Gauge

on:
  push:
    branches:
      - 'master'
      - 'develop'
      - 'hotfix/**'
      - 'feature/**'
      - 'renovate/**'
      - 'dependabot/**'
    paths:
      - 'packages/app/native/gauge/apple/macos/**/*'
      - '.github/workflows/ci-app-native-macos-gauge.yaml'
      - '.github/workflows/ci-app-native-macos-template.yaml'

permissions:
  contents: write

concurrency:
  group: ci-app-native-macos-gauge-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: ./.github/workflows/ci-app-native-macos-template.yaml
    with:
      package: packages/app/native/gauge/apple/macos
      releaseName: app-native-macos-gauge-latest
      releaseTitle: App - Native - macOS - Gauge (latest)
      artifactPrefix: macos-gauge
