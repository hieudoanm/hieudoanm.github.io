name: CI - App - Headless - Backbone

on:
  push:
    branches:
      - 'master'
      - 'develop'
      - 'hotfix/**'
      - 'feature/**'
      - 'renovate/**'
      - 'dependabot/**'
    paths:
      - 'packages/app/headless/backbone/languages/go/**'
      - 'packages/app/headless/backbone/languages/rust/**'
      - '.github/workflows/ci-app-headless-backbone.yaml'
      - '.github/workflows/ci-app-headless-template.yaml'

permissions:
  contents: write

concurrency:
  group: ci-app-headless-backbone-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: ./.github/workflows/ci-app-headless-template.yaml
    with:
      package: packages/app/headless/backbone/languages/go
      rustPackage: packages/app/headless/backbone/languages/rust
      releaseName: app-headless-backbone-latest
      releaseTitle: App - Headless - Backbone (latest)
      artifactName: app-headless-backbone
      artifactPath: packages/app/headless/backbone/languages/go/bin/*
      rustArtifactPath: packages/app/headless/backbone/languages/rust/target/release/backbone
      installZig: false
      installRustDeps: false
