name: CI - App - Hybrid - Keynotes

on:
  push:
    branches:
      - 'master'
      - 'develop'
      - 'hotfix/**'
      - 'feature/**'
      - 'renovate/**'
      - 'dependabot/**'
    paths:
      - 'packages/app/hybrid/keynotes/**'
      - '.github/workflows/ci-app-hybrid-keynotes.yaml'
      - '.github/workflows/ci-app-hybrid-template.yaml'

permissions:
  contents: write

concurrency:
  group: ci-app-hybrid-keynotes-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: ./.github/workflows/ci-app-hybrid-template.yaml
    with:
      package: packages/app/hybrid/keynotes
      filter: '@hieudoanm.github.io/keynotes'
      releaseName: app-hybrid-keynotes-latest
      releaseTitle: App - Hybrid - Keynotes (latest)
      artifactPrefix: keynotes
