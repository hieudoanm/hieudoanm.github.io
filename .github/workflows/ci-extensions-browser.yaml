name: CI - Extensions - Browser

on:
  push:
    branches:
      - 'master'
      - 'develop'
      - 'hotfix/**'
      - 'feature/**'
      - 'renovate/**'
      - 'dependabot/**'
    paths:
      - 'packages/extensions/browser/**'
      - '.github/workflows/ci-extensions-browser.yaml'

permissions:
  contents: write

concurrency:
  group: ci-extensions-browser-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.1
      - name: Install pnpm
        uses: pnpm/action-setup@v6.0.10
        with:
          version: '11.22.0'
      - name: Install Node.js
        uses: actions/setup-node@v7.0.0
        with:
          node-version: '26.7.0'
          cache: 'pnpm'
      - name: Setup Chrome
        uses: browser-actions/setup-chrome@v2.2.0
      - name: Install Dependencies
        run: pnpm install --frozen-lockfile
      - name: Build
        run: pnpm run build --filter=@hieudoanm.github.io/extension
      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v7.0.1
        with:
          name: hieudoanm-extension
          path: packages/extensions/browser/hieudoanm/ts/download/

  publish:
    name: Publish to GitHub Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.1
      - name: Download Build Artifacts
        uses: actions/download-artifact@v8.0.1
        with:
          path: artifacts
      - name: Collect Release Artifacts
        run: |
          mkdir -p release
          find artifacts -type f -exec cp {} release/ \;
      - name: Publish Release
        run: |
          gh release delete extensions-browser-latest --yes --cleanup-tag 2>/dev/null || true
          gh release create extensions-browser-latest \
            --title "Extensions - Browser Extensions (latest)" \
            --notes "Auto-built Browser Extensions from commit ${{ github.sha }}" \
            release/*
        env:
          GH_TOKEN: ${{ github.token }}
