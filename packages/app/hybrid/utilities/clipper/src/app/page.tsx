import type { FC } from 'react';
import ClipboardHistory from '@/components/organisms/ClipboardHistory';

const HomePage: FC = () => (
  <div className="h-screen">
    <ClipboardHistory />
  </div>
);

export default HomePage;
