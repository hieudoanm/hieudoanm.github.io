import type { Responder } from './types';

export const POOL = 100;
export const MIN_THRESHOLD = 20;
export const MAX_THRESHOLD = 45;
export const TOTAL_ROUNDS = 5;

export const RESPONDER: Responder = {
  id: 'rival',
  name: 'Rival',
  emoji: '🎭',
};
