export const BASE_PRICE = 10;
export const BASE_Q = 10;
export const MIN_PRICE = 1;
export const MAX_PRICE = 30;

export const EPSILONS = [0.5, 1, 2, 0.2, 3, 1];

export const TOTAL_ROUNDS = EPSILONS.length;

export const RULE =
  'Elasticity of demand e = (%ΔQ) / (%ΔP). Total revenue peaks where |e| = 1.';
