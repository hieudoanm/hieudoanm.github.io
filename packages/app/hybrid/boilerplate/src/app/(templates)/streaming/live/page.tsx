'use client';

import { LiveChannelsTemplate } from '@/components/templates/streaming/LiveChannelsTemplate';

const LiveChannelsPage = () => <LiveChannelsTemplate />;

export default LiveChannelsPage;
