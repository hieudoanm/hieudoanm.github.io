# New Tab - New Tab Extension

Haven on every new tab — opens the hieudoanm home app instead of the browser's
default new-tab page.

## Install

Load as an unpacked extension in Chrome/Edge/Firefox from `dist/v3` (or `dist/v2`).
