1:"$Sreact.fragment"
2:I[83070,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js"],""]
e:X
13:X
13:C
0:{"buildId":"kwMAhgW1ckibYawstx_6-","data":[{"rsc":["$","$1","c",{"children":[["$","div",null,{"className":"h-screen overflow-y-auto","children":["$","main",null,{"className":"mx-auto max-w-3xl px-4 py-10","children":[["$","p",null,{"className":"mb-6 opacity-60","children":["$","$L2",null,{"className":"link","href":"/posts/","children":"← All posts"}]}],["$","article",null,{"children":[["$","header",null,{"className":"mb-8","children":[["$","h1",null,{"className":"text-3xl font-bold","children":"Google Calendar — Scheduling"}],["$","p",null,{"className":"text-base-content/70 mt-2","children":"Event scheduling, availability, invites, recurring events, reminders."}],["$","p",null,{"className":"text-base-content/50 mt-3 text-sm","children":["Hieu Doan"," · ",["$","span",null,{"className":"capitalize","children":"medium"}]," ·"," ",["$","span",null,{"className":"capitalize","children":"productivity"}]]}],["$","div",null,{"className":"mt-3 flex flex-wrap gap-1.5","children":[["$","span","booking",{"className":"badge badge-outline badge-sm","children":"booking"}],["$","span","scheduling",{"className":"badge badge-outline badge-sm","children":"scheduling"}]]}]]}],["$","section",null,{"children":[["$","h2",null,{"className":"mb-2 text-xl font-semibold","children":"Interview Questions"}],["$","ul",null,{"className":"list-disc space-y-1 pl-6","children":[["$","li","Design a calendar / scheduling service",{"children":"Design a calendar / scheduling service"}],["$","li","How do you detect overlapping events and find free slots?",{"children":"How do you detect overlapping events and find free slots?"}],["$","li","How do you distribute invites and manage RSVPs?",{"children":"How do you distribute invites and manage RSVPs?"}],["$","li","How do you handle recurring events?",{"children":"How do you handle recurring events?"}],["$","li","How do you sync events across devices offline?",{"children":"How do you sync events across devices offline?"}]]}]]}],["$","section",null,{"className":"mt-8","children":[["$","h2",null,{"className":"mb-4 text-xl font-semibold","children":"Answers"}],["$","div",null,{"className":"space-y-6","children":[["$","div","Design a calendar / scheduling service",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"Design a calendar / scheduling service"}],[["$","p","The core is an event service that owns CRUD for events, a sync service that replicates changes to clients, and an availability engine that answers \"when is this person free?\" Each event has a title, time range, timezone, attendees, recurrence rule, and an organizer; events persist in an events DB (typically a sharded relational store keyed by user, with time-range columns indexed) and are materialized into a search index for cross-user queries like \"who is free on Tuesday.\" Invites go through an invitation service that creates attendee copies (or shared-event references), tracks RSVP state, and drives notifications. The API gateway fronts the mobile and web clients, and a notifier schedules reminders — email, push, or in-app — well before each event.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"The core is an event service that owns CRUD for events, a sync service that replicates changes to clients, and an availability engine that answers \"when is this person free?\" Each event has a title, time range, timezone, attendees, recurrence rule, and an organizer; events persist in an events DB (typically a sharded relational store keyed by user, with time-range columns indexed) and are materialized into a search index for cross-user queries like \"who is free on Tuesday.\" Invites go through an invitation service that creates attendee copies (or shared-event references), tracks RSVP state, and drives notifications. The API gateway fronts the mobile and web clients, and a notifier schedules reminders — email, push, or in-app — well before each event."}]]}],"$L3"]]}],"$L4","$L5","$L6","$L7"]}]]}],"$L8"]}],"$L9"]}]}],["$La","$Lb"],"$Lc"]}],"isPartial":"$@d","staleTime":"$e","varyParams":null},{"rsc":"$Lf","isPartial":"$@10","staleTime":"$e","varyParams":null},{"rsc":"$L11","isPartial":"$@12","staleTime":"$e","varyParams":"$13"},{"rsc":"$L14","isPartial":"$@15","staleTime":"$e","varyParams":"$13"},{"rsc":"$L16","isPartial":"$@17","staleTime":"$e","varyParams":null}],"isUpgradeableISRFallback":false,"a":"$@18","rootVaryParams":null,"needsRuntimeRequest":"$@19"}
1a:I[94844,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js"],"default"]
1b:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"OutletBoundary"]
1c:"$Sreact.suspense"
1e:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"ViewportBoundary"]
1f:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"MetadataBoundary"]
20:I[93226,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default"]
21:I[15750,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default"]
22:I[93226,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"LoadingBoundaryProvider"]
23:I[46791,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/2td1d3x1ljv-f.js"],"default"]
:HL["/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","style"]
:HL["/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","style"]
3:["$","p","Design choices follow the product's requirements. Timezone handling is stored as the user's timezone with the event time in UTC plus an IANA zone; recurrence is never stored as expanded rows but as a rule with an exclusion set. Reads are heavily cached (a slots cache holds precomputed free/busy windows per user for the next N days) because availability queries are the hot path. Writes are transactional per user to avoid double-booking within a single calendar, while cross-user conflicts are resolved by the availability check rather than locks. The system must scale to many attendees without fan-out storms: for very large calendars, invites are batched and read-your-own-writes consistency is relaxed to eventual consistency across copies.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Design choices follow the product's requirements. Timezone handling is stored as the user's timezone with the event time in UTC plus an IANA zone; recurrence is never stored as expanded rows but as a rule with an exclusion set. Reads are heavily cached (a slots cache holds precomputed free/busy windows per user for the next N days) because availability queries are the hot path. Writes are transactional per user to avoid double-booking within a single calendar, while cross-user conflicts are resolved by the availability check rather than locks. The system must scale to many attendees without fan-out storms: for very large calendars, invites are batched and read-your-own-writes consistency is relaxed to eventual consistency across copies."}]]}]
4:["$","div","How do you detect overlapping events and find free slots?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you detect overlapping events and find free slots?"}],[["$","p","Detecting overlaps is a classic interval problem: two events overlap when `startA < endB AND startB < endA`. For one user, the events DB stores rows with a (user_id, start_time, end_time) composite index, so a range query on the index returns candidate events in O(log n + k), and the service verifies overlaps in memory over the small result set. The availability engine inverts this: to find free slots it loads the user's busy intervals for the requested window (from a precomputed slots cache or by querying the index), merges adjacent intervals, and subtracts them from the requested range, respecting the user's working hours and out-of-office blocks. With many participants, the engine intersects each person's free windows — take one user's free intervals and filter by the next user's, iteratively — and ranks the remaining gaps by size and preference (earliest, longest, or during business hours).",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Detecting overlaps is a classic interval problem: two events overlap when "}],["$","code","1",{"className":"bg-base-300 rounded px-1 font-mono text-sm","children":"startA < endB AND startB < endA"}],["$","$1","2",{"children":". For one user, the events DB stores rows with a (user_id, start_time, end_time) composite index, so a range query on the index returns candidate events in O(log n + k), and the service verifies overlaps in memory over the small result set. The availability engine inverts this: to find free slots it loads the user's busy intervals for the requested window (from a precomputed slots cache or by querying the index), merges adjacent intervals, and subtracts them from the requested range, respecting the user's working hours and out-of-office blocks. With many participants, the engine intersects each person's free windows — take one user's free intervals and filter by the next user's, iteratively — and ranks the remaining gaps by size and preference (earliest, longest, or during business hours)."}]]}],["$","p","Two practical concerns dominate. First, caching: per-user free/busy windows for the next few weeks are expensive to recompute, so the availability engine writes them to a cache and invalidates on any event change; this makes group scheduling fast even though each participant's true availability is computed from the authoritative DB. Second, partial overlap: because attendees are rarely fully free, the engine must report gaps of at least a minimum duration and let the caller decide; tools like a binary interval-tree or a segment-tree per user support \"earliest gap of duration X\" queries in logarithmic time. Concurrency is handled by optimistic versioning — if two people book the same slot simultaneously, one commit wins and the other is told to retry.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Two practical concerns dominate. First, caching: per-user free/busy windows for the next few weeks are expensive to recompute, so the availability engine writes them to a cache and invalidates on any event change; this makes group scheduling fast even though each participant's true availability is computed from the authoritative DB. Second, partial overlap: because attendees are rarely fully free, the engine must report gaps of at least a minimum duration and let the caller decide; tools like a binary interval-tree or a segment-tree per user support \"earliest gap of duration X\" queries in logarithmic time. Concurrency is handled by optimistic versioning — if two people book the same slot simultaneously, one commit wins and the other is told to retry."}]]}]]]}]
5:["$","div","How do you distribute invites and manage RSVPs?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you distribute invites and manage RSVPs?"}],[["$","p","Invites are modeled as attendee records attached to an event. When an organizer creates an event with attendees, the invitation service fans out: for small calendars each attendee gets a reference to the shared event plus an RSVP state (invited, accepted, declined, tentative); for very large audiences the service can create per-attendee copies or rely on shared-events with a counter. Each attendee's calendar needs to reflect the event, so writes propagate to the invitee's data via a sync feed rather than holding a global lock on the event. The RSVP flow is simple: an attendee accepts, the service updates the attendee record, marks the event on the attendee's calendar as busy, and notifies the organizer. Change propagation (reschedule, cancel, add attendee) is done by updating the event and emitting a versioned change that each affected attendee's calendar consumes.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Invites are modeled as attendee records attached to an event. When an organizer creates an event with attendees, the invitation service fans out: for small calendars each attendee gets a reference to the shared event plus an RSVP state (invited, accepted, declined, tentative); for very large audiences the service can create per-attendee copies or rely on shared-events with a counter. Each attendee's calendar needs to reflect the event, so writes propagate to the invitee's data via a sync feed rather than holding a global lock on the event. The RSVP flow is simple: an attendee accepts, the service updates the attendee record, marks the event on the attendee's calendar as busy, and notifies the organizer. Change propagation (reschedule, cancel, add attendee) is done by updating the event and emitting a versioned change that each affected attendee's calendar consumes."}]]}],["$","p","Two failure modes matter. First, an unreliable attendee: RSVP status is tracked per attendee and the organizer gets a summary (X accepted, Y pending) computed by aggregation; reminders for unanswered invites are re-sent by the notifier before the event. Second, partial delivery: invites flow through a retryable queue so a temporarily unavailable mailbox or app doesn't lose the invite, and each client pulls its full event list on reconnect, so the RSVP and invite states reconcile by eventual consistency. The invitation service must also handle privacy — each attendee sees only their own participation and the shared event details, never the organizer's other commitments — which is why free/busy is exposed as busy-time, not event details, to third parties.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Two failure modes matter. First, an unreliable attendee: RSVP status is tracked per attendee and the organizer gets a summary (X accepted, Y pending) computed by aggregation; reminders for unanswered invites are re-sent by the notifier before the event. Second, partial delivery: invites flow through a retryable queue so a temporarily unavailable mailbox or app doesn't lose the invite, and each client pulls its full event list on reconnect, so the RSVP and invite states reconcile by eventual consistency. The invitation service must also handle privacy — each attendee sees only their own participation and the shared event details, never the organizer's other commitments — which is why free/busy is exposed as busy-time, not event details, to third parties."}]]}]]]}]
6:["$","div","How do you handle recurring events?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you handle recurring events?"}],[["$","p","Recurrence is stored as a rule (e.g., `RRULE:FREQ=WEEKLY;INTERVAL=2;BYDAY=TU`) plus an exceptions list — individual instances that were moved, cancelled, or had edits — rather than expanding every occurrence into a row. On query, the service expands the rule into concrete instances for the requested window using a recurrence engine (the iCalendar RRULE expansion algorithm with a bounded horizon, e.g., up to a few years), applies the exceptions, and merges in single-instance events. This keeps storage small and edits cheap: modifying one instance adds an override; modifying the whole series updates the rule and its version. Any overlap and conflict detection runs against the expanded instances for the window of interest.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Recurrence is stored as a rule (e.g., "}],["$","code","1",{"className":"bg-base-300 rounded px-1 font-mono text-sm","children":"RRULE:FREQ=WEEKLY;INTERVAL=2;BYDAY=TU"}],["$","$1","2",{"children":") plus an exceptions list — individual instances that were moved, cancelled, or had edits — rather than expanding every occurrence into a row. On query, the service expands the rule into concrete instances for the requested window using a recurrence engine (the iCalendar RRULE expansion algorithm with a bounded horizon, e.g., up to a few years), applies the exceptions, and merges in single-instance events. This keeps storage small and edits cheap: modifying one instance adds an override; modifying the whole series updates the rule and its version. Any overlap and conflict detection runs against the expanded instances for the window of interest."}]]}],["$","p","The tricky parts are edge cases. A recurring event that spans months must not be fully expanded at write time, but a query needs only the instances overlapping its window, so expansion is a per-request, horizon-bounded computation. Moving or cancelling a single instance must survive the rule update: overrides are keyed by (series_id, original_start) and the recurrence engine checks overrides before emitting an instance. Deleting the series must decide whether to delete all overrides too. Timezone DST transitions shift instance times across boundaries, so expansion is always done in the event's stored timezone, then converted to UTC for the bus-cache and cross-user availability. Because expansion is cheap and stateless, it can run on many instances of the event service; the recurrence engine is pure and deterministic, which also makes it unit-testable in isolation.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"The tricky parts are edge cases. A recurring event that spans months must not be fully expanded at write time, but a query needs only the instances overlapping its window, so expansion is a per-request, horizon-bounded computation. Moving or cancelling a single instance must survive the rule update: overrides are keyed by (series_id, original_start) and the recurrence engine checks overrides before emitting an instance. Deleting the series must decide whether to delete all overrides too. Timezone DST transitions shift instance times across boundaries, so expansion is always done in the event's stored timezone, then converted to UTC for the bus-cache and cross-user availability. Because expansion is cheap and stateless, it can run on many instances of the event service; the recurrence engine is pure and deterministic, which also makes it unit-testable in isolation."}]]}]]]}]
7:["$","div","How do you sync events across devices offline?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you sync events across devices offline?"}],[["$","p","Offline sync uses change-based replication with per-device cursors. Each event and its attachments carry a version (e.g., a logical clock or DB timestamp), and the sync service maintains a change log per user: every create/update/delete appends a change record with a monotonic sequence number. A device tracks the last sequence it saw and pulls the delta since then on reconnect; it also batches local edits into an outbox and pushes them, with conflict resolution — last-write-wins with timestamps or a three-way merge for text fields like descriptions and titles. Clients keep a local cache of the user's calendar, so read-only operations work fully offline and writes queue locally, stamped with a client-generated UUID so retries are idempotent.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Offline sync uses change-based replication with per-device cursors. Each event and its attachments carry a version (e.g., a logical clock or DB timestamp), and the sync service maintains a change log per user: every create/update/delete appends a change record with a monotonic sequence number. A device tracks the last sequence it saw and pulls the delta since then on reconnect; it also batches local edits into an outbox and pushes them, with conflict resolution — last-write-wins with timestamps or a three-way merge for text fields like descriptions and titles. Clients keep a local cache of the user's calendar, so read-only operations work fully offline and writes queue locally, stamped with a client-generated UUID so retries are idempotent."}]]}],["$","p","Conflict handling is the crux. Two edits to the same event from different devices must merge: a simple \"last writer wins\" works for most fields, but changes like moving an event while another device edits the description need field-level merging, so edits are recorded as per-field operations. The sync service serializes the change log per user and hands each client the events it can see (respecting attendee/visibility permissions). For scale, users are sharded across sync servers and the change log is compacted periodically — old records are folded into a snapshot and clients that are too far behind rebuild from the snapshot plus recent deltas. Push updates (via WebSocket or push notifications) give near-real-time convergence, while the pull-on-reconnect path guarantees eventual consistency even if push is missed. Monitoring tracks sync lag and conflict rates per user to catch regressions in the merge logic.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Conflict handling is the crux. Two edits to the same event from different devices must merge: a simple \"last writer wins\" works for most fields, but changes like moving an event while another device edits the description need field-level merging, so edits are recorded as per-field operations. The sync service serializes the change log per user and hands each client the events it can see (respecting attendee/visibility permissions). For scale, users are sharded across sync servers and the change log is compacted periodically — old records are folded into a snapshot and clients that are too far behind rebuild from the snapshot plus recent deltas. Push updates (via WebSocket or push notifications) give near-real-time convergence, while the pull-on-reconnect path guarantees eventual consistency even if push is missed. Monitoring tracks sync lag and conflict rates per user to catch regressions in the merge logic."}]]}]]]}]
8:["$","details",null,{"className":"mt-8","children":[["$","summary",null,{"className":"cursor-pointer font-medium","children":"Diagram source"}],["$","pre",null,{"className":"bg-base-200 mt-2 overflow-x-auto rounded-lg p-4 font-mono text-xs","children":"title: Calendar\nnode user: User [round, icon=browser]\nnode app: Calendar App [icon=browser]\nnode api: API Gateway [icon=server]\nnode event: Event Service [icon=compute]\nnode avail: Availability Engine [icon=search]\nnode invite: Invitation Service [icon=mail]\nnode sync: Sync Service [icon=sync]\nnode notifier: Reminders [icon=message]\nnode db: Events DB [cylinder, icon=database]\nnode cache: Slots Cache [cylinder, icon=cache]\nnode search: Event Search [icon=search]\n\nedge user -> app: create event\nedge app -> api: schedule\nedge api -> event: CRUD\nedge event -> db: persist\nedge event -> avail: find slots\nedge avail -> cache: query\nedge event -> invite: notify guests\nedge invite -> notifier: reminders\nedge sync -> api: pull changes\nedge api -> search: index events\nedge event -> notifier: schedule reminders"}]]}]
9:["$","div",null,{"className":"mt-8","children":["$","$L1a",null,{"text":"title: Calendar\nnode user: User [round, icon=browser]\nnode app: Calendar App [icon=browser]\nnode api: API Gateway [icon=server]\nnode event: Event Service [icon=compute]\nnode avail: Availability Engine [icon=search]\nnode invite: Invitation Service [icon=mail]\nnode sync: Sync Service [icon=sync]\nnode notifier: Reminders [icon=message]\nnode db: Events DB [cylinder, icon=database]\nnode cache: Slots Cache [cylinder, icon=cache]\nnode search: Event Search [icon=search]\n\nedge user -> app: create event\nedge app -> api: schedule\nedge api -> event: CRUD\nedge event -> db: persist\nedge event -> avail: find slots\nedge avail -> cache: query\nedge event -> invite: notify guests\nedge invite -> notifier: reminders\nedge sync -> api: pull changes\nedge api -> search: index events\nedge event -> notifier: schedule reminders","name":"Google Calendar — Scheduling"}]}]
a:["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","async":true}]
b:["$","script","script-1",{"src":"/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js","async":true}]
c:["$","$L1b",null,{"children":["$","$1c",null,{"name":"Next.MetadataOutlet","children":"$@1d"}]}]
f:["$","$1","h",{"children":[null,["$","$L1e",null,{"children":[["$","meta","0",{"charSet":"utf-8"}],["$","meta","1",{"name":"viewport","content":"width=device-width, initial-scale=1"}]]}],["$","div",null,{"hidden":true,"children":["$","$L1f",null,{"children":["$","$1c",null,{"name":"Next.Metadata","children":[["$","title","0",{"children":"Google Calendar — Scheduling — Diagram Examples"}],["$","meta","1",{"name":"description","content":"Event scheduling, availability, invites, recurring events, reminders."}],["$","link","2",{"rel":"manifest","href":"/manifest.json"}],["$","meta","3",{"name":"mobile-web-app-capable","content":"yes"}],["$","meta","4",{"name":"apple-mobile-web-app-title","content":"Diagram"}],["$","meta","5",{"name":"apple-mobile-web-app-status-bar-style","content":"black-translucent"}]]}]}]}],["$","meta",null,{"name":"next-size-adjust","content":""}]]}]
11:["$","$1","c",{"children":[null,["$","$L20",null,{"parallelRouterKey":"children","template":["$","$L21",null,{}]}]]}]
14:["$","$1","c",{"children":[null,["$","$L20",null,{"parallelRouterKey":"children","template":["$","$L21",null,{}]}]]}]
16:["$","$L22",null,{"loading":[["$","div","l",{"className":"flex h-screen items-center justify-center","children":["$","span",null,{"className":"loading loading-spinner loading-lg text-primary"}]}],[],null],"children":["$","$1","c",{"children":[[["$","link","0",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","precedence":"next"}],["$","link","1",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","precedence":"next"}],["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","async":true}]],["$","html",null,{"lang":"en","data-theme":"diagram","className":"inter_537b2856-module__DOofUa__variable jetbrains_mono_da46d059-module__t9WmUG__variable","children":[["$","head",null,{"children":["$","link",null,{"rel":"apple-touch-icon","href":"/icons/icon-192.png"}]}],["$","body",null,{"className":"h-screen overflow-hidden","children":["$","$L20",null,{"parallelRouterKey":"children","error":"$23","errorStyles":[],"errorScripts":[["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/2td1d3x1ljv-f.js","async":true}]],"template":["$","$L21",null,{}],"notFound":[["$","div",null,{"className":"flex h-screen flex-col items-center justify-center gap-4","children":[["$","h1",null,{"className":"text-2xl","children":"404"}],["$","p",null,{"className":"text-sm","children":"This diagram does not exist."}],["$","a",null,{"href":"/","className":"btn btn-primary btn-sm","children":"Back to editor"}]]}],[]]}]}]]}]]}]}]
1d:null
19:true
e:300
e:C
18:0
10:"$undefined"
12:"$undefined"
15:"$undefined"
17:"$undefined"
d:"$undefined"
