1:I[93226,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"LoadingBoundaryProvider"]
2:"$Sreact.fragment"
3:I[93226,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default"]
4:I[46791,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/2td1d3x1ljv-f.js"],"default"]
5:I[15750,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default"]
7:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"OutletBoundary"]
8:"$Sreact.suspense"
b:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"ViewportBoundary"]
d:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"MetadataBoundary"]
f:I[56193,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default",1]
:HL["/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","style"]
:HL["/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","style"]
:HL["/downloads/diagram/_next/static/media/70bc3e132a0a741e-s.p.3t6q91iet4nsy.woff2","font",{"crossOrigin":"","type":"font/woff2"}]
:HL["/downloads/diagram/_next/static/media/83afe278b6a6bb3c-s.p.2bn3s6zvc0dyp.woff2","font",{"crossOrigin":"","type":"font/woff2"}]
a:X
0:{"P":null,"c":["","posts","translation-service",""],"q":"","i":false,"f":[[["",{"children":["posts",{"children":[["slug","translation-service","d",[]],{"children":["__PAGE__",{},"$undefined","$undefined",4608]},"$undefined","$undefined",4608]},"$undefined","$undefined",4608]},"$undefined","$undefined",4628],[["$","$L1",null,{"loading":[["$","div","l",{"className":"flex h-screen items-center justify-center","children":["$","span",null,{"className":"loading loading-spinner loading-lg text-primary"}]}],[],null],"children":["$","$2","c",{"children":[[["$","link","0",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","precedence":"next","crossOrigin":"$undefined","nonce":"$undefined"}],["$","link","1",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","precedence":"next","crossOrigin":"$undefined","nonce":"$undefined"}],["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","async":true,"nonce":"$undefined"}]],["$","html",null,{"lang":"en","data-theme":"diagram","className":"inter_537b2856-module__DOofUa__variable jetbrains_mono_da46d059-module__t9WmUG__variable","children":[["$","head",null,{"children":["$","link",null,{"rel":"apple-touch-icon","href":"/icons/icon-192.png"}]}],["$","body",null,{"className":"h-screen overflow-hidden","children":["$","$L3",null,{"parallelRouterKey":"children","error":"$4","errorStyles":[],"errorScripts":[["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/2td1d3x1ljv-f.js","async":true}]],"template":["$","$L5",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":[["$","div",null,{"className":"flex h-screen flex-col items-center justify-center gap-4","children":[["$","h1",null,{"className":"text-2xl","children":"404"}],["$","p",null,{"className":"text-sm","children":"This diagram does not exist."}],["$","a",null,{"href":"/","className":"btn btn-primary btn-sm","children":"Back to editor"}]]}],[]],"forbidden":"$undefined","unauthorized":"$undefined"}]}]]}]]}]}],{"children":[["$","$2","c",{"children":[null,["$","$L3",null,{"parallelRouterKey":"children","error":"$undefined","errorStyles":"$undefined","errorScripts":"$undefined","template":["$","$L5",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":"$undefined","forbidden":"$undefined","unauthorized":"$undefined"}]]}],{"children":[["$","$2","c",{"children":[null,["$","$L3",null,{"parallelRouterKey":"children","error":"$undefined","errorStyles":"$undefined","errorScripts":"$undefined","template":["$","$L5",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":"$undefined","forbidden":"$undefined","unauthorized":"$undefined"}]]}],{"children":[["$","$2","c",{"children":["$L6",[["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","async":true,"nonce":"$undefined"}],["$","script","script-1",{"src":"/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js","async":true,"nonce":"$undefined"}]],["$","$L7",null,{"children":["$","$8",null,{"name":"Next.MetadataOutlet","children":"$@9"}]}]]}],{},null,false,null]},null,false,"$a"]},null,false,"$a"]},null,false,null],["$","$2","h",{"children":[null,["$","$Lb",null,{"children":"$Lc"}],["$","div",null,{"hidden":true,"children":["$","$Ld",null,{"children":["$","$8",null,{"name":"Next.Metadata","children":"$Le"}]}]}],["$","meta",null,{"name":"next-size-adjust","content":""}]]}],false]],"m":"$undefined","G":["$f",[["$","link","0",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","precedence":"next","crossOrigin":"$undefined","nonce":"$undefined"}],["$","link","1",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","precedence":"next","crossOrigin":"$undefined","nonce":"$undefined"}]]],"S":true,"h":null,"r":"$undefined","s":"$undefined","a":"$undefined","l":"$undefined","p":"$undefined","d":"$undefined","b":"YtoKbkX7y76MytYfCubSL"}
10:I[83070,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js"],""]
a:C
6:["$","div",null,{"className":"h-screen overflow-y-auto","children":["$","main",null,{"className":"mx-auto max-w-3xl px-4 py-10","children":[["$","p",null,{"className":"mb-6 opacity-60","children":["$","$L10",null,{"className":"link","href":"/posts/","children":"← All posts"}]}],["$","article",null,{"children":[["$","header",null,{"className":"mb-8","children":[["$","h1",null,{"className":"text-3xl font-bold","children":"Google Translate — Translation"}],["$","p",null,{"className":"text-base-content/70 mt-2","children":"Text translation, language detection, models, caching."}],["$","p",null,{"className":"text-base-content/50 mt-3 text-sm","children":["Hieu Doan"," · ",["$","span",null,{"className":"capitalize","children":"hard"}]," ·"," ",["$","span",null,{"className":"capitalize","children":"ai"}]]}],["$","div",null,{"className":"mt-3 flex flex-wrap gap-1.5","children":[["$","span","cache",{"className":"badge badge-outline badge-sm","children":"cache"}],["$","span","ml",{"className":"badge badge-outline badge-sm","children":"ml"}],["$","span","translation",{"className":"badge badge-outline badge-sm","children":"translation"}]]}]]}],["$","section",null,{"children":[["$","h2",null,{"className":"mb-2 text-xl font-semibold","children":"Interview Questions"}],["$","ul",null,{"className":"list-disc space-y-1 pl-6","children":[["$","li","Design a translation service",{"children":"Design a translation service"}],["$","li","How do you serve translation models at scale?",{"children":"How do you serve translation models at scale?"}],["$","li","How do you detect source language automatically?",{"children":"How do you detect source language automatically?"}],["$","li","How do you translate documents while preserving format?",{"children":"How do you translate documents while preserving format?"}],["$","li","How do you cache translations safely?",{"children":"How do you cache translations safely?"}]]}]]}],["$","section",null,{"className":"mt-8","children":[["$","h2",null,{"className":"mb-4 text-xl font-semibold","children":"Answers"}],["$","div",null,{"className":"space-y-6","children":[["$","div","Design a translation service",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"Design a translation service"}],[["$","p","A translation service accepts text in one language and returns it in another, and the hard problems are all about the model serving layer. The client sends text through the API Gateway, which first asks the Language Detect service to identify the source language when it is not supplied, then passes the request to the Translation Engine. The engine consults the Translation Cache for an exact or fuzzy match, and on a miss it performs a neural model inference through the Model Serving tier. A Format Preserver handles the special case of structured documents, keeping layout and markup intact while only the natural-language content is translated. Batch jobs flow through the Batch Queue, and the Usage Tracking service logs every translation for billing and model improvement.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"A translation service accepts text in one language and returns it in another, and the hard problems are all about the model serving layer. The client sends text through the API Gateway, which first asks the Language Detect service to identify the source language when it is not supplied, then passes the request to the Translation Engine. The engine consults the Translation Cache for an exact or fuzzy match, and on a miss it performs a neural model inference through the Model Serving tier. A Format Preserver handles the special case of structured documents, keeping layout and markup intact while only the natural-language content is translated. Batch jobs flow through the Batch Queue, and the Usage Tracking service logs every translation for billing and model improvement."}]]}],"$L11","$L12"]]}],"$L13","$L14","$L15","$L16"]}]]}],"$L17"]}],"$L18"]}]}]
1d:I[94844,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js"],"default"]
11:["$","p","The architectural heart is the separation between a stateless, latency critical inference path and a durable, asynchronous accounting and training path. Individual text translation must return in well under a second, so it runs against a dedicated GPU inference fleet sized for peak load. Everything that can be decided without the model is decided earlier: the cache absorbs repeated phrases, and language detection uses a fast statistical model before any heavy neural work. The result is that only genuinely novel text reaches the expensive inference tier, which both controls cost and keeps latency predictable.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"The architectural heart is the separation between a stateless, latency critical inference path and a durable, asynchronous accounting and training path. Individual text translation must return in well under a second, so it runs against a dedicated GPU inference fleet sized for peak load. Everything that can be decided without the model is decided earlier: the cache absorbs repeated phrases, and language detection uses a fast statistical model before any heavy neural work. The result is that only genuinely novel text reaches the expensive inference tier, which both controls cost and keeps latency predictable."}]]}]
12:["$","p","Quality is the product, so the service must balance three currencies: speed, cost, and accuracy. Latency drives the serving architecture, GPU cost drives caching and batching, and accuracy drives model choice and continuous evaluation. The design deliberately keeps the model serving layer pluggable, because models are retrained and replaced frequently. A feedback loop collects user corrections and system-sampled evaluations to measure quality drift per language pair, and a deployment pipeline can roll out a new model to a fraction of traffic before promoting it broadly.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"Quality is the product, so the service must balance three currencies: speed, cost, and accuracy. Latency drives the serving architecture, GPU cost drives caching and batching, and accuracy drives model choice and continuous evaluation. The design deliberately keeps the model serving layer pluggable, because models are retrained and replaced frequently. A feedback loop collects user corrections and system-sampled evaluations to measure quality drift per language pair, and a deployment pipeline can roll out a new model to a fraction of traffic before promoting it broadly."}]]}]
13:["$","div","How do you serve translation models at scale?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you serve translation models at scale?"}],[["$","p","Model serving is the cost center of the system, so it is engineered for throughput per GPU. The Translation Engine does not talk to one monolithic model; it talks to a Model Serving tier that runs a family of models, one per language pair or language family, and routes each request to the smallest model that can handle it. Models are batched at the serving layer: a GPU decoder is dramatically more efficient when it processes many sentences in one batch, so the engine buffers requests for a few milliseconds and packs them into a single inference call. This adds a controlled amount of latency in exchange for an order of magnitude better hardware utilization.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"Model serving is the cost center of the system, so it is engineered for throughput per GPU. The Translation Engine does not talk to one monolithic model; it talks to a Model Serving tier that runs a family of models, one per language pair or language family, and routes each request to the smallest model that can handle it. Models are batched at the serving layer: a GPU decoder is dramatically more efficient when it processes many sentences in one batch, so the engine buffers requests for a few milliseconds and packs them into a single inference call. This adds a controlled amount of latency in exchange for an order of magnitude better hardware utilization."}]]}],["$","p","The fleet is sharded by language. Popular pairs such as English to Spanish get dedicated capacity, while long-tail pairs share pools, so a spike in one language does not steal capacity from another. Autoscaling is driven by queue depth at the serving tier rather than CPU, because the correct signal is how long inference waits behind the batch. Each model version runs alongside the previous one during rollout, with a shadow or canary phase, so a regression in translation quality can be caught and rolled back without affecting all users.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"The fleet is sharded by language. Popular pairs such as English to Spanish get dedicated capacity, while long-tail pairs share pools, so a spike in one language does not steal capacity from another. Autoscaling is driven by queue depth at the serving tier rather than CPU, because the correct signal is how long inference waits behind the batch. Each model version runs alongside the previous one during rollout, with a shadow or canary phase, so a regression in translation quality can be caught and rolled back without affecting all users."}]]}],["$","p","Serving also relies on a hot model cache. Common sentences, greetings, and high-frequency business phrases are cached in front of the model, and a hash of the normalized source and target language indexes the cache. The cache is most effective for repeated strings such as UI text, product descriptions, and customer replies. Because models improve, cache entries are versioned by model ID so a new model invalidates the cache naturally. The inference tier is the only place the expensive work happens, and every layer in front of it exists to ensure the model is asked to do as little as possible.",{"className":"mb-2 leading-relaxed","children":["$L19"]}]]]}]
14:["$","div","How do you detect source language automatically?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you detect source language automatically?"}],[["$","p","Language detection runs before translation and needs to be near-instant and cheap. The detection model is statistical rather than neural: it uses character n-gram distributions, because each language has a distinctive profile of which letter sequences are common, plus a small list of highly discriminative function words. The detector scores the input text against every language model it knows and returns the highest-probability match with a confidence score. For very short inputs, where character statistics are weak, the detector combines script detection, the character set itself often identifies the language family, with fallback rules and a list of common words.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"Language detection runs before translation and needs to be near-instant and cheap. The detection model is statistical rather than neural: it uses character n-gram distributions, because each language has a distinctive profile of which letter sequences are common, plus a small list of highly discriminative function words. The detector scores the input text against every language model it knows and returns the highest-probability match with a confidence score. For very short inputs, where character statistics are weak, the detector combines script detection, the character set itself often identifies the language family, with fallback rules and a list of common words."}]]}],["$","p","The real challenge is ambiguity. \"Hello\" is English, but single words and proper nouns can be ambiguous across languages, and code-switched text mixes languages within one sentence. The detector is therefore trained to produce a confidence value, and low-confidence results are handled explicitly rather than guessed. When the user supplies a source language, detection is skipped entirely, and the API returns the detected language alongside the translation so clients can confirm it. Mixing within a document is handled at the sentence level, which is a good unit for both detection and translation.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"The real challenge is ambiguity. \"Hello\" is English, but single words and proper nouns can be ambiguous across languages, and code-switched text mixes languages within one sentence. The detector is therefore trained to produce a confidence value, and low-confidence results are handled explicitly rather than guessed. When the user supplies a source language, detection is skipped entirely, and the API returns the detected language alongside the translation so clients can confirm it. Mixing within a document is handled at the sentence level, which is a good unit for both detection and translation."}]]}],["$","p","Detection must also degrade gracefully under adversarial or unusual input. Emoji, URLs, brand names, and markup add noise, so text is normalized before scoring: markup is stripped, URLs are removed, and casing is ignored. The detector is trained on the same noisy distribution it sees in production, including tweets and chat messages, not clean prose. A special case is short text from a language that shares a script, such as Serbian and Croatian, where n-gram models are very close; for these pairs the service may report a family-level language and let the translation model handle the ambiguity.",{"className":"mb-2 leading-relaxed","children":["$L1a"]}]]]}]
15:["$","div","How do you translate documents while preserving format?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you translate documents while preserving format?"}],[["$","p","Translating a document is fundamentally different from translating a plain string, because the content is interleaved with structure that must survive translation. The Format Preserver first parses the document into a tree of blocks: headings, paragraphs, tables, lists, and code spans, with their formatting attributes. Only the leaf text nodes are extracted as segments for translation, while the surrounding structure is held aside as a template. After each text segment is translated, it is reinserted into its original position in the tree, and the document is serialized back to its native format. The output is a translated document that is structurally identical to the input.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"Translating a document is fundamentally different from translating a plain string, because the content is interleaved with structure that must survive translation. The Format Preserver first parses the document into a tree of blocks: headings, paragraphs, tables, lists, and code spans, with their formatting attributes. Only the leaf text nodes are extracted as segments for translation, while the surrounding structure is held aside as a template. After each text segment is translated, it is reinserted into its original position in the tree, and the document is serialized back to its native format. The output is a translated document that is structurally identical to the input."}]]}],["$","p","Markup and placeholders are the subtle danger. URLs, HTML entities, template variables, and inline code must not be translated, so the parser marks them as protected spans before segmentation. The translation engine receives placeholder tokens in place of these spans and restores them afterward, which also protects the model from being confused by embedded syntax. The same mechanism handles formatting boundaries that the model must not cross, such as a sentence split across a bold tag, where the translator is forced to keep the formatting split in the same place. Length constraints are handled at this layer too, since translated text is often longer or shorter than the source and must still fit table columns.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"Markup and placeholders are the subtle danger. URLs, HTML entities, template variables, and inline code must not be translated, so the parser marks them as protected spans before segmentation. The translation engine receives placeholder tokens in place of these spans and restores them afterward, which also protects the model from being confused by embedded syntax. The same mechanism handles formatting boundaries that the model must not cross, such as a sentence split across a bold tag, where the translator is forced to keep the formatting split in the same place. Length constraints are handled at this layer too, since translated text is often longer or shorter than the source and must still fit table columns."}]]}],["$","p","The service batches structured translation asynchronously. Large documents are split into many segments, which are processed through the same batch path as other bulk translation and reassembled by the format layer once all segments complete. Long-running jobs return a job identifier and the client polls for completion. Alignment must be preserved end to end, so each segment carries a stable identifier that maps it back to its position in the document tree. The final check is a round-trip validation that every source segment has exactly one translated counterpart, catching dropped or duplicated segments before the document is released.",{"className":"mb-2 leading-relaxed","children":["$L1b"]}]]]}]
16:["$","div","How do you cache translations safely?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you cache translations safely?"}],[["$","p","Caching translations saves money but risks serving stale or wrong output, so the cache is designed conservatively. The cache key is the normalized source text, source language, target language, and model version, and only high-confidence translations are written into it. A phrase that the model produced with low confidence is never cached, because serving that guess to thousands of users compounds one weak inference. The Translation Cache is a distributed cache with a TTL, and entries are versioned so a model upgrade invalidates the old translations without a manual flush. Content that is naturally cacheable, UI strings, product names, and templates, dominates the hit rate while free-form user text rarely hits.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"Caching translations saves money but risks serving stale or wrong output, so the cache is designed conservatively. The cache key is the normalized source text, source language, target language, and model version, and only high-confidence translations are written into it. A phrase that the model produced with low confidence is never cached, because serving that guess to thousands of users compounds one weak inference. The Translation Cache is a distributed cache with a TTL, and entries are versioned so a model upgrade invalidates the old translations without a manual flush. Content that is naturally cacheable, UI strings, product names, and templates, dominates the hit rate while free-form user text rarely hits."}]]}],["$","p","Safety also means not caching content that must not be retained. Sensitive text, personal messages, and legally protected content are handled differently: the service marks requests with a no-store policy, and compliance rules prevent certain tenants from being written to the cache at all. Cache reads are exact-match on normalized text, and normalization is deliberate, lowercasing and trimming, so subtle differences do not produce near-miss collisions that serve the wrong translation for a slightly different input. Hash collisions are resolved by storing the full key with the value and verifying it on every read.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"Safety also means not caching content that must not be retained. Sensitive text, personal messages, and legally protected content are handled differently: the service marks requests with a no-store policy, and compliance rules prevent certain tenants from being written to the cache at all. Cache reads are exact-match on normalized text, and normalization is deliberate, lowercasing and trimming, so subtle differences do not produce near-miss collisions that serve the wrong translation for a slightly different input. Hash collisions are resolved by storing the full key with the value and verifying it on every read."}]]}],["$","p","Cache poisoning is guarded against because the cache is write-only from the translation path. Only the Translation Engine can write entries, and it writes only from verified model output, so a client cannot inject a value. The cache is also monitored for hit-rate and correctness drift per language pair; a sudden change in either triggers an investigation before the model or cache is trusted again. TTLs are chosen per content class, shorter for slang and idiomatic phrases that change meaning quickly, longer for technical terminology, and the whole layer is designed so that a total cache failure degrades to slower service, never to incorrect service.",{"className":"mb-2 leading-relaxed","children":["$L1c"]}]]]}]
17:["$","details",null,{"className":"mt-8","children":[["$","summary",null,{"className":"cursor-pointer font-medium","children":"Diagram source"}],["$","pre",null,{"className":"bg-base-200 mt-2 overflow-x-auto rounded-lg p-4 font-mono text-xs","children":"title: Translation Service\nnode user: User [round, icon=browser]\nnode app: Translate App [icon=browser]\nnode gateway: API Gateway [icon=server]\nnode detect: Language Detect [icon=search]\nnode translate: Translation Engine [icon=compute]\nnode models: Model Serving [icon=cloud]\nnode format: Format Preserver [icon=file]\nnode cache: Translation Cache [cylinder, icon=cache]\nnode queue: Batch Queue [icon=queue]\nnode audit: Usage Tracking [icon=compute]\nnode db: Usage DB [cylinder, icon=database]\n\nedge user -> app: input text\nedge app -> gateway: request\nedge gateway -> detect: language\nedge gateway -> translate: translate\nedge translate -> models: infer\nedge translate -> cache: check\nedge models -> cache: store\nedge gateway -> format: document\nedge format -> app: output\nedge gateway -> queue: batch\nedge queue -> audit: log\nedge audit -> db: store"}]]}]
18:["$","div",null,{"className":"mt-8","children":["$","$L1d",null,{"text":"title: Translation Service\nnode user: User [round, icon=browser]\nnode app: Translate App [icon=browser]\nnode gateway: API Gateway [icon=server]\nnode detect: Language Detect [icon=search]\nnode translate: Translation Engine [icon=compute]\nnode models: Model Serving [icon=cloud]\nnode format: Format Preserver [icon=file]\nnode cache: Translation Cache [cylinder, icon=cache]\nnode queue: Batch Queue [icon=queue]\nnode audit: Usage Tracking [icon=compute]\nnode db: Usage DB [cylinder, icon=database]\n\nedge user -> app: input text\nedge app -> gateway: request\nedge gateway -> detect: language\nedge gateway -> translate: translate\nedge translate -> models: infer\nedge translate -> cache: check\nedge models -> cache: store\nedge gateway -> format: document\nedge format -> app: output\nedge gateway -> queue: batch\nedge queue -> audit: log\nedge audit -> db: store","name":"Google Translate — Translation"}]}]
19:["$","$2","0",{"children":"Serving also relies on a hot model cache. Common sentences, greetings, and high-frequency business phrases are cached in front of the model, and a hash of the normalized source and target language indexes the cache. The cache is most effective for repeated strings such as UI text, product descriptions, and customer replies. Because models improve, cache entries are versioned by model ID so a new model invalidates the cache naturally. The inference tier is the only place the expensive work happens, and every layer in front of it exists to ensure the model is asked to do as little as possible."}]
1a:["$","$2","0",{"children":"Detection must also degrade gracefully under adversarial or unusual input. Emoji, URLs, brand names, and markup add noise, so text is normalized before scoring: markup is stripped, URLs are removed, and casing is ignored. The detector is trained on the same noisy distribution it sees in production, including tweets and chat messages, not clean prose. A special case is short text from a language that shares a script, such as Serbian and Croatian, where n-gram models are very close; for these pairs the service may report a family-level language and let the translation model handle the ambiguity."}]
1b:["$","$2","0",{"children":"The service batches structured translation asynchronously. Large documents are split into many segments, which are processed through the same batch path as other bulk translation and reassembled by the format layer once all segments complete. Long-running jobs return a job identifier and the client polls for completion. Alignment must be preserved end to end, so each segment carries a stable identifier that maps it back to its position in the document tree. The final check is a round-trip validation that every source segment has exactly one translated counterpart, catching dropped or duplicated segments before the document is released."}]
1c:["$","$2","0",{"children":"Cache poisoning is guarded against because the cache is write-only from the translation path. Only the Translation Engine can write entries, and it writes only from verified model output, so a client cannot inject a value. The cache is also monitored for hit-rate and correctness drift per language pair; a sudden change in either triggers an investigation before the model or cache is trusted again. TTLs are chosen per content class, shorter for slang and idiomatic phrases that change meaning quickly, longer for technical terminology, and the whole layer is designed so that a total cache failure degrades to slower service, never to incorrect service."}]
c:[["$","meta","0",{"charSet":"utf-8"}],["$","meta","1",{"name":"viewport","content":"width=device-width, initial-scale=1"}]]
9:null
e:[["$","title","0",{"children":"Google Translate — Translation — Diagram Examples"}],["$","meta","1",{"name":"description","content":"Text translation, language detection, models, caching."}],["$","link","2",{"rel":"manifest","href":"/manifest.json","crossOrigin":"$undefined"}],["$","meta","3",{"name":"mobile-web-app-capable","content":"yes"}],["$","meta","4",{"name":"apple-mobile-web-app-title","content":"Diagram"}],["$","meta","5",{"name":"apple-mobile-web-app-status-bar-style","content":"black-translucent"}]]
