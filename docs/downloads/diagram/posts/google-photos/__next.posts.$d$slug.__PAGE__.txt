1:"$Sreact.fragment"
2:I[83070,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js"],""]
e:X
13:X
13:C
0:{"buildId":"YtoKbkX7y76MytYfCubSL","data":[{"rsc":["$","$1","c",{"children":[["$","div",null,{"className":"h-screen overflow-y-auto","children":["$","main",null,{"className":"mx-auto max-w-3xl px-4 py-10","children":[["$","p",null,{"className":"mb-6 opacity-60","children":["$","$L2",null,{"className":"link","href":"/posts/","children":"← All posts"}]}],["$","article",null,{"children":[["$","header",null,{"className":"mb-8","children":[["$","h1",null,{"className":"text-3xl font-bold","children":"Google Photos — Photo Library"}],["$","p",null,{"className":"text-base-content/70 mt-2","children":"Photo upload, dedupe, thumbnails, face grouping, sharing."}],["$","p",null,{"className":"text-base-content/50 mt-3 text-sm","children":["Hieu Doan"," · ",["$","span",null,{"className":"capitalize","children":"medium"}]," ·"," ",["$","span",null,{"className":"capitalize","children":"media"}]]}],["$","div",null,{"className":"mt-3 flex flex-wrap gap-1.5","children":[["$","span","photo",{"className":"badge badge-outline badge-sm","children":"photo"}]]}]]}],["$","section",null,{"children":[["$","h2",null,{"className":"mb-2 text-xl font-semibold","children":"Interview Questions"}],["$","ul",null,{"className":"list-disc space-y-1 pl-6","children":[["$","li","Design a photo backup and sharing service",{"children":"Design a photo backup and sharing service"}],["$","li","How do you handle large uploads and duplicate detection?",{"children":"How do you handle large uploads and duplicate detection?"}],["$","li","How do you organize photos by time, location, and people?",{"children":"How do you organize photos by time, location, and people?"}],["$","li","How do you generate and serve thumbnails at scale?",{"children":"How do you generate and serve thumbnails at scale?"}],["$","li","How do you design sharing and permissions?",{"children":"How do you design sharing and permissions?"}]]}]]}],["$","section",null,{"className":"mt-8","children":[["$","h2",null,{"className":"mb-4 text-xl font-semibold","children":"Answers"}],["$","div",null,{"className":"space-y-6","children":[["$","div","Design a photo backup and sharing service",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"Design a photo backup and sharing service"}],[["$","p","The service has three workloads: a high-throughput upload path, a processing pipeline that enriches each photo, and a fast browse/serve path. Uploads stream to an object storage layer with resumable multipart transfer, then enqueue a processing job; the processing pipeline generates thumbnails and multiple resolution variants, extracts EXIF metadata, runs face recognition and scene tagging, and records everything into a metadata store. Browse goes through the API gateway to a catalog service that reads the metadata store and serves thumbnails from a thumbnail cache backed by object storage. Sharing is a separate permission layer: albums group photos, and share links or collaborator grants control read access for each item.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"The service has three workloads: a high-throughput upload path, a processing pipeline that enriches each photo, and a fast browse/serve path. Uploads stream to an object storage layer with resumable multipart transfer, then enqueue a processing job; the processing pipeline generates thumbnails and multiple resolution variants, extracts EXIF metadata, runs face recognition and scene tagging, and records everything into a metadata store. Browse goes through the API gateway to a catalog service that reads the metadata store and serves thumbnails from a thumbnail cache backed by object storage. Sharing is a separate permission layer: albums group photos, and share links or collaborator grants control read access for each item."}]]}],"$L3"]]}],"$L4","$L5","$L6","$L7"]}]]}],"$L8"]}],"$L9"]}]}],["$La","$Lb"],"$Lc"]}],"isPartial":"$@d","staleTime":"$e","varyParams":null},{"rsc":"$Lf","isPartial":"$@10","staleTime":"$e","varyParams":null},{"rsc":"$L11","isPartial":"$@12","staleTime":"$e","varyParams":"$13"},{"rsc":"$L14","isPartial":"$@15","staleTime":"$e","varyParams":"$13"},{"rsc":"$L16","isPartial":"$@17","staleTime":"$e","varyParams":null}],"isUpgradeableISRFallback":false,"a":"$@18","rootVaryParams":null,"needsRuntimeRequest":"$@19"}
1a:I[94844,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js"],"default"]
1b:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"OutletBoundary"]
1c:"$Sreact.suspense"
1e:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"ViewportBoundary"]
1f:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"MetadataBoundary"]
20:I[93226,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default"]
21:I[15750,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default"]
22:I[93226,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"LoadingBoundaryProvider"]
23:I[46791,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/2td1d3x1ljv-f.js"],"default"]
:HL["/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","style"]
:HL["/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","style"]
3:["$","p","Storage and metadata are separated deliberately. The original image bytes live in object storage addressed by content hash, so identical photos dedupe to a single blob; the metadata store holds per-photo rows (owner, captured-at, location, tags, face IDs, album membership, visibility) in a sharded database, with a search index for queries like \"photos of person X in 2023.\" This split means browse traffic never touches the raw blobs — it reads metadata and streams low-res variants — while originals are fetched only for download or full-resolution view. The system must handle billions of photos, so the catalog is partitioned by owner, writes are batched, and the processing queue is elastic: encode and tagging workers scale with backlog, and everything is idempotent so a failed job can be retried safely.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Storage and metadata are separated deliberately. The original image bytes live in object storage addressed by content hash, so identical photos dedupe to a single blob; the metadata store holds per-photo rows (owner, captured-at, location, tags, face IDs, album membership, visibility) in a sharded database, with a search index for queries like \"photos of person X in 2023.\" This split means browse traffic never touches the raw blobs — it reads metadata and streams low-res variants — while originals are fetched only for download or full-resolution view. The system must handle billions of photos, so the catalog is partitioned by owner, writes are batched, and the processing queue is elastic: encode and tagging workers scale with backlog, and everything is idempotent so a failed job can be retried safely."}]]}]
4:["$","div","How do you handle large uploads and duplicate detection?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you handle large uploads and duplicate detection?"}],[["$","p","Large photos (and videos) are uploaded with resumable chunked transfer: the client slices the file into fixed-size parts, uploads each with a part ID, and the upload service reassembles them on the server side or signs a direct-to-storage path. A failed chunk retries without re-sending the whole file, and a session token tracks progress so an interrupted phone upload resumes later. Deduplication is content-based: the client computes a hash (e.g., SHA-256) of the full file and the server maintains a hash → blob map in a dedup index; if the hash already exists under the same owner, the upload records a new photo row referencing the existing blob and skips the bytes — a huge saving because the same vacation photo often arrives via camera, WhatsApp, and screenshots. Across owners, storage can be shared at the blob level with per-owner metadata rows and reference counting.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Large photos (and videos) are uploaded with resumable chunked transfer: the client slices the file into fixed-size parts, uploads each with a part ID, and the upload service reassembles them on the server side or signs a direct-to-storage path. A failed chunk retries without re-sending the whole file, and a session token tracks progress so an interrupted phone upload resumes later. Deduplication is content-based: the client computes a hash (e.g., SHA-256) of the full file and the server maintains a hash → blob map in a dedup index; if the hash already exists under the same owner, the upload records a new photo row referencing the existing blob and skips the bytes — a huge saving because the same vacation photo often arrives via camera, WhatsApp, and screenshots. Across owners, storage can be shared at the blob level with per-owner metadata rows and reference counting."}]]}],["$","p","Racing uploads and hash collisions are the edge cases. Two concurrent uploads of the same file must resolve to one blob, so the dedup check happens inside an atomic insert with a unique hash key — the loser becomes a second reference rather than a second copy. For videos and RAW files the same flow applies, with the upload service holding a manifest of parts until the final \"complete\" call. Because hashes are computed client-side, the server never trusts them blindly: a verification step can sample bytes on first write, and corrupt files are rejected at the validate stage before they are promoted to the visible library. The upload path is also throttled and queued so a full-library backup doesn't starve other traffic.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Racing uploads and hash collisions are the edge cases. Two concurrent uploads of the same file must resolve to one blob, so the dedup check happens inside an atomic insert with a unique hash key — the loser becomes a second reference rather than a second copy. For videos and RAW files the same flow applies, with the upload service holding a manifest of parts until the final \"complete\" call. Because hashes are computed client-side, the server never trusts them blindly: a verification step can sample bytes on first write, and corrupt files are rejected at the validate stage before they are promoted to the visible library. The upload path is also throttled and queued so a full-library backup doesn't starve other traffic."}]]}]]]}]
5:["$","div","How do you organize photos by time, location, and people?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you organize photos by time, location, and people?"}],[["$","p","Organization is metadata-driven. Capture time comes from EXIF (falling back to file timestamp); location comes from GPS coordinates in EXIF that are reverse-geocoded into city/country labels; people come from a face recognition model. The processing pipeline runs face detection to crop a normalized face region, embeds it into a feature vector, and clusters vectors by identity using an offline clustering job (e.g., hierarchical or agglomerative clustering on embeddings, re-run periodically as new photos arrive). Each cluster becomes a person entity in a face index, and the user confirms or merges clusters through the UI, which feeds the labels back as training signal. Scene tags (sunset, beach, dog) come from a parallel classifier on the thumbnail.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Organization is metadata-driven. Capture time comes from EXIF (falling back to file timestamp); location comes from GPS coordinates in EXIF that are reverse-geocoded into city/country labels; people come from a face recognition model. The processing pipeline runs face detection to crop a normalized face region, embeds it into a feature vector, and clusters vectors by identity using an offline clustering job (e.g., hierarchical or agglomerative clustering on embeddings, re-run periodically as new photos arrive). Each cluster becomes a person entity in a face index, and the user confirms or merges clusters through the UI, which feeds the labels back as training signal. Scene tags (sunset, beach, dog) come from a parallel classifier on the thumbnail."}]]}],["$","p","Queries combine these facets. The catalog service exposes the photo rows to a search index so \"dogs in 2021 near the beach\" is a structured query over tags, time range, and geohash. The trade-off is between online classification (low latency, per-upload cost) and offline clustering (batch, global consistency). Face clustering is necessarily offline and can be slow, so the UI shows a \"recently added\" set immediately and the full People view fills in as clusters land. Duplicates and near-duplicates (same scene, slightly different angle) are handled by a separate similarity pass that groups them, letting the user clean up. All of this metadata is derived data — it can be regenerated by reprocessing, so the original photos remain the source of truth and new models re-run over the library without rewriting blobs.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Queries combine these facets. The catalog service exposes the photo rows to a search index so \"dogs in 2021 near the beach\" is a structured query over tags, time range, and geohash. The trade-off is between online classification (low latency, per-upload cost) and offline clustering (batch, global consistency). Face clustering is necessarily offline and can be slow, so the UI shows a \"recently added\" set immediately and the full People view fills in as clusters land. Duplicates and near-duplicates (same scene, slightly different angle) are handled by a separate similarity pass that groups them, letting the user clean up. All of this metadata is derived data — it can be regenerated by reprocessing, so the original photos remain the source of truth and new models re-run over the library without rewriting blobs."}]]}]]]}]
6:["$","div","How do you generate and serve thumbnails at scale?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you generate and serve thumbnails at scale?"}],[["$","p","Thumbnail generation is a classic write-once, read-many pattern handled by a processing pipeline. After upload, a job enqueues the photo; a thumbnail worker decodes the image and renders multiple variants (e.g., 256px, 512px, 1080px, full-view) into object storage, then writes a manifest row into the metadata store. Because decode is CPU-heavy, workers scale horizontally with the queue and the work is partitioned so big bursts (a phone dumping 5,000 photos) drain predictably; a pyramid of progressively larger variants lets the client request exactly the size it needs. Serving is CDN-first: thumbnails live in object storage behind a CDN with long TTLs and cache keys that include the variant size, so repeated scrolls and shared links hit edge caches and never touch compute.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Thumbnail generation is a classic write-once, read-many pattern handled by a processing pipeline. After upload, a job enqueues the photo; a thumbnail worker decodes the image and renders multiple variants (e.g., 256px, 512px, 1080px, full-view) into object storage, then writes a manifest row into the metadata store. Because decode is CPU-heavy, workers scale horizontally with the queue and the work is partitioned so big bursts (a phone dumping 5,000 photos) drain predictably; a pyramid of progressively larger variants lets the client request exactly the size it needs. Serving is CDN-first: thumbnails live in object storage behind a CDN with long TTLs and cache keys that include the variant size, so repeated scrolls and shared links hit edge caches and never touch compute."}]]}],["$","p","The hard problem is latency on cold paths. The client wants to scroll instantly while thumbnails are still being generated, so the UI streams low-res placeholders (a tiny embedded preview or the first generated variant) and upgrades as variants land; the metadata store records generation state per photo. A thumbnail cache (an in-memory/edge cache in front of storage) absorbs the hottest items, and cache-miss storms on a newly uploaded batch are smoothed by generating on-demand as a fallback for rarely viewed originals. Trade-offs: generating every variant for every photo wastes compute, so a size-on-demand policy is used for full-view originals, while only the grid-size thumbnails are pre-generated. Monitoring tracks encode throughput, queue backlog, and thumbnail cache hit rate to keep scroll latency under budget.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"The hard problem is latency on cold paths. The client wants to scroll instantly while thumbnails are still being generated, so the UI streams low-res placeholders (a tiny embedded preview or the first generated variant) and upgrades as variants land; the metadata store records generation state per photo. A thumbnail cache (an in-memory/edge cache in front of storage) absorbs the hottest items, and cache-miss storms on a newly uploaded batch are smoothed by generating on-demand as a fallback for rarely viewed originals. Trade-offs: generating every variant for every photo wastes compute, so a size-on-demand policy is used for full-view originals, while only the grid-size thumbnails are pre-generated. Monitoring tracks encode throughput, queue backlog, and thumbnail cache hit rate to keep scroll latency under budget."}]]}]]]}]
7:["$","div","How do you design sharing and permissions?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you design sharing and permissions?"}],[["$","p","Sharing is an access-control layer over otherwise private photos. Each photo and album carries an ACL: owner, plus granted collaborators and share links. A share service issues two kinds of grants — user-scoped (a friend's account ID with viewer/contributor roles) and link-scoped (an opaque token URL that grants anyone with the link read or contribute access) — and the catalog service filters every read against the ACL. Sharing is post-composed: a shared album is not a copy of photos but a view that references items by ID, so when the owner edits or deletes a photo, the effect is visible to everyone with access, which is the expected behavior in a photo service.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Sharing is an access-control layer over otherwise private photos. Each photo and album carries an ACL: owner, plus granted collaborators and share links. A share service issues two kinds of grants — user-scoped (a friend's account ID with viewer/contributor roles) and link-scoped (an opaque token URL that grants anyone with the link read or contribute access) — and the catalog service filters every read against the ACL. Sharing is post-composed: a shared album is not a copy of photos but a view that references items by ID, so when the owner edits or deletes a photo, the effect is visible to everyone with access, which is the expected behavior in a photo service."}]]}],["$","p","Security and scale trade-offs follow. Permissions are cached with the session and enforced at the API layer; the metadata store and search index are filtered with the same ACL so sharing can't leak via search. Token links are the tricky surface: they are shareable by forwarding, so the system supports link expiration, revoke-by-reissue (rotating the token invalidates old links), and abuse detection on unusual download spikes. When an owner is removed from an album or deletes their account, a revocation sweep updates the grants. Because ACL checks happen per photo read, hot shared albums are cached along with their grant lists, and the share service keeps an audit log of who granted access to whom — the compliance record for \"did anyone see this photo\" queries. The invariant is that object storage blobs are never directly reachable; every read passes through the permission layer.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Security and scale trade-offs follow. Permissions are cached with the session and enforced at the API layer; the metadata store and search index are filtered with the same ACL so sharing can't leak via search. Token links are the tricky surface: they are shareable by forwarding, so the system supports link expiration, revoke-by-reissue (rotating the token invalidates old links), and abuse detection on unusual download spikes. When an owner is removed from an album or deletes their account, a revocation sweep updates the grants. Because ACL checks happen per photo read, hot shared albums are cached along with their grant lists, and the share service keeps an audit log of who granted access to whom — the compliance record for \"did anyone see this photo\" queries. The invariant is that object storage blobs are never directly reachable; every read passes through the permission layer."}]]}]]]}]
8:["$","details",null,{"className":"mt-8","children":[["$","summary",null,{"className":"cursor-pointer font-medium","children":"Diagram source"}],["$","pre",null,{"className":"bg-base-200 mt-2 overflow-x-auto rounded-lg p-4 font-mono text-xs","children":"title: Photo Library\nnode user: User [round, icon=browser]\nnode app: Photos App [icon=browser]\nnode api: API Gateway [icon=server]\nnode upload: Upload Service [icon=cloud]\nnode storage: Object Storage [cylinder, icon=database]\nnode face: Face Recognition [icon=compute]\nnode tag: Tagging Service [icon=search]\nnode album: Album Service [icon=file]\nnode share: Share Service [icon=users]\nnode cache: Thumbnail Cache [cylinder, icon=cache]\nnode queue: Processing Queue [icon=queue]\n\nedge user -> app: upload photos\nedge app -> upload: stream\nedge upload -> storage: store\nedge upload -> queue: enqueue\nedge queue -> face: detect faces\nedge queue -> tag: classify\nedge face -> tag: labels\nedge app -> api: browse\nedge api -> album: group\nedge api -> share: share link\nedge api -> cache: thumbnails"}]]}]
9:["$","div",null,{"className":"mt-8","children":["$","$L1a",null,{"text":"title: Photo Library\nnode user: User [round, icon=browser]\nnode app: Photos App [icon=browser]\nnode api: API Gateway [icon=server]\nnode upload: Upload Service [icon=cloud]\nnode storage: Object Storage [cylinder, icon=database]\nnode face: Face Recognition [icon=compute]\nnode tag: Tagging Service [icon=search]\nnode album: Album Service [icon=file]\nnode share: Share Service [icon=users]\nnode cache: Thumbnail Cache [cylinder, icon=cache]\nnode queue: Processing Queue [icon=queue]\n\nedge user -> app: upload photos\nedge app -> upload: stream\nedge upload -> storage: store\nedge upload -> queue: enqueue\nedge queue -> face: detect faces\nedge queue -> tag: classify\nedge face -> tag: labels\nedge app -> api: browse\nedge api -> album: group\nedge api -> share: share link\nedge api -> cache: thumbnails","name":"Google Photos — Photo Library"}]}]
a:["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","async":true}]
b:["$","script","script-1",{"src":"/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js","async":true}]
c:["$","$L1b",null,{"children":["$","$1c",null,{"name":"Next.MetadataOutlet","children":"$@1d"}]}]
f:["$","$1","h",{"children":[null,["$","$L1e",null,{"children":[["$","meta","0",{"charSet":"utf-8"}],["$","meta","1",{"name":"viewport","content":"width=device-width, initial-scale=1"}]]}],["$","div",null,{"hidden":true,"children":["$","$L1f",null,{"children":["$","$1c",null,{"name":"Next.Metadata","children":[["$","title","0",{"children":"Google Photos — Photo Library — Diagram Examples"}],["$","meta","1",{"name":"description","content":"Photo upload, dedupe, thumbnails, face grouping, sharing."}],["$","link","2",{"rel":"manifest","href":"/manifest.json"}],["$","meta","3",{"name":"mobile-web-app-capable","content":"yes"}],["$","meta","4",{"name":"apple-mobile-web-app-title","content":"Diagram"}],["$","meta","5",{"name":"apple-mobile-web-app-status-bar-style","content":"black-translucent"}]]}]}]}],["$","meta",null,{"name":"next-size-adjust","content":""}]]}]
11:["$","$1","c",{"children":[null,["$","$L20",null,{"parallelRouterKey":"children","template":["$","$L21",null,{}]}]]}]
14:["$","$1","c",{"children":[null,["$","$L20",null,{"parallelRouterKey":"children","template":["$","$L21",null,{}]}]]}]
16:["$","$L22",null,{"loading":[["$","div","l",{"className":"flex h-screen items-center justify-center","children":["$","span",null,{"className":"loading loading-spinner loading-lg text-primary"}]}],[],null],"children":["$","$1","c",{"children":[[["$","link","0",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","precedence":"next"}],["$","link","1",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","precedence":"next"}],["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","async":true}]],["$","html",null,{"lang":"en","data-theme":"diagram","className":"inter_537b2856-module__DOofUa__variable jetbrains_mono_da46d059-module__t9WmUG__variable","children":[["$","head",null,{"children":["$","link",null,{"rel":"apple-touch-icon","href":"/icons/icon-192.png"}]}],["$","body",null,{"className":"h-screen overflow-hidden","children":["$","$L20",null,{"parallelRouterKey":"children","error":"$23","errorStyles":[],"errorScripts":[["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/2td1d3x1ljv-f.js","async":true}]],"template":["$","$L21",null,{}],"notFound":[["$","div",null,{"className":"flex h-screen flex-col items-center justify-center gap-4","children":[["$","h1",null,{"className":"text-2xl","children":"404"}],["$","p",null,{"className":"text-sm","children":"This diagram does not exist."}],["$","a",null,{"href":"/","className":"btn btn-primary btn-sm","children":"Back to editor"}]]}],[]]}]}]]}]]}]}]
1d:null
19:true
e:300
e:C
18:0
10:"$undefined"
12:"$undefined"
15:"$undefined"
17:"$undefined"
d:"$undefined"
