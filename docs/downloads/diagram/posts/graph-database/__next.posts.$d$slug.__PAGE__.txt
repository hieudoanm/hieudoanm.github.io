1:"$Sreact.fragment"
2:I[83070,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js"],""]
e:X
13:X
13:C
0:{"buildId":"kwMAhgW1ckibYawstx_6-","data":[{"rsc":["$","$1","c",{"children":[["$","div",null,{"className":"h-screen overflow-y-auto","children":["$","main",null,{"className":"mx-auto max-w-3xl px-4 py-10","children":[["$","p",null,{"className":"mb-6 opacity-60","children":["$","$L2",null,{"className":"link","href":"/posts/","children":"← All posts"}]}],["$","article",null,{"children":[["$","header",null,{"className":"mb-8","children":[["$","h1",null,{"className":"text-3xl font-bold","children":"Graph Database"}],["$","p",null,{"className":"text-base-content/70 mt-2","children":"Nodes, edges, traversals, index-free adjacency."}],["$","p",null,{"className":"text-base-content/50 mt-3 text-sm","children":["Hieu Doan"," · ",["$","span",null,{"className":"capitalize","children":"medium"}]," ·"," ",["$","span",null,{"className":"capitalize","children":"storage"}]]}],["$","div",null,{"className":"mt-3 flex flex-wrap gap-1.5","children":[["$","span","database",{"className":"badge badge-outline badge-sm","children":"database"}],["$","span","graph",{"className":"badge badge-outline badge-sm","children":"graph"}]]}]]}],["$","section",null,{"children":[["$","h2",null,{"className":"mb-2 text-xl font-semibold","children":"Interview Questions"}],["$","ul",null,{"className":"list-disc space-y-1 pl-6","children":[["$","li","Design a graph database",{"children":"Design a graph database"}],["$","li","How do you store graph data efficiently?",{"children":"How do you store graph data efficiently?"}],["$","li","How do you run deep traversals fast?",{"children":"How do you run deep traversals fast?"}],["$","li","How do you handle hot nodes?",{"children":"How do you handle hot nodes?"}],["$","li","How do you partition a graph at scale?",{"children":"How do you partition a graph at scale?"}]]}]]}],["$","section",null,{"className":"mt-8","children":[["$","h2",null,{"className":"mb-4 text-xl font-semibold","children":"Answers"}],["$","div",null,{"className":"space-y-6","children":[["$","div","Design a graph database",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"Design a graph database"}],[["$","p","A graph database stores entities as nodes and the relationships between them as edges, and it makes the relationships first-class: queries traverse edges rather than joining tables. The architecture centers on a graph store that holds node storage and edge storage, a traversal engine that walks the graph, and a property index for filtering nodes by attributes. The gateway parses incoming queries, the query service plans them, and the traversal engine executes walks against the store, using the cache for hot paths and consulting the metadata database for schema and partition information. The defining property of the storage is index-free adjacency: each node keeps direct pointers to its neighbors, so following an edge is a pointer dereference, not a lookup.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"A graph database stores entities as nodes and the relationships between them as edges, and it makes the relationships first-class: queries traverse edges rather than joining tables. The architecture centers on a graph store that holds node storage and edge storage, a traversal engine that walks the graph, and a property index for filtering nodes by attributes. The gateway parses incoming queries, the query service plans them, and the traversal engine executes walks against the store, using the cache for hot paths and consulting the metadata database for schema and partition information. The defining property of the storage is index-free adjacency: each node keeps direct pointers to its neighbors, so following an edge is a pointer dereference, not a lookup."}]]}],["$","p","The choice of edge direction matters. Edges are directed and typed, and the storage keeps both incoming and outgoing adjacency lists so traversals can run in either direction at equal cost. Nodes and edges both carry properties, and the query language — Cypher, Gremlin, or GQL — expresses patterns like finding friends of friends or the shortest path between two users. Because the interesting queries are relationship-heavy, the design goal is to make multi-hop traversals fast in memory while keeping cold data cheap. The hard problems are scale-out, hot nodes, and query planning, since a naive traversal can fan out exponentially: starting from a celebrity node, two hops over following edges visits millions of nodes. The design also keeps the query surface small and typed: schema enforcement validates node labels and edge types up front, so malformed queries fail fast at plan time instead of walking the whole graph.",{"className":"mb-2 leading-relaxed","children":["$L3"]}]]]}],"$L4","$L5","$L6","$L7"]}]]}],"$L8"]}],"$L9"]}]}],["$La","$Lb"],"$Lc"]}],"isPartial":"$@d","staleTime":"$e","varyParams":null},{"rsc":"$Lf","isPartial":"$@10","staleTime":"$e","varyParams":null},{"rsc":"$L11","isPartial":"$@12","staleTime":"$e","varyParams":"$13"},{"rsc":"$L14","isPartial":"$@15","staleTime":"$e","varyParams":"$13"},{"rsc":"$L16","isPartial":"$@17","staleTime":"$e","varyParams":null}],"isUpgradeableISRFallback":false,"a":"$@18","rootVaryParams":null,"needsRuntimeRequest":"$@19"}
1a:I[94844,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js"],"default"]
1b:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"OutletBoundary"]
1c:"$Sreact.suspense"
1e:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"ViewportBoundary"]
1f:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"MetadataBoundary"]
20:I[93226,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default"]
21:I[15750,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default"]
22:I[93226,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"LoadingBoundaryProvider"]
23:I[46791,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/2td1d3x1ljv-f.js"],"default"]
:HL["/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","style"]
:HL["/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","style"]
3:["$","$1","0",{"children":"The choice of edge direction matters. Edges are directed and typed, and the storage keeps both incoming and outgoing adjacency lists so traversals can run in either direction at equal cost. Nodes and edges both carry properties, and the query language — Cypher, Gremlin, or GQL — expresses patterns like finding friends of friends or the shortest path between two users. Because the interesting queries are relationship-heavy, the design goal is to make multi-hop traversals fast in memory while keeping cold data cheap. The hard problems are scale-out, hot nodes, and query planning, since a naive traversal can fan out exponentially: starting from a celebrity node, two hops over following edges visits millions of nodes. The design also keeps the query surface small and typed: schema enforcement validates node labels and edge types up front, so malformed queries fail fast at plan time instead of walking the whole graph."}]
4:["$","div","How do you store graph data efficiently?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you store graph data efficiently?"}],[["$","p","Graph storage is organized around adjacency. The node store holds each node's identity, its properties, and a label or type; the edge store holds each edge's source, target, type, and properties. Efficient layout is key: the store keeps adjacency lists physically contiguous, so following all outgoing edges of a node means scanning a sequential run of memory rather than scattering across pages. The edge store maintains both forward and backward adjacency lists, and it stores edges grouped by source node so that a traversal that fans out reads contiguous blocks. Properties are compressed and stored off the adjacency path, so a traversal that only needs neighbor identities does not pay for property bloat.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Graph storage is organized around adjacency. The node store holds each node's identity, its properties, and a label or type; the edge store holds each edge's source, target, type, and properties. Efficient layout is key: the store keeps adjacency lists physically contiguous, so following all outgoing edges of a node means scanning a sequential run of memory rather than scattering across pages. The edge store maintains both forward and backward adjacency lists, and it stores edges grouped by source node so that a traversal that fans out reads contiguous blocks. Properties are compressed and stored off the adjacency path, so a traversal that only needs neighbor identities does not pay for property bloat."}]]}],["$","p","The relationship between node and edge storage is designed for locality. Each node row embeds a pointer or a range reference into the adjacency lists, making a hop a single address computation. High-degree structures are handled with a degree counter stored on the node, so the planner knows the fan-out before traversing. The store also supports duplicate-free edges and composite keys so there is a canonical identity per relationship. Writes are append-mostly — adding an edge appends to the adjacency list — which keeps writes cheap, and deletes mark tombstones that are reclaimed during compaction. This layout is what makes index-free adjacency real: the storage format and the traversal engine are designed together so that the expensive part of a graph query is reading neighbors, not finding them. The layout is validated by benchmarks on real traversals.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"The relationship between node and edge storage is designed for locality. Each node row embeds a pointer or a range reference into the adjacency lists, making a hop a single address computation. High-degree structures are handled with a degree counter stored on the node, so the planner knows the fan-out before traversing. The store also supports duplicate-free edges and composite keys so there is a canonical identity per relationship. Writes are append-mostly — adding an edge appends to the adjacency list — which keeps writes cheap, and deletes mark tombstones that are reclaimed during compaction. This layout is what makes index-free adjacency real: the storage format and the traversal engine are designed together so that the expensive part of a graph query is reading neighbors, not finding them. The layout is validated by benchmarks on real traversals."}]]}]]]}]
5:["$","div","How do you run deep traversals fast?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you run deep traversals fast?"}],[["$","p","Deep traversals — six or more hops — fail fast if the engine expands blindly. The traversal engine plans an execution strategy before walking: it reads the degree of each frontier node, estimates the fan-out at each depth, and chooses an order that prunes the search space early. For queries with a fixed pattern, like shortest path, bidirectional search expands from both ends and meets in the middle, which turns a fan-out of ten per hop over five hops into two walks of about three hops each, cutting the work from ten to the fifth power to roughly two times ten to the third. Bounded-depth traversals can also stop once the target set is found.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Deep traversals — six or more hops — fail fast if the engine expands blindly. The traversal engine plans an execution strategy before walking: it reads the degree of each frontier node, estimates the fan-out at each depth, and chooses an order that prunes the search space early. For queries with a fixed pattern, like shortest path, bidirectional search expands from both ends and meets in the middle, which turns a fan-out of ten per hop over five hops into two walks of about three hops each, cutting the work from ten to the fifth power to roughly two times ten to the third. Bounded-depth traversals can also stop once the target set is found."}]]}],["$","p","The engine also caches aggressively. The hot path cache holds frequently repeated subgraph results — friends of a user, the comment thread for a post — so repeat queries never re-walk the graph, and the cache is invalidated only when the involved nodes or edges change. Columnar or vectorized adjacency iteration lets the engine process entire frontiers in batches rather than one node at a time, and parallel traversal splits frontiers across threads when the subgraph is safe to visit independently. When a deep traversal legitimately needs to inspect a large portion of the graph, the engine converts it into a batched, potentially external-memory walk with a visited set, so memory use is bounded even when the result is huge. The planner's job is to make the fast case fast and the hard case predictable.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"The engine also caches aggressively. The hot path cache holds frequently repeated subgraph results — friends of a user, the comment thread for a post — so repeat queries never re-walk the graph, and the cache is invalidated only when the involved nodes or edges change. Columnar or vectorized adjacency iteration lets the engine process entire frontiers in batches rather than one node at a time, and parallel traversal splits frontiers across threads when the subgraph is safe to visit independently. When a deep traversal legitimately needs to inspect a large portion of the graph, the engine converts it into a batched, potentially external-memory walk with a visited set, so memory use is bounded even when the result is huge. The planner's job is to make the fast case fast and the hard case predictable."}]]}]]]}]
6:["$","div","How do you handle hot nodes?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you handle hot nodes?"}],[["$","p","Hot nodes — celebrities, popular items, central hubs — create two problems: the read path saturates because every traversal touches them, and the write path contends because many edges attach to them. The read side is handled by the hot path cache. Subgraphs that repeatedly include the same hot nodes, like the followers of a celebrity or related products for a popular listing, are materialized and served from memory, and the cache is keyed by the entry point and depth so a fan-out from a hot node becomes a cache hit instead of a walk of hundreds of thousands of edges. Partitioning matters too: a hot node and its immediate neighborhood should live on one node, so traversals through it stay local.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Hot nodes — celebrities, popular items, central hubs — create two problems: the read path saturates because every traversal touches them, and the write path contends because many edges attach to them. The read side is handled by the hot path cache. Subgraphs that repeatedly include the same hot nodes, like the followers of a celebrity or related products for a popular listing, are materialized and served from memory, and the cache is keyed by the entry point and depth so a fan-out from a hot node becomes a cache hit instead of a walk of hundreds of thousands of edges. Partitioning matters too: a hot node and its immediate neighborhood should live on one node, so traversals through it stay local."}]]}],["$","p","The write side is the harder problem. A celebrity gaining a million followers means a million edge insertions to one logical node, and if those inserts serialize on that node's adjacency list, the write path collapses. The store separates the hot node's adjacency into sharded edge partitions, so inserts can proceed in parallel, and it may maintain the node's core identity on one partition while its high-degree adjacency fans out across several. The degree counter is updated with eventual consistency and corrected by background aggregation. When a node becomes hot — a viral post or a sudden surge — the partitioner reacts by splitting its edges across more partitions and warming the cache. Monitoring degree and read rates per node drives these decisions, because a node that is merely popular today can be the one that brings the cluster down tomorrow.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"The write side is the harder problem. A celebrity gaining a million followers means a million edge insertions to one logical node, and if those inserts serialize on that node's adjacency list, the write path collapses. The store separates the hot node's adjacency into sharded edge partitions, so inserts can proceed in parallel, and it may maintain the node's core identity on one partition while its high-degree adjacency fans out across several. The degree counter is updated with eventual consistency and corrected by background aggregation. When a node becomes hot — a viral post or a sudden surge — the partitioner reacts by splitting its edges across more partitions and warming the cache. Monitoring degree and read rates per node drives these decisions, because a node that is merely popular today can be the one that brings the cluster down tomorrow."}]]}]]]}]
7:["$","div","How do you partition a graph at scale?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you partition a graph at scale?"}],[["$","p","Partitioning a graph is fundamentally hard because edges want to cross the partition boundary. If the store naively shards by node ID, a traversal that crosses partitions turns into distributed round trips, and deep traversals become network-latency-bound. The answer is locality-aware partitioning: the partitioner assigns nodes to shards so that dense communities stay together, using hash-based clustering or a graph partitioning algorithm that minimizes cut edges. Frequent query patterns — the users who interact, the pages that link — are placed on the same shard so the common traversals stay local. A replication factor covers the boundary edges: a replicated edge or a replicated copy of a hot node lets a traversal proceed locally instead of fetching across the wire.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Partitioning a graph is fundamentally hard because edges want to cross the partition boundary. If the store naively shards by node ID, a traversal that crosses partitions turns into distributed round trips, and deep traversals become network-latency-bound. The answer is locality-aware partitioning: the partitioner assigns nodes to shards so that dense communities stay together, using hash-based clustering or a graph partitioning algorithm that minimizes cut edges. Frequent query patterns — the users who interact, the pages that link — are placed on the same shard so the common traversals stay local. A replication factor covers the boundary edges: a replicated edge or a replicated copy of a hot node lets a traversal proceed locally instead of fetching across the wire."}]]}],["$","p","Because real graphs change, partitioning cannot be a one-time choice. The partitioner observes access patterns and degree growth, and it rebalances by moving nodes or migrating subgraphs between shards during low-traffic windows, with the metadata database tracking where each node currently lives. Cross-partition queries are still supported — the query planner breaks a traversal into local segments and coordinates between shards — but they are the fallback, not the norm. Distributed transactions across shards are avoided where possible because they are expensive; updates are scoped to a single partition for the common case, and multi-partition updates use a two-phase or saga approach. The design goal is to push shared-nothing reality as far down as possible: shards are the failure domain and the scale unit, and the closer the graph sits to its community, the less often a query leaves home.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Because real graphs change, partitioning cannot be a one-time choice. The partitioner observes access patterns and degree growth, and it rebalances by moving nodes or migrating subgraphs between shards during low-traffic windows, with the metadata database tracking where each node currently lives. Cross-partition queries are still supported — the query planner breaks a traversal into local segments and coordinates between shards — but they are the fallback, not the norm. Distributed transactions across shards are avoided where possible because they are expensive; updates are scoped to a single partition for the common case, and multi-partition updates use a two-phase or saga approach. The design goal is to push shared-nothing reality as far down as possible: shards are the failure domain and the scale unit, and the closer the graph sits to its community, the less often a query leaves home."}]]}]]]}]
8:["$","details",null,{"className":"mt-8","children":[["$","summary",null,{"className":"cursor-pointer font-medium","children":"Diagram source"}],["$","pre",null,{"className":"bg-base-200 mt-2 overflow-x-auto rounded-lg p-4 font-mono text-xs","children":"title: Graph Database\nnode app: Client App [icon=browser]\nnode gateway: API Gateway [icon=server]\nnode query: Query Service [icon=compute]\nnode graph: Graph Store [icon=compute]\nnode node: Node Storage [cylinder, icon=database]\nnode edge: Edge Storage [cylinder, icon=database]\nnode index: Property Index [icon=search]\nnode traverse: Traversal Engine [icon=compute]\nnode cache: Hot Path Cache [icon=cache]\nnode partition: Partitioner [icon=worker]\nnode db: Metadata DB [cylinder, icon=database]\n\nedge app -> gateway: query\nedge gateway -> query: parse\nedge query -> traverse: plan\nedge traverse -> graph: walk\nedge graph -> node: load\nedge graph -> edge: follow\nedge traverse -> index: lookup\nedge traverse -> cache: check\nedge cache -> traverse: hit\nedge partition -> graph: shard\nedge query -> db: meta"}]]}]
9:["$","div",null,{"className":"mt-8","children":["$","$L1a",null,{"text":"title: Graph Database\nnode app: Client App [icon=browser]\nnode gateway: API Gateway [icon=server]\nnode query: Query Service [icon=compute]\nnode graph: Graph Store [icon=compute]\nnode node: Node Storage [cylinder, icon=database]\nnode edge: Edge Storage [cylinder, icon=database]\nnode index: Property Index [icon=search]\nnode traverse: Traversal Engine [icon=compute]\nnode cache: Hot Path Cache [icon=cache]\nnode partition: Partitioner [icon=worker]\nnode db: Metadata DB [cylinder, icon=database]\n\nedge app -> gateway: query\nedge gateway -> query: parse\nedge query -> traverse: plan\nedge traverse -> graph: walk\nedge graph -> node: load\nedge graph -> edge: follow\nedge traverse -> index: lookup\nedge traverse -> cache: check\nedge cache -> traverse: hit\nedge partition -> graph: shard\nedge query -> db: meta","name":"Graph Database"}]}]
a:["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","async":true}]
b:["$","script","script-1",{"src":"/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js","async":true}]
c:["$","$L1b",null,{"children":["$","$1c",null,{"name":"Next.MetadataOutlet","children":"$@1d"}]}]
f:["$","$1","h",{"children":[null,["$","$L1e",null,{"children":[["$","meta","0",{"charSet":"utf-8"}],["$","meta","1",{"name":"viewport","content":"width=device-width, initial-scale=1"}]]}],["$","div",null,{"hidden":true,"children":["$","$L1f",null,{"children":["$","$1c",null,{"name":"Next.Metadata","children":[["$","title","0",{"children":"Graph Database — Diagram Examples"}],["$","meta","1",{"name":"description","content":"Nodes, edges, traversals, index-free adjacency."}],["$","link","2",{"rel":"manifest","href":"/manifest.json"}],["$","meta","3",{"name":"mobile-web-app-capable","content":"yes"}],["$","meta","4",{"name":"apple-mobile-web-app-title","content":"Diagram"}],["$","meta","5",{"name":"apple-mobile-web-app-status-bar-style","content":"black-translucent"}]]}]}]}],["$","meta",null,{"name":"next-size-adjust","content":""}]]}]
11:["$","$1","c",{"children":[null,["$","$L20",null,{"parallelRouterKey":"children","template":["$","$L21",null,{}]}]]}]
14:["$","$1","c",{"children":[null,["$","$L20",null,{"parallelRouterKey":"children","template":["$","$L21",null,{}]}]]}]
16:["$","$L22",null,{"loading":[["$","div","l",{"className":"flex h-screen items-center justify-center","children":["$","span",null,{"className":"loading loading-spinner loading-lg text-primary"}]}],[],null],"children":["$","$1","c",{"children":[[["$","link","0",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","precedence":"next"}],["$","link","1",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","precedence":"next"}],["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","async":true}]],["$","html",null,{"lang":"en","data-theme":"diagram","className":"inter_537b2856-module__DOofUa__variable jetbrains_mono_da46d059-module__t9WmUG__variable","children":[["$","head",null,{"children":["$","link",null,{"rel":"apple-touch-icon","href":"/icons/icon-192.png"}]}],["$","body",null,{"className":"h-screen overflow-hidden","children":["$","$L20",null,{"parallelRouterKey":"children","error":"$23","errorStyles":[],"errorScripts":[["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/2td1d3x1ljv-f.js","async":true}]],"template":["$","$L21",null,{}],"notFound":[["$","div",null,{"className":"flex h-screen flex-col items-center justify-center gap-4","children":[["$","h1",null,{"className":"text-2xl","children":"404"}],["$","p",null,{"className":"text-sm","children":"This diagram does not exist."}],["$","a",null,{"href":"/","className":"btn btn-primary btn-sm","children":"Back to editor"}]]}],[]]}]}]]}]]}]}]
1d:null
19:true
e:300
e:C
18:0
10:"$undefined"
12:"$undefined"
15:"$undefined"
17:"$undefined"
d:"$undefined"
