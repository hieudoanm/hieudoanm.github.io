1:"$Sreact.fragment"
2:I[83070,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js"],""]
e:X
13:X
13:C
0:{"buildId":"kwMAhgW1ckibYawstx_6-","data":[{"rsc":["$","$1","c",{"children":[["$","div",null,{"className":"h-screen overflow-y-auto","children":["$","main",null,{"className":"mx-auto max-w-3xl px-4 py-10","children":[["$","p",null,{"className":"mb-6 opacity-60","children":["$","$L2",null,{"className":"link","href":"/posts/","children":"← All posts"}]}],["$","article",null,{"children":[["$","header",null,{"className":"mb-8","children":[["$","h1",null,{"className":"text-3xl font-bold","children":"Ad Serving — Ad Network"}],["$","p",null,{"className":"text-base-content/70 mt-2","children":"Real-time auction, targeting, budgets, attribution."}],["$","p",null,{"className":"text-base-content/50 mt-3 text-sm","children":["Hieu Doan"," · ",["$","span",null,{"className":"capitalize","children":"medium"}]," ·"," ",["$","span",null,{"className":"capitalize","children":"ecommerce"}]]}],["$","div",null,{"className":"mt-3 flex flex-wrap gap-1.5","children":[["$","span","realtime",{"className":"badge badge-outline badge-sm","children":"realtime"}],["$","span","recommendation",{"className":"badge badge-outline badge-sm","children":"recommendation"}]]}]]}],["$","section",null,{"children":[["$","h2",null,{"className":"mb-2 text-xl font-semibold","children":"Interview Questions"}],["$","ul",null,{"className":"list-disc space-y-1 pl-6","children":[["$","li","Design an ad serving platform",{"children":"Design an ad serving platform"}],["$","li","How do you run a real-time auction under latency?",{"children":"How do you run a real-time auction under latency?"}],["$","li","How do you target ads without hurting performance?",{"children":"How do you target ads without hurting performance?"}],["$","li","How do you enforce budgets and frequency caps?",{"children":"How do you enforce budgets and frequency caps?"}],["$","li","How do you attribute conversions across touchpoints?",{"children":"How do you attribute conversions across touchpoints?"}]]}]]}],["$","section",null,{"className":"mt-8","children":[["$","h2",null,{"className":"mb-4 text-xl font-semibold","children":"Answers"}],["$","div",null,{"className":"space-y-6","children":[["$","div","Design an ad serving platform",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"Design an ad serving platform"}],[["$","p","The platform brokers a real-time auction between publishers who sell impressions and advertisers who buy them. When a user opens a publisher site, the page fires an ad request to the supply-side platform (SSP), which calls the ad exchange. The exchange fans the impression out to demand-side platforms (DSPs); each DSP evaluates its advertisers' targeting rules against the user, prices the impression, and returns a bid. The exchange selects the highest eligible bidder, the SSP renders the winning creative, and a tracking service logs the impression and click. Every step runs under a hard latency budget — the whole auction round trip must complete in roughly 100ms or the page forfeits the slot.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"The platform brokers a real-time auction between publishers who sell impressions and advertisers who buy them. When a user opens a publisher site, the page fires an ad request to the supply-side platform (SSP), which calls the ad exchange. The exchange fans the impression out to demand-side platforms (DSPs); each DSP evaluates its advertisers' targeting rules against the user, prices the impression, and returns a bid. The exchange selects the highest eligible bidder, the SSP renders the winning creative, and a tracking service logs the impression and click. Every step runs under a hard latency budget — the whole auction round trip must complete in roughly 100ms or the page forfeits the slot."}]]}],["$","p","The data model is centered on a few stores: an ad store holding creative metadata (size, destination URL, policy compliance), a user profile store holding segments and identifiers for targeting, a budget service tracking spend per campaign, and a reporting pipeline that turns raw impression and click events into advertiser dashboards. Because the critical path is read-heavy and latency-bound, the auction logic runs in memory — profiles are pre-loaded, targeting is an inverted index, and no database round trip happens between receiving a request and returning a bid. Consent management sits at the edges: profile building and sharing honor opt-out and consent signals so targeted advertising stays lawful. The platform also plans for failure: if the exchange or a DSP times out, a fallback path serves a house ad so the page always has something to render, and impression loss is measured so any degradation is visible.",{"className":"mb-2 leading-relaxed","children":["$L3"]}]]]}],"$L4","$L5","$L6","$L7"]}]]}],"$L8"]}],"$L9"]}]}],["$La","$Lb"],"$Lc"]}],"isPartial":"$@d","staleTime":"$e","varyParams":null},{"rsc":"$Lf","isPartial":"$@10","staleTime":"$e","varyParams":null},{"rsc":"$L11","isPartial":"$@12","staleTime":"$e","varyParams":"$13"},{"rsc":"$L14","isPartial":"$@15","staleTime":"$e","varyParams":"$13"},{"rsc":"$L16","isPartial":"$@17","staleTime":"$e","varyParams":null}],"isUpgradeableISRFallback":false,"a":"$@18","rootVaryParams":null,"needsRuntimeRequest":"$@19"}
1a:I[94844,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js"],"default"]
1b:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"OutletBoundary"]
1c:"$Sreact.suspense"
1e:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"ViewportBoundary"]
1f:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"MetadataBoundary"]
20:I[93226,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default"]
21:I[15750,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default"]
22:I[93226,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"LoadingBoundaryProvider"]
23:I[46791,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/2td1d3x1ljv-f.js"],"default"]
:HL["/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","style"]
:HL["/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","style"]
3:["$","$1","0",{"children":"The data model is centered on a few stores: an ad store holding creative metadata (size, destination URL, policy compliance), a user profile store holding segments and identifiers for targeting, a budget service tracking spend per campaign, and a reporting pipeline that turns raw impression and click events into advertiser dashboards. Because the critical path is read-heavy and latency-bound, the auction logic runs in memory — profiles are pre-loaded, targeting is an inverted index, and no database round trip happens between receiving a request and returning a bid. Consent management sits at the edges: profile building and sharing honor opt-out and consent signals so targeted advertising stays lawful. The platform also plans for failure: if the exchange or a DSP times out, a fallback path serves a house ad so the page always has something to render, and impression loss is measured so any degradation is visible."}]
4:["$","div","How do you run a real-time auction under latency?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you run a real-time auction under latency?"}],[["$","p","The auction is a fan-out-and-merge. The exchange sends the impression to many DSPs in parallel over pre-warmed keep-alive connections, waits for bids up to a deadline (commonly 80–100ms from request receipt), and then picks the highest eligible bid above the seller's floor price. Nothing on this path may hit a slow store: user profiles and segment memberships are resident in RAM, and targeting runs as an in-memory lookup, so a DSP answers in single-digit milliseconds. The auction per node is single-threaded and event-driven, which lets one exchange instance handle tens of thousands of concurrent impressions without lock contention.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"The auction is a fan-out-and-merge. The exchange sends the impression to many DSPs in parallel over pre-warmed keep-alive connections, waits for bids up to a deadline (commonly 80–100ms from request receipt), and then picks the highest eligible bid above the seller's floor price. Nothing on this path may hit a slow store: user profiles and segment memberships are resident in RAM, and targeting runs as an in-memory lookup, so a DSP answers in single-digit milliseconds. The auction per node is single-threaded and event-driven, which lets one exchange instance handle tens of thousands of concurrent impressions without lock contention."}]]}],["$","p","Timeouts are mandatory. A DSP that misses the deadline is dropped and the auction proceeds with the bids that arrived — the page would rather show a cheaper ad than wait. Reserve price checks happen first so the exchange short-circuits when even the best possible bid cannot clear the floor, and each bid is validated for price integrity and creative compliance before winning. If no bid clears, a low-CPM house or remnant ad wins the pass. Horizontal scaling comes free because exchange nodes are stateless; impressions are hashed across nodes by site or user, and the profile data they need is replicated in each node's memory, keeping P99 auction latency stable as QPS grows. Load shedding is explicit: when a node exceeds its auction budget, it stops accepting new requests rather than serving late bids, because a bid returned after the deadline is worthless.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Timeouts are mandatory. A DSP that misses the deadline is dropped and the auction proceeds with the bids that arrived — the page would rather show a cheaper ad than wait. Reserve price checks happen first so the exchange short-circuits when even the best possible bid cannot clear the floor, and each bid is validated for price integrity and creative compliance before winning. If no bid clears, a low-CPM house or remnant ad wins the pass. Horizontal scaling comes free because exchange nodes are stateless; impressions are hashed across nodes by site or user, and the profile data they need is replicated in each node's memory, keeping P99 auction latency stable as QPS grows. Load shedding is explicit: when a node exceeds its auction budget, it stops accepting new requests rather than serving late bids, because a bid returned after the deadline is worthless."}]]}]]]}]
5:["$","div","How do you target ads without hurting performance?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you target ads without hurting performance?"}],[["$","p","Targeting is split into a coarse stage and a fine stage so expensive logic only runs on a tiny candidate set. Coarse targeting happens at bid time against pre-computed, in-memory segments: user attributes like geo, device, interests, and retargeting lists are materialized into segment memberships (bitsets or roaring bitmaps) refreshed asynchronously from streaming events. A DSP looks up the user's segment IDs and walks an inverted index from segment to eligible campaign IDs, yielding a candidate set in microseconds without touching a database. Consent and privacy rules gate which segments are usable per user, so blocked segments simply never appear in the index for that profile.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Targeting is split into a coarse stage and a fine stage so expensive logic only runs on a tiny candidate set. Coarse targeting happens at bid time against pre-computed, in-memory segments: user attributes like geo, device, interests, and retargeting lists are materialized into segment memberships (bitsets or roaring bitmaps) refreshed asynchronously from streaming events. A DSP looks up the user's segment IDs and walks an inverted index from segment to eligible campaign IDs, yielding a candidate set in microseconds without touching a database. Consent and privacy rules gate which segments are usable per user, so blocked segments simply never appear in the index for that profile."}]]}],["$","p","Fine targeting — budget checks, frequency caps, creative constraints, and bid adjustment — then runs only on the handful of candidate ads that passed the coarse filter, not on the full campaign table. Profile data is allowed to be minutes stale, which is acceptable because targeting accuracy barely moves in that window; the freshness trade-off buys a hot path that never reads from disk. Because the matching logic lives inside the DSP process and nodes are sharded by user ID, targeting capacity scales linearly with the number of DSP instances. This two-stage design is the answer to \"target without hurting performance\": do the broad narrowing in memory, then apply the precise rules to almost nothing. A/B benchmarking of targeting rules and index builds runs continuously, so a rule change that bloats the index or slows lookup is caught in staging before it ships to the real-time path.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Fine targeting — budget checks, frequency caps, creative constraints, and bid adjustment — then runs only on the handful of candidate ads that passed the coarse filter, not on the full campaign table. Profile data is allowed to be minutes stale, which is acceptable because targeting accuracy barely moves in that window; the freshness trade-off buys a hot path that never reads from disk. Because the matching logic lives inside the DSP process and nodes are sharded by user ID, targeting capacity scales linearly with the number of DSP instances. This two-stage design is the answer to \"target without hurting performance\": do the broad narrowing in memory, then apply the precise rules to almost nothing. A/B benchmarking of targeting rules and index builds runs continuously, so a rule change that bloats the index or slows lookup is caught in staging before it ships to the real-time path."}]]}]]]}]
6:["$","div","How do you enforce budgets and frequency caps?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you enforce budgets and frequency caps?"}],[["$","p","Spend tracking lives in a distributed counter store such as Redis. On every auction win, the budget service atomically increments a per-campaign daily-spend counter, and if the increment would cross the budget the campaign is removed from the candidate set for subsequent auctions. Because auctions are concurrent across many exchange nodes, reservation must be atomic — a Lua script that checks the remaining budget and increments in one step prevents overspend races. Budget enforcement is optimistic by design: a small overspend is tolerated within the auction window and reconciled in daily batch, so the system does not serialize every impression on a global lock.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Spend tracking lives in a distributed counter store such as Redis. On every auction win, the budget service atomically increments a per-campaign daily-spend counter, and if the increment would cross the budget the campaign is removed from the candidate set for subsequent auctions. Because auctions are concurrent across many exchange nodes, reservation must be atomic — a Lua script that checks the remaining budget and increments in one step prevents overspend races. Budget enforcement is optimistic by design: a small overspend is tolerated within the auction window and reconciled in daily batch, so the system does not serialize every impression on a global lock."}]]}],["$","p","Frequency caps are a separate set of counters keyed by (user, campaign) that count impressions over a rolling window and are checked in the fine targeting stage; counters expire with the window so cap state stays bounded. Pacing smooths delivery across the day: the budget service computes an expected hourly spend and lowers the bid multiplier when a campaign is ahead of plan, so a campaign neither exhausts its budget at 9am nor trickles out at a constant low rate. When the counter store is unavailable the auction fails open — the campaign bids without budget enforcement and the event is logged — rather than dropping all traffic; the reconciler then corrects the drift once the store returns. Budget counters are sharded by campaign so a single campaign's spend rate never bottlenecks on one counter, and the reconciler reports over- and under-delivery per campaign.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Frequency caps are a separate set of counters keyed by (user, campaign) that count impressions over a rolling window and are checked in the fine targeting stage; counters expire with the window so cap state stays bounded. Pacing smooths delivery across the day: the budget service computes an expected hourly spend and lowers the bid multiplier when a campaign is ahead of plan, so a campaign neither exhausts its budget at 9am nor trickles out at a constant low rate. When the counter store is unavailable the auction fails open — the campaign bids without budget enforcement and the event is logged — rather than dropping all traffic; the reconciler then corrects the drift once the store returns. Budget counters are sharded by campaign so a single campaign's spend rate never bottlenecks on one counter, and the reconciler reports over- and under-delivery per campaign."}]]}]]]}]
7:["$","div","How do you attribute conversions across touchpoints?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you attribute conversions across touchpoints?"}],[["$","p","Every served ad embeds a unique click ID, and the tracking pixel carries it through to the advertiser's site, so the tracking service receives impression, click, and conversion events that share the same lineage. Each event is tagged with the click ID, user ID, campaign ID, and timestamp and streamed into a data pipeline. Attribution joins conversions back to the click or impression that caused them within a configurable attribution window (typically 1–30 days): exact joins on the click ID, with cookie and device IDs as fallback keys when a conversion happens on a different page or browser. Click-through and view-through attribution are tracked separately because they have very different base rates.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Every served ad embeds a unique click ID, and the tracking pixel carries it through to the advertiser's site, so the tracking service receives impression, click, and conversion events that share the same lineage. Each event is tagged with the click ID, user ID, campaign ID, and timestamp and streamed into a data pipeline. Attribution joins conversions back to the click or impression that caused them within a configurable attribution window (typically 1–30 days): exact joins on the click ID, with cookie and device IDs as fallback keys when a conversion happens on a different page or browser. Click-through and view-through attribution are tracked separately because they have very different base rates."}]]}],["$","p","Multi-touch models distribute credit across the ordered sequence of touchpoints that preceded a conversion — last-click, first-click, and time-decay all reduce to weighted sums over the user's event stream. The join is expensive, so it runs as a batched offline job over event logs rather than inline, and the results feed both the reporting store and the budget optimizer's return-on-spend model. Cross-device attribution is inherently approximate because identifiers differ per device, so the system merges through a deterministic identity graph and clearly separates attributed from unattributed conversions in the reports. That honesty — plus configurable windows and models — is what makes the attribution numbers trusted by advertisers. The pipeline is idempotent — duplicate events are deduplicated on the (click_id, event_type, timestamp) key — so retries in the event collector never double-count a conversion.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Multi-touch models distribute credit across the ordered sequence of touchpoints that preceded a conversion — last-click, first-click, and time-decay all reduce to weighted sums over the user's event stream. The join is expensive, so it runs as a batched offline job over event logs rather than inline, and the results feed both the reporting store and the budget optimizer's return-on-spend model. Cross-device attribution is inherently approximate because identifiers differ per device, so the system merges through a deterministic identity graph and clearly separates attributed from unattributed conversions in the reports. That honesty — plus configurable windows and models — is what makes the attribution numbers trusted by advertisers. The pipeline is idempotent — duplicate events are deduplicated on the (click_id, event_type, timestamp) key — so retries in the event collector never double-count a conversion."}]]}]]]}]
8:["$","details",null,{"className":"mt-8","children":[["$","summary",null,{"className":"cursor-pointer font-medium","children":"Diagram source"}],["$","pre",null,{"className":"bg-base-200 mt-2 overflow-x-auto rounded-lg p-4 font-mono text-xs","children":"title: Ad Serving\nnode user: User [round, icon=browser]\nnode site: Publisher Site [icon=browser]\nnode ssp: Ad Request [icon=server]\nnode dsp: DSP Bidding [icon=compute]\nnode adx: Ad Exchange [icon=queue]\nnode profile: User Profile [cylinder, icon=database]\nnode target: Targeting Engine [icon=search]\nnode budget: Budget Service [icon=cache]\nnode tracking: Tracking Service [icon=worker]\nnode adstore: Ad Store [cylinder, icon=file]\nnode report: Reporting [icon=search]\n\nedge user -> site: open\nedge site -> ssp: request\nedge ssp -> adx: auction\nedge adx -> dsp: bid\nedge dsp -> target: match\nedge target -> profile: lookup\nedge adx -> ssp: winner\nedge ssp -> user: ad\nedge user -> tracking: impression\nedge tracking -> budget: charge\nedge tracking -> report: log"}]]}]
9:["$","div",null,{"className":"mt-8","children":["$","$L1a",null,{"text":"title: Ad Serving\nnode user: User [round, icon=browser]\nnode site: Publisher Site [icon=browser]\nnode ssp: Ad Request [icon=server]\nnode dsp: DSP Bidding [icon=compute]\nnode adx: Ad Exchange [icon=queue]\nnode profile: User Profile [cylinder, icon=database]\nnode target: Targeting Engine [icon=search]\nnode budget: Budget Service [icon=cache]\nnode tracking: Tracking Service [icon=worker]\nnode adstore: Ad Store [cylinder, icon=file]\nnode report: Reporting [icon=search]\n\nedge user -> site: open\nedge site -> ssp: request\nedge ssp -> adx: auction\nedge adx -> dsp: bid\nedge dsp -> target: match\nedge target -> profile: lookup\nedge adx -> ssp: winner\nedge ssp -> user: ad\nedge user -> tracking: impression\nedge tracking -> budget: charge\nedge tracking -> report: log","name":"Ad Serving — Ad Network"}]}]
a:["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","async":true}]
b:["$","script","script-1",{"src":"/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js","async":true}]
c:["$","$L1b",null,{"children":["$","$1c",null,{"name":"Next.MetadataOutlet","children":"$@1d"}]}]
f:["$","$1","h",{"children":[null,["$","$L1e",null,{"children":[["$","meta","0",{"charSet":"utf-8"}],["$","meta","1",{"name":"viewport","content":"width=device-width, initial-scale=1"}]]}],["$","div",null,{"hidden":true,"children":["$","$L1f",null,{"children":["$","$1c",null,{"name":"Next.Metadata","children":[["$","title","0",{"children":"Ad Serving — Ad Network — Diagram Examples"}],["$","meta","1",{"name":"description","content":"Real-time auction, targeting, budgets, attribution."}],["$","link","2",{"rel":"manifest","href":"/manifest.json"}],["$","meta","3",{"name":"mobile-web-app-capable","content":"yes"}],["$","meta","4",{"name":"apple-mobile-web-app-title","content":"Diagram"}],["$","meta","5",{"name":"apple-mobile-web-app-status-bar-style","content":"black-translucent"}]]}]}]}],["$","meta",null,{"name":"next-size-adjust","content":""}]]}]
11:["$","$1","c",{"children":[null,["$","$L20",null,{"parallelRouterKey":"children","template":["$","$L21",null,{}]}]]}]
14:["$","$1","c",{"children":[null,["$","$L20",null,{"parallelRouterKey":"children","template":["$","$L21",null,{}]}]]}]
16:["$","$L22",null,{"loading":[["$","div","l",{"className":"flex h-screen items-center justify-center","children":["$","span",null,{"className":"loading loading-spinner loading-lg text-primary"}]}],[],null],"children":["$","$1","c",{"children":[[["$","link","0",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","precedence":"next"}],["$","link","1",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","precedence":"next"}],["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","async":true}]],["$","html",null,{"lang":"en","data-theme":"diagram","className":"inter_537b2856-module__DOofUa__variable jetbrains_mono_da46d059-module__t9WmUG__variable","children":[["$","head",null,{"children":["$","link",null,{"rel":"apple-touch-icon","href":"/icons/icon-192.png"}]}],["$","body",null,{"className":"h-screen overflow-hidden","children":["$","$L20",null,{"parallelRouterKey":"children","error":"$23","errorStyles":[],"errorScripts":[["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/2td1d3x1ljv-f.js","async":true}]],"template":["$","$L21",null,{}],"notFound":[["$","div",null,{"className":"flex h-screen flex-col items-center justify-center gap-4","children":[["$","h1",null,{"className":"text-2xl","children":"404"}],["$","p",null,{"className":"text-sm","children":"This diagram does not exist."}],["$","a",null,{"href":"/","className":"btn btn-primary btn-sm","children":"Back to editor"}]]}],[]]}]}]]}]]}]}]
1d:null
19:true
e:300
e:C
18:0
10:"$undefined"
12:"$undefined"
15:"$undefined"
17:"$undefined"
d:"$undefined"
