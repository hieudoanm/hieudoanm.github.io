1:"$Sreact.fragment"
2:I[83070,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js"],""]
f:X
14:X
14:C
0:{"buildId":"kwMAhgW1ckibYawstx_6-","data":[{"rsc":["$","$1","c",{"children":[["$","div",null,{"className":"h-screen overflow-y-auto","children":["$","main",null,{"className":"mx-auto max-w-3xl px-4 py-10","children":[["$","p",null,{"className":"mb-6 opacity-60","children":["$","$L2",null,{"className":"link","href":"/posts/","children":"← All posts"}]}],["$","article",null,{"children":[["$","header",null,{"className":"mb-8","children":[["$","h1",null,{"className":"text-3xl font-bold","children":"Webhook Delivery"}],["$","p",null,{"className":"text-base-content/70 mt-2","children":"Event subscription, delivery, retries, signing."}],["$","p",null,{"className":"text-base-content/50 mt-3 text-sm","children":["Hieu Doan"," · ",["$","span",null,{"className":"capitalize","children":"medium"}]," ·"," ",["$","span",null,{"className":"capitalize","children":"infrastructure"}]]}],["$","div",null,{"className":"mt-3 flex flex-wrap gap-1.5","children":[["$","span","event-driven",{"className":"badge badge-outline badge-sm","children":"event-driven"}],["$","span","realtime",{"className":"badge badge-outline badge-sm","children":"realtime"}],["$","span","security",{"className":"badge badge-outline badge-sm","children":"security"}]]}]]}],["$","section",null,{"children":[["$","h2",null,{"className":"mb-2 text-xl font-semibold","children":"Interview Questions"}],["$","ul",null,{"className":"list-disc space-y-1 pl-6","children":[["$","li","Design a webhook delivery system",{"children":"Design a webhook delivery system"}],["$","li","How do you deliver events reliably?",{"children":"How do you deliver events reliably?"}],["$","li","How do you handle retries and backoff?",{"children":"How do you handle retries and backoff?"}],["$","li","How do you secure webhook payloads?",{"children":"How do you secure webhook payloads?"}],["$","li","How do you handle slow or down endpoints?",{"children":"How do you handle slow or down endpoints?"}]]}]]}],["$","section",null,{"className":"mt-8","children":[["$","h2",null,{"className":"mb-4 text-xl font-semibold","children":"Answers"}],["$","div",null,{"className":"space-y-6","children":[["$","div","Design a webhook delivery system",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"Design a webhook delivery system"}],[["$","p","A webhook delivery system moves events from an internal service to customer-owned endpoints over the public internet, which is inherently unreliable. I would build it around four components. A subscription service stores which endpoint receives which event types. A delivery engine turns events into HTTP deliveries. A durable delivery queue decouples event production from outbound HTTP, and a retry scheduler plus dead-letter queue handles the inevitable failures. A delivery database records every attempt, status, and payload so the whole system is observable and replayable.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"A webhook delivery system moves events from an internal service to customer-owned endpoints over the public internet, which is inherently unreliable. I would build it around four components. A subscription service stores which endpoint receives which event types. A delivery engine turns events into HTTP deliveries. A durable delivery queue decouples event production from outbound HTTP, and a retry scheduler plus dead-letter queue handles the inevitable failures. A delivery database records every attempt, status, and payload so the whole system is observable and replayable."}]]}],["$","p","The flow starts when an event source emits an event to the service, which forwards it through the gateway to the delivery engine. The engine resolves the endpoint from the subscription store, signs the payload, and enqueues a delivery task. Workers pick the task up, perform the POST, and await an acknowledgement. Success is recorded as a single attempt. Failure schedules a retry with backoff through the retry scheduler, and persistent failures eventually land in the dead-letter queue for inspection and replay.",{"className":"mb-2 leading-relaxed","children":["$L3"]}],"$L4"]]}],"$L5","$L6","$L7","$L8"]}]]}],"$L9"]}],"$La"]}]}],["$Lb","$Lc"],"$Ld"]}],"isPartial":"$@e","staleTime":"$f","varyParams":null},{"rsc":"$L10","isPartial":"$@11","staleTime":"$f","varyParams":null},{"rsc":"$L12","isPartial":"$@13","staleTime":"$f","varyParams":"$14"},{"rsc":"$L15","isPartial":"$@16","staleTime":"$f","varyParams":"$14"},{"rsc":"$L17","isPartial":"$@18","staleTime":"$f","varyParams":null}],"isUpgradeableISRFallback":false,"a":"$@19","rootVaryParams":null,"needsRuntimeRequest":"$@1a"}
1c:I[94844,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js"],"default"]
1d:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"OutletBoundary"]
1e:"$Sreact.suspense"
20:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"ViewportBoundary"]
21:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"MetadataBoundary"]
22:I[93226,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default"]
23:I[15750,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default"]
24:I[93226,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"LoadingBoundaryProvider"]
25:I[46791,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/2td1d3x1ljv-f.js"],"default"]
:HL["/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","style"]
:HL["/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","style"]
3:["$","$1","0",{"children":"The flow starts when an event source emits an event to the service, which forwards it through the gateway to the delivery engine. The engine resolves the endpoint from the subscription store, signs the payload, and enqueues a delivery task. Workers pick the task up, perform the POST, and await an acknowledgement. Success is recorded as a single attempt. Failure schedules a retry with backoff through the retry scheduler, and persistent failures eventually land in the dead-letter queue for inspection and replay."}]
4:["$","p","Reliability is the dominant design concern, so everything is built for at-least-once delivery with idempotency. The delivery queue is the source of truth for pending work; if a worker crashes mid-POST, the task is re-delivered. The customer sees an idempotency header on every payload and can deduplicate. The tradeoff is that the customer must be prepared for duplicates, which is standard practice for webhook systems such as Stripe. The design also keeps delivery latency bounded: it never blocks the event source, so producers see a fast acknowledgement and delivery happens on its own schedule.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Reliability is the dominant design concern, so everything is built for at-least-once delivery with idempotency. The delivery queue is the source of truth for pending work; if a worker crashes mid-POST, the task is re-delivered. The customer sees an idempotency header on every payload and can deduplicate. The tradeoff is that the customer must be prepared for duplicates, which is standard practice for webhook systems such as Stripe. The design also keeps delivery latency bounded: it never blocks the event source, so producers see a fast acknowledgement and delivery happens on its own schedule."}]]}]
5:["$","div","How do you deliver events reliably?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you deliver events reliably?"}],[["$","p","Reliable delivery means every accepted event is either delivered or recorded as failed, even when endpoints are down. The delivery queue is durable and replicated, and a task is only acknowledged after the HTTP request completes. The delivery engine batches events for the same endpoint where sensible, but keeps each event independent in the queue so one slow event never blocks the rest. I would write the attempt outcome to the delivery database on every transition, giving an audit trail of exactly what was sent, when, and what the response was.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Reliable delivery means every accepted event is either delivered or recorded as failed, even when endpoints are down. The delivery queue is durable and replicated, and a task is only acknowledged after the HTTP request completes. The delivery engine batches events for the same endpoint where sensible, but keeps each event independent in the queue so one slow event never blocks the rest. I would write the attempt outcome to the delivery database on every transition, giving an audit trail of exactly what was sent, when, and what the response was."}]]}],["$","p","The synchronous path stays minimal. The event source publishes to the gateway, the engine resolves subscriptions and enqueues, and the worker performs the delivery asynchronously. Because the queue sits between production and delivery, a burst of events or a down endpoint does not back up the producers. Ordering is preserved per endpoint when the customer requires it, which I would implement by keying the queue on endpoint so a single consumer processes events for that endpoint in order, while different endpoints run in parallel.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"The synchronous path stays minimal. The event source publishes to the gateway, the engine resolves subscriptions and enqueues, and the worker performs the delivery asynchronously. Because the queue sits between production and delivery, a burst of events or a down endpoint does not back up the producers. Ordering is preserved per endpoint when the customer requires it, which I would implement by keying the queue on endpoint so a single consumer processes events for that endpoint in order, while different endpoints run in parallel."}]]}],["$","p","The hardest part is knowing when a delivery actually succeeded. A 2xx response is success. Anything else is a candidate for retry, but timeouts are ambiguous: the endpoint may have processed the event and then died before responding. The design handles this by making every payload carry an event id and a timestamp, and by documenting at-least-once semantics so customers build idempotent receivers. A reconciliation job scans the delivery database for attempts stuck in flight past a timeout and re-enqueues them, which closes the gap between what the customer saw and what we think they saw.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"The hardest part is knowing when a delivery actually succeeded. A 2xx response is success. Anything else is a candidate for retry, but timeouts are ambiguous: the endpoint may have processed the event and then died before responding. The design handles this by making every payload carry an event id and a timestamp, and by documenting at-least-once semantics so customers build idempotent receivers. A reconciliation job scans the delivery database for attempts stuck in flight past a timeout and re-enqueues them, which closes the gap between what the customer saw and what we think they saw."}]]}]]]}]
6:["$","div","How do you handle retries and backoff?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you handle retries and backoff?"}],[["$","p","Retries exist because transient failures are common and most endpoints recover within seconds or minutes. The retry scheduler maintains a per-delivery schedule with exponential backoff: start at a few seconds, double each attempt, add jitter to avoid thundering herds, and cap the interval at a few minutes. I would separate the retry scheduler from the main delivery queue so failed deliveries do not sit at the head of the queue and block new events. Each retry re-enters the delivery queue with an updated due time, and the scheduler is the component that decides when that happens.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Retries exist because transient failures are common and most endpoints recover within seconds or minutes. The retry scheduler maintains a per-delivery schedule with exponential backoff: start at a few seconds, double each attempt, add jitter to avoid thundering herds, and cap the interval at a few minutes. I would separate the retry scheduler from the main delivery queue so failed deliveries do not sit at the head of the queue and block new events. Each retry re-enters the delivery queue with an updated due time, and the scheduler is the component that decides when that happens."}]]}],["$","p","Not every failure deserves the same treatment. A 429 rate-limit response should retry with respect for the Retry-After header, a 5xx should retry with standard backoff, and a 4xx client error such as 404 usually means the endpoint is misconfigured and should fail fast. I would classify errors on receipt, set an attempt budget per delivery, and move permanently failing deliveries to the dead-letter queue with the full request and response history so an operator can replay them after fixing the endpoint.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Not every failure deserves the same treatment. A 429 rate-limit response should retry with respect for the Retry-After header, a 5xx should retry with standard backoff, and a 4xx client error such as 404 usually means the endpoint is misconfigured and should fail fast. I would classify errors on receipt, set an attempt budget per delivery, and move permanently failing deliveries to the dead-letter queue with the full request and response history so an operator can replay them after fixing the endpoint."}]]}],["$","p","Backoff must coordinate with the endpoint's tolerance. If every failed webhook retries at the same moment, a recovering endpoint gets a burst that knocks it down again, so jitter is mandatory. I would also implement circuit breaking per endpoint: after consecutive failures the scheduler pauses deliveries to that endpoint and resumes on a timer, which protects both the customer's infrastructure and ours. The tradeoff is that circuit breaking adds latency to legitimate deliveries, so the thresholds are tuned per endpoint tier. All retry state is derived from the delivery database, so a scheduler crash does not lose the schedule.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Backoff must coordinate with the endpoint's tolerance. If every failed webhook retries at the same moment, a recovering endpoint gets a burst that knocks it down again, so jitter is mandatory. I would also implement circuit breaking per endpoint: after consecutive failures the scheduler pauses deliveries to that endpoint and resumes on a timer, which protects both the customer's infrastructure and ours. The tradeoff is that circuit breaking adds latency to legitimate deliveries, so the thresholds are tuned per endpoint tier. All retry state is derived from the delivery database, so a scheduler crash does not lose the schedule."}]]}]]]}]
7:["$","div","How do you secure webhook payloads?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you secure webhook payloads?"}],[["$","p","Webhooks are privileged calls from a trusted platform into a customer's infrastructure, so the customer must be able to verify the payload came from us and was not tampered with. I would use asymmetric signing: the platform holds a private key, and each customer endpoint verifies a signature derived from the payload. The signature covers the raw body plus a timestamp, so replaying an old payload with a fresh timestamp fails verification. Customers receive the public key through the dashboard, and the payload signer component generates a fresh signature for every attempt.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Webhooks are privileged calls from a trusted platform into a customer's infrastructure, so the customer must be able to verify the payload came from us and was not tampered with. I would use asymmetric signing: the platform holds a private key, and each customer endpoint verifies a signature derived from the payload. The signature covers the raw body plus a timestamp, so replaying an old payload with a fresh timestamp fails verification. Customers receive the public key through the dashboard, and the payload signer component generates a fresh signature for every attempt."}]]}],["$","p","Replay and timestamping need explicit handling. The signature includes a timestamp, and customers are instructed to reject deliveries older than a small skew window. Because webhooks can legitimately be delayed by retries, I would allow the customer to configure the skew window rather than hardcoding it. Delivery attempts to the same endpoint should be distinguishable; each attempt gets a unique delivery id that is included in the signed data, so a replayed request from a previous attempt is rejected even though the payload event is identical.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Replay and timestamping need explicit handling. The signature includes a timestamp, and customers are instructed to reject deliveries older than a small skew window. Because webhooks can legitimately be delayed by retries, I would allow the customer to configure the skew window rather than hardcoding it. Delivery attempts to the same endpoint should be distinguishable; each attempt gets a unique delivery id that is included in the signed data, so a replayed request from a previous attempt is rejected even though the payload event is identical."}]]}],["$","p","Sensitive content is handled at the source. The signer runs on a dedicated path so keys never enter the application tier, and the delivery engine fetches the signing key from a secret store. Payloads are delivered over TLS only, and delivery bodies may contain sensitive fields; I would let event producers mark fields as redacted so the payload sent to the customer omits them or includes them encrypted to the customer's public key. The delivery database stores payload hashes rather than full bodies where possible, so a database leak does not expose customer data. Verification failures are logged and can trigger alerting, since they usually indicate a misconfigured or compromised endpoint.",{"className":"mb-2 leading-relaxed","children":["$L1b"]}]]]}]
8:["$","div","How do you handle slow or down endpoints?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you handle slow or down endpoints?"}],[["$","p","Slow and down endpoints are the normal case for webhooks, so the system is designed to contain their impact. The delivery queue gives each task an HTTP timeout, and a slow endpoint is detected by measuring p95 latency across attempts. The engine concurrency limits per endpoint prevent slow deliveries from consuming the whole worker pool; when a down endpoint fails its attempts, its tasks back up in the queue while other endpoints proceed. I would isolate endpoints by assigning them dedicated queues, so one customer's outage never delays another customer's deliveries.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Slow and down endpoints are the normal case for webhooks, so the system is designed to contain their impact. The delivery queue gives each task an HTTP timeout, and a slow endpoint is detected by measuring p95 latency across attempts. The engine concurrency limits per endpoint prevent slow deliveries from consuming the whole worker pool; when a down endpoint fails its attempts, its tasks back up in the queue while other endpoints proceed. I would isolate endpoints by assigning them dedicated queues, so one customer's outage never delays another customer's deliveries."}]]}],["$","p","Down endpoints go through a defined lifecycle. When deliveries keep failing, the retry scheduler backs off and eventually marks the endpoint as paused, halting deliveries until a liveness check succeeds. The customer is notified through the dashboard that their endpoint is paused, which is an operator signal they can act on. During the outage the queue absorbs the backlog, bounded by a retention policy; events older than the retention window are dropped into the dead-letter queue so they are not lost silently.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Down endpoints go through a defined lifecycle. When deliveries keep failing, the retry scheduler backs off and eventually marks the endpoint as paused, halting deliveries until a liveness check succeeds. The customer is notified through the dashboard that their endpoint is paused, which is an operator signal they can act on. During the outage the queue absorbs the backlog, bounded by a retention policy; events older than the retention window are dropped into the dead-letter queue so they are not lost silently."}]]}],["$","p","The design trades backlog for simplicity. Keeping events for hours during an outage preserves their value, but an unbounded backlog threatens the whole system, so I would set a per-endpoint cap and a global cap with clear semantics for what gets dropped first. When the endpoint recovers, the engine drains the backlog with a controlled concurrency ramp so it does not overwhelm the customer's now-healthy server. Replay is supported from the delivery database, and the dead-letter queue lets an operator re-inject individual deliveries after fixing a root cause. The result is that a single dead endpoint degrades nothing except its own deliveries.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"The design trades backlog for simplicity. Keeping events for hours during an outage preserves their value, but an unbounded backlog threatens the whole system, so I would set a per-endpoint cap and a global cap with clear semantics for what gets dropped first. When the endpoint recovers, the engine drains the backlog with a controlled concurrency ramp so it does not overwhelm the customer's now-healthy server. Replay is supported from the delivery database, and the dead-letter queue lets an operator re-inject individual deliveries after fixing a root cause. The result is that a single dead endpoint degrades nothing except its own deliveries."}]]}]]]}]
9:["$","details",null,{"className":"mt-8","children":[["$","summary",null,{"className":"cursor-pointer font-medium","children":"Diagram source"}],["$","pre",null,{"className":"bg-base-200 mt-2 overflow-x-auto rounded-lg p-4 font-mono text-xs","children":"title: Webhook Delivery\nnode source: Event Source [icon=cloud]\nnode app: Service [icon=server]\nnode gateway: API Gateway [icon=server]\nnode sub: Subscription Service [icon=compute]\nnode deliver: Delivery Engine [icon=worker]\nnode queue: Delivery Queue [icon=queue]\nnode endpoint: Customer Endpoint [icon=server]\nnode sign: Payload Signer [icon=shield]\nnode retry: Retry Scheduler [icon=cache]\nnode dlq: Dead Letter Queue [icon=queue]\nnode db: Delivery DB [cylinder, icon=database]\n\nedge source -> app: event\nedge app -> gateway: forward\nedge gateway -> deliver: submit\nedge deliver -> sub: endpoints\nedge sub -> queue: enqueue\nedge deliver -> sign: payload\nedge sign -> endpoint: send\nedge endpoint -> deliver: ack\nedge deliver -> retry: backoff\nedge retry -> dlq: fail\nedge deliver -> db: log"}]]}]
a:["$","div",null,{"className":"mt-8","children":["$","$L1c",null,{"text":"title: Webhook Delivery\nnode source: Event Source [icon=cloud]\nnode app: Service [icon=server]\nnode gateway: API Gateway [icon=server]\nnode sub: Subscription Service [icon=compute]\nnode deliver: Delivery Engine [icon=worker]\nnode queue: Delivery Queue [icon=queue]\nnode endpoint: Customer Endpoint [icon=server]\nnode sign: Payload Signer [icon=shield]\nnode retry: Retry Scheduler [icon=cache]\nnode dlq: Dead Letter Queue [icon=queue]\nnode db: Delivery DB [cylinder, icon=database]\n\nedge source -> app: event\nedge app -> gateway: forward\nedge gateway -> deliver: submit\nedge deliver -> sub: endpoints\nedge sub -> queue: enqueue\nedge deliver -> sign: payload\nedge sign -> endpoint: send\nedge endpoint -> deliver: ack\nedge deliver -> retry: backoff\nedge retry -> dlq: fail\nedge deliver -> db: log","name":"Webhook Delivery"}]}]
b:["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","async":true}]
c:["$","script","script-1",{"src":"/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js","async":true}]
d:["$","$L1d",null,{"children":["$","$1e",null,{"name":"Next.MetadataOutlet","children":"$@1f"}]}]
10:["$","$1","h",{"children":[null,["$","$L20",null,{"children":[["$","meta","0",{"charSet":"utf-8"}],["$","meta","1",{"name":"viewport","content":"width=device-width, initial-scale=1"}]]}],["$","div",null,{"hidden":true,"children":["$","$L21",null,{"children":["$","$1e",null,{"name":"Next.Metadata","children":[["$","title","0",{"children":"Webhook Delivery — Diagram Examples"}],["$","meta","1",{"name":"description","content":"Event subscription, delivery, retries, signing."}],["$","link","2",{"rel":"manifest","href":"/manifest.json"}],["$","meta","3",{"name":"mobile-web-app-capable","content":"yes"}],["$","meta","4",{"name":"apple-mobile-web-app-title","content":"Diagram"}],["$","meta","5",{"name":"apple-mobile-web-app-status-bar-style","content":"black-translucent"}]]}]}]}],["$","meta",null,{"name":"next-size-adjust","content":""}]]}]
12:["$","$1","c",{"children":[null,["$","$L22",null,{"parallelRouterKey":"children","template":["$","$L23",null,{}]}]]}]
15:["$","$1","c",{"children":[null,["$","$L22",null,{"parallelRouterKey":"children","template":["$","$L23",null,{}]}]]}]
17:["$","$L24",null,{"loading":[["$","div","l",{"className":"flex h-screen items-center justify-center","children":["$","span",null,{"className":"loading loading-spinner loading-lg text-primary"}]}],[],null],"children":["$","$1","c",{"children":[[["$","link","0",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","precedence":"next"}],["$","link","1",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","precedence":"next"}],["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","async":true}]],["$","html",null,{"lang":"en","data-theme":"diagram","className":"inter_537b2856-module__DOofUa__variable jetbrains_mono_da46d059-module__t9WmUG__variable","children":[["$","head",null,{"children":["$","link",null,{"rel":"apple-touch-icon","href":"/icons/icon-192.png"}]}],["$","body",null,{"className":"h-screen overflow-hidden","children":["$","$L22",null,{"parallelRouterKey":"children","error":"$25","errorStyles":[],"errorScripts":[["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/2td1d3x1ljv-f.js","async":true}]],"template":["$","$L23",null,{}],"notFound":[["$","div",null,{"className":"flex h-screen flex-col items-center justify-center gap-4","children":[["$","h1",null,{"className":"text-2xl","children":"404"}],["$","p",null,{"className":"text-sm","children":"This diagram does not exist."}],["$","a",null,{"href":"/","className":"btn btn-primary btn-sm","children":"Back to editor"}]]}],[]]}]}]]}]]}]}]
1b:["$","$1","0",{"children":"Sensitive content is handled at the source. The signer runs on a dedicated path so keys never enter the application tier, and the delivery engine fetches the signing key from a secret store. Payloads are delivered over TLS only, and delivery bodies may contain sensitive fields; I would let event producers mark fields as redacted so the payload sent to the customer omits them or includes them encrypted to the customer's public key. The delivery database stores payload hashes rather than full bodies where possible, so a database leak does not expose customer data. Verification failures are logged and can trigger alerting, since they usually indicate a misconfigured or compromised endpoint."}]
1f:null
1a:true
f:300
f:C
19:0
11:"$undefined"
13:"$undefined"
16:"$undefined"
18:"$undefined"
e:"$undefined"
