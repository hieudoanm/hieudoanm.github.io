1:"$Sreact.fragment"
2:I[83070,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js"],""]
f:X
14:X
14:C
0:{"buildId":"kwMAhgW1ckibYawstx_6-","data":[{"rsc":["$","$1","c",{"children":[["$","div",null,{"className":"h-screen overflow-y-auto","children":["$","main",null,{"className":"mx-auto max-w-3xl px-4 py-10","children":[["$","p",null,{"className":"mb-6 opacity-60","children":["$","$L2",null,{"className":"link","href":"/posts/","children":"← All posts"}]}],["$","article",null,{"children":[["$","header",null,{"className":"mb-8","children":[["$","h1",null,{"className":"text-3xl font-bold","children":"Backup Service"}],["$","p",null,{"className":"text-base-content/70 mt-2","children":"Scheduling, snapshots, restore, verification."}],["$","p",null,{"className":"text-base-content/50 mt-3 text-sm","children":["Hieu Doan"," · ",["$","span",null,{"className":"capitalize","children":"medium"}]," ·"," ",["$","span",null,{"className":"capitalize","children":"infrastructure"}]]}],["$","div",null,{"className":"mt-3 flex flex-wrap gap-1.5","children":[["$","span","scheduling",{"className":"badge badge-outline badge-sm","children":"scheduling"}],["$","span","security",{"className":"badge badge-outline badge-sm","children":"security"}],["$","span","storage",{"className":"badge badge-outline badge-sm","children":"storage"}]]}]]}],["$","section",null,{"children":[["$","h2",null,{"className":"mb-2 text-xl font-semibold","children":"Interview Questions"}],["$","ul",null,{"className":"list-disc space-y-1 pl-6","children":[["$","li","Design a backup service",{"children":"Design a backup service"}],["$","li","How do you take consistent snapshots?",{"children":"How do you take consistent snapshots?"}],["$","li","How do you store backups efficiently?",{"children":"How do you store backups efficiently?"}],["$","li","How do you restore data quickly?",{"children":"How do you restore data quickly?"}],["$","li","How do you verify backup integrity?",{"children":"How do you verify backup integrity?"}]]}]]}],["$","section",null,{"className":"mt-8","children":[["$","h2",null,{"className":"mb-4 text-xl font-semibold","children":"Answers"}],["$","div",null,{"className":"space-y-6","children":[["$","div","Design a backup service",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"Design a backup service"}],[["$","p","A backup service protects data against loss by periodically copying it to an independent store that survives failures of the primary system. The service needs five capabilities: scheduling, so backups happen on a policy without human action; snapshotting, so each backup captures a consistent point-in-time; storage, so backups are written durably and cheaply; verification, so backups are known to be restorable; and restore, so data can be recovered quickly when needed. A backup that cannot be restored is not a backup — it is wasted disk.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"A backup service protects data against loss by periodically copying it to an independent store that survives failures of the primary system. The service needs five capabilities: scheduling, so backups happen on a policy without human action; snapshotting, so each backup captures a consistent point-in-time; storage, so backups are written durably and cheaply; verification, so backups are known to be restorable; and restore, so data can be recovered quickly when needed. A backup that cannot be restored is not a backup — it is wasted disk."}]]}],["$","p","The architecture separates control from execution. A scheduler holds the backup policy per protected asset (frequency, retention, destination) and triggers runs. The backup engine executes each run: it coordinates a snapshot, splits it into chunks, deduplicates against previously stored chunks, and writes new chunks to the backup store. The store is append-mostly and immutable, which is what protects against ransomware and accidental deletes — an attacker who compromises the primary cannot retroactively alter history. A verification layer re-reads chunks and, for databases, replays them into a throwaway instance to prove recoverability.",{"className":"mb-2 leading-relaxed","children":["$L3"]}],"$L4"]]}],"$L5","$L6","$L7","$L8"]}]]}],"$L9"]}],"$La"]}]}],["$Lb","$Lc"],"$Ld"]}],"isPartial":"$@e","staleTime":"$f","varyParams":null},{"rsc":"$L10","isPartial":"$@11","staleTime":"$f","varyParams":null},{"rsc":"$L12","isPartial":"$@13","staleTime":"$f","varyParams":"$14"},{"rsc":"$L15","isPartial":"$@16","staleTime":"$f","varyParams":"$14"},{"rsc":"$L17","isPartial":"$@18","staleTime":"$f","varyParams":null}],"isUpgradeableISRFallback":false,"a":"$@19","rootVaryParams":null,"needsRuntimeRequest":"$@1a"}
1d:I[94844,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js"],"default"]
1e:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"OutletBoundary"]
1f:"$Sreact.suspense"
21:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"ViewportBoundary"]
22:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"MetadataBoundary"]
23:I[93226,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default"]
24:I[15750,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default"]
25:I[93226,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"LoadingBoundaryProvider"]
26:I[46791,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/2td1d3x1ljv-f.js"],"default"]
:HL["/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","style"]
:HL["/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","style"]
3:["$","$1","0",{"children":"The architecture separates control from execution. A scheduler holds the backup policy per protected asset (frequency, retention, destination) and triggers runs. The backup engine executes each run: it coordinates a snapshot, splits it into chunks, deduplicates against previously stored chunks, and writes new chunks to the backup store. The store is append-mostly and immutable, which is what protects against ransomware and accidental deletes — an attacker who compromises the primary cannot retroactively alter history. A verification layer re-reads chunks and, for databases, replays them into a throwaway instance to prove recoverability."}]
4:["$","p","The design decisions that matter are consistency (can a snapshot be applied correctly?), efficiency (how much storage and bandwidth does history cost?), and recovery time (how fast can the newest and the oldest backups be restored?). These three goals pull against each other: full snapshots restore fast but cost the most; incremental-only backups cost little but chain restores together; and verification costs resources that protect the one property that actually matters — the ability to come back from data loss.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"The design decisions that matter are consistency (can a snapshot be applied correctly?), efficiency (how much storage and bandwidth does history cost?), and recovery time (how fast can the newest and the oldest backups be restored?). These three goals pull against each other: full snapshots restore fast but cost the most; incremental-only backups cost little but chain restores together; and verification costs resources that protect the one property that actually matters — the ability to come back from data loss."}]]}]
5:["$","div","How do you take consistent snapshots?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you take consistent snapshots?"}],[["$","p","Consistency is about producing a snapshot that represents a valid state of the data as of a single point in time, even though the data is being written constantly. For databases the problem is sharp: copying tables one by one can yield a mixture of transactions where row A reflects a later state than row B, and replaying that combination corrupts the restore. The standard solution is to coordinate with the database's own consistency machinery, using hot backups on transactional engines (where the engine exposes a snapshot transaction) or quiescing writes briefly while files are copied.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Consistency is about producing a snapshot that represents a valid state of the data as of a single point in time, even though the data is being written constantly. For databases the problem is sharp: copying tables one by one can yield a mixture of transactions where row A reflects a later state than row B, and replaying that combination corrupts the restore. The standard solution is to coordinate with the database's own consistency machinery, using hot backups on transactional engines (where the engine exposes a snapshot transaction) or quiescing writes briefly while files are copied."}]]}],["$","p","A common practical approach is the \"copy then apply log\" pattern. The backup engine first records the current log position, then copies the data files, then notes the log position at copy completion. The backup is the data files plus the log segment between the two positions, so a restore replays exactly the transactions that fall in that window. This gives crash-consistency without stopping writes, and it is why backups of database-backed systems almost always involve the database's log, not just its files.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"A common practical approach is the \"copy then apply log\" pattern. The backup engine first records the current log position, then copies the data files, then notes the log position at copy completion. The backup is the data files plus the log segment between the two positions, so a restore replays exactly the transactions that fall in that window. This gives crash-consistency without stopping writes, and it is why backups of database-backed systems almost always involve the database's log, not just its files."}]]}],["$","p","For distributed systems, a single consistent snapshot is not enough; the snapshot must also be ordered across components so a restore does not reintroduce a read that references a record the backup of another component dropped. The design handles this with a global snapshot coordination point that takes the snapshot at a well-defined offset across all volumes, then uses application-level reconciliation after restore for the remaining skew. Whatever the mechanism, the snapshot metadata records the exact time and log position, because that metadata is what makes incremental backup and point-in-time restore possible later.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"For distributed systems, a single consistent snapshot is not enough; the snapshot must also be ordered across components so a restore does not reintroduce a read that references a record the backup of another component dropped. The design handles this with a global snapshot coordination point that takes the snapshot at a well-defined offset across all volumes, then uses application-level reconciliation after restore for the remaining skew. Whatever the mechanism, the snapshot metadata records the exact time and log position, because that metadata is what makes incremental backup and point-in-time restore possible later."}]]}]]]}]
6:["$","div","How do you store backups efficiently?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you store backups efficiently?"}],[["$","p","The two levers for storage efficiency are deduplication and incremental backup, used together. Deduplication operates at the chunk level: the engine splits every file into fixed-size or content-defined chunks, hashes each chunk, and stores only chunks not already present in the store. Chunk-level hashing means a 2 KB edit to a large file costs only the affected chunks, and common data shared across many machines (an OS image, a shared library) is stored once globally. Content-defined chunking, where boundaries are derived from the data rather than fixed offsets, makes deduplication robust to inserts that would shift every fixed-size chunk.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"The two levers for storage efficiency are deduplication and incremental backup, used together. Deduplication operates at the chunk level: the engine splits every file into fixed-size or content-defined chunks, hashes each chunk, and stores only chunks not already present in the store. Chunk-level hashing means a 2 KB edit to a large file costs only the affected chunks, and common data shared across many machines (an OS image, a shared library) is stored once globally. Content-defined chunking, where boundaries are derived from the data rather than fixed offsets, makes deduplication robust to inserts that would shift every fixed-size chunk."}]]}],["$","p","Incremental backup complements deduplication by never resending what has not changed. A periodic incremental compares the current snapshot against the last one, uploads only changed chunks, and records a new manifest that lists the chunks composing the backup. The manifest is small and cheap to keep; the chunks are large and stored once. Retention then becomes a manifest-management problem: deleting an old backup means discarding its manifest and garbage- collecting any chunks no longer referenced by any remaining manifest, which runs offline to avoid corrupting the store during active backup.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Incremental backup complements deduplication by never resending what has not changed. A periodic incremental compares the current snapshot against the last one, uploads only changed chunks, and records a new manifest that lists the chunks composing the backup. The manifest is small and cheap to keep; the chunks are large and stored once. Retention then becomes a manifest-management problem: deleting an old backup means discarding its manifest and garbage- collecting any chunks no longer referenced by any remaining manifest, which runs offline to avoid corrupting the store during active backup."}]]}],["$","p","Storage tiering contains the cost of history. New backups land on fast tiered storage for quick recovery of recent data, and cold history rolls to cheaper object or archive tiers according to policy. Compression and encryption are applied before the chunks leave the backup engine — compression reduces bandwidth and space, encryption ensures the backup store cannot be read if it is exfiltrated, and both run before the bytes hit the network so the store never sees plaintext. The efficiency gains are substantial in practice: with daily incrementals and monthly fulls, a year of history typically costs a small multiple of one full backup's size.",{"className":"mb-2 leading-relaxed","children":["$L1b"]}]]]}]
7:["$","div","How do you restore data quickly?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you restore data quickly?"}],[["$","p","Restore speed is measured two ways: how fast the most recent state can be recovered, and how fast any historical point can be reached. Recent-state restore is optimized by keeping the latest full snapshot plus its recent incrementals in fast storage, and by restoring in parallel — chunk downloads are fanned out across storage workers, and the engine reassembles files concurrently. A database restore additionally replays the log segment, which is deliberately the smallest replayable unit, so the engine downloads chunks, writes files, and replays the log to land at the exact desired point.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Restore speed is measured two ways: how fast the most recent state can be recovered, and how fast any historical point can be reached. Recent-state restore is optimized by keeping the latest full snapshot plus its recent incrementals in fast storage, and by restoring in parallel — chunk downloads are fanned out across storage workers, and the engine reassembles files concurrently. A database restore additionally replays the log segment, which is deliberately the smallest replayable unit, so the engine downloads chunks, writes files, and replays the log to land at the exact desired point."}]]}],["$","p","Point-in-time recovery (PITR) is where the snapshot model pays off. Because the backup engine records the log position of every snapshot, an operator can request \"the database as of 3:14 PM Tuesday\", and the restore downloads the snapshot closest before that time plus the log segments up to the target timestamp. The engine then replays those segments, then truncates at the target. PITR converts a backup system from \"you get last night\" to \"you get the state just before the incident\", which is usually the operational goal.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Point-in-time recovery (PITR) is where the snapshot model pays off. Because the backup engine records the log position of every snapshot, an operator can request \"the database as of 3:14 PM Tuesday\", and the restore downloads the snapshot closest before that time plus the log segments up to the target timestamp. The engine then replays those segments, then truncates at the target. PITR converts a backup system from \"you get last night\" to \"you get the state just before the incident\", which is usually the operational goal."}]]}],["$","p","Restore is also a scaling exercise: the first question after a failure is how many restores can run in parallel without exhausting storage bandwidth or CPU. The design therefore treats restores as first-class jobs with their own queue, priority, and progress tracking, and it pre-computes per-job chunk lists so the heavy planning does not happen during the recovery window. Finally, restore is tested, not assumed: the verification process described next exercises the restore path on a schedule, so when the real recovery happens the code path has already proven itself in production.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Restore is also a scaling exercise: the first question after a failure is how many restores can run in parallel without exhausting storage bandwidth or CPU. The design therefore treats restores as first-class jobs with their own queue, priority, and progress tracking, and it pre-computes per-job chunk lists so the heavy planning does not happen during the recovery window. Finally, restore is tested, not assumed: the verification process described next exercises the restore path on a schedule, so when the real recovery happens the code path has already proven itself in production."}]]}]]]}]
8:["$","div","How do you verify backup integrity?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you verify backup integrity?"}],[["$","p","Verification exists because backups fail silently: the primary looks fine, the storage looks fine, and the corruption or missing chunk is only discovered at restore time, when it is too late. The first layer is automated, lightweight verification on every backup run. After chunks are written, the engine re-reads a sample and recomputes hashes to detect corrupt or short writes, and each backup's manifest is checked to confirm every referenced chunk exists in the store. These checks catch the common causes — partial uploads, silent disk errors, lost objects.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Verification exists because backups fail silently: the primary looks fine, the storage looks fine, and the corruption or missing chunk is only discovered at restore time, when it is too late. The first layer is automated, lightweight verification on every backup run. After chunks are written, the engine re-reads a sample and recomputes hashes to detect corrupt or short writes, and each backup's manifest is checked to confirm every referenced chunk exists in the store. These checks catch the common causes — partial uploads, silent disk errors, lost objects."}]]}],["$","p","The second layer is full integrity verification on a schedule. The verifier walks every manifest, confirms every chunk is present, and recomputes chunk hashes against the stored hash records. For database backups this runs deeper: the verifier spins up a throwaway restore of the latest backup into a disposable instance, replays the log, and runs validation queries (table counts, checksums, a representative query set) to prove the backup actually produces a working database. This is the strongest signal the service can emit, because it exercises the exact restore path with real machinery.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"The second layer is full integrity verification on a schedule. The verifier walks every manifest, confirms every chunk is present, and recomputes chunk hashes against the stored hash records. For database backups this runs deeper: the verifier spins up a throwaway restore of the latest backup into a disposable instance, replays the log, and runs validation queries (table counts, checksums, a representative query set) to prove the backup actually produces a working database. This is the strongest signal the service can emit, because it exercises the exact restore path with real machinery."}]]}],["$","p","The third layer is the restore drill. Periodic tests restore the oldest retained backup, a mid-history point, and a PITR target into a sandbox, then report success or failure per target. If a backup is found corrupt, the pipeline reacts automatically: it raises an alert, quarantines the corrupt data, and if possible reconstructs the affected chunks from adjacent snapshots or by re-capturing from the primary. Every verification result is recorded in the backup catalog, so the catalog becomes a report card proving the stated retention policy is real — and it is the first thing an auditor or an operator checks during an incident.",{"className":"mb-2 leading-relaxed","children":["$L1c"]}]]]}]
9:["$","details",null,{"className":"mt-8","children":[["$","summary",null,{"className":"cursor-pointer font-medium","children":"Diagram source"}],["$","pre",null,{"className":"bg-base-200 mt-2 overflow-x-auto rounded-lg p-4 font-mono text-xs","children":"title: Backup Service\nnode admin: Operator [round, icon=browser]\nnode app: Backup Console [icon=browser]\nnode gateway: API Gateway [icon=server]\nnode schedule: Scheduler [icon=compute]\nnode backup: Backup Engine [icon=worker]\nnode snapshot: Snapshot Service [icon=compute]\nnode store: Backup Store [cylinder, icon=file]\nnode dedupe: Deduplication [icon=cache]\nnode verify: Verification [icon=shield]\nnode restore: Restore Service [icon=compute]\nnode db: Backup DB [cylinder, icon=database]\n\nedge admin -> app: configure\nedge app -> gateway: schedule\nedge gateway -> schedule: policy\nedge schedule -> backup: trigger\nedge backup -> snapshot: capture\nedge snapshot -> dedupe: chunks\nedge dedupe -> store: save\nedge backup -> verify: check\nedge verify -> db: status\nedge admin -> app: restore\nedge app -> restore: recover"}]]}]
a:["$","div",null,{"className":"mt-8","children":["$","$L1d",null,{"text":"title: Backup Service\nnode admin: Operator [round, icon=browser]\nnode app: Backup Console [icon=browser]\nnode gateway: API Gateway [icon=server]\nnode schedule: Scheduler [icon=compute]\nnode backup: Backup Engine [icon=worker]\nnode snapshot: Snapshot Service [icon=compute]\nnode store: Backup Store [cylinder, icon=file]\nnode dedupe: Deduplication [icon=cache]\nnode verify: Verification [icon=shield]\nnode restore: Restore Service [icon=compute]\nnode db: Backup DB [cylinder, icon=database]\n\nedge admin -> app: configure\nedge app -> gateway: schedule\nedge gateway -> schedule: policy\nedge schedule -> backup: trigger\nedge backup -> snapshot: capture\nedge snapshot -> dedupe: chunks\nedge dedupe -> store: save\nedge backup -> verify: check\nedge verify -> db: status\nedge admin -> app: restore\nedge app -> restore: recover","name":"Backup Service"}]}]
b:["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","async":true}]
c:["$","script","script-1",{"src":"/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js","async":true}]
d:["$","$L1e",null,{"children":["$","$1f",null,{"name":"Next.MetadataOutlet","children":"$@20"}]}]
10:["$","$1","h",{"children":[null,["$","$L21",null,{"children":[["$","meta","0",{"charSet":"utf-8"}],["$","meta","1",{"name":"viewport","content":"width=device-width, initial-scale=1"}]]}],["$","div",null,{"hidden":true,"children":["$","$L22",null,{"children":["$","$1f",null,{"name":"Next.Metadata","children":[["$","title","0",{"children":"Backup Service — Diagram Examples"}],["$","meta","1",{"name":"description","content":"Scheduling, snapshots, restore, verification."}],["$","link","2",{"rel":"manifest","href":"/manifest.json"}],["$","meta","3",{"name":"mobile-web-app-capable","content":"yes"}],["$","meta","4",{"name":"apple-mobile-web-app-title","content":"Diagram"}],["$","meta","5",{"name":"apple-mobile-web-app-status-bar-style","content":"black-translucent"}]]}]}]}],["$","meta",null,{"name":"next-size-adjust","content":""}]]}]
12:["$","$1","c",{"children":[null,["$","$L23",null,{"parallelRouterKey":"children","template":["$","$L24",null,{}]}]]}]
15:["$","$1","c",{"children":[null,["$","$L23",null,{"parallelRouterKey":"children","template":["$","$L24",null,{}]}]]}]
17:["$","$L25",null,{"loading":[["$","div","l",{"className":"flex h-screen items-center justify-center","children":["$","span",null,{"className":"loading loading-spinner loading-lg text-primary"}]}],[],null],"children":["$","$1","c",{"children":[[["$","link","0",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","precedence":"next"}],["$","link","1",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","precedence":"next"}],["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","async":true}]],["$","html",null,{"lang":"en","data-theme":"diagram","className":"inter_537b2856-module__DOofUa__variable jetbrains_mono_da46d059-module__t9WmUG__variable","children":[["$","head",null,{"children":["$","link",null,{"rel":"apple-touch-icon","href":"/icons/icon-192.png"}]}],["$","body",null,{"className":"h-screen overflow-hidden","children":["$","$L23",null,{"parallelRouterKey":"children","error":"$26","errorStyles":[],"errorScripts":[["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/2td1d3x1ljv-f.js","async":true}]],"template":["$","$L24",null,{}],"notFound":[["$","div",null,{"className":"flex h-screen flex-col items-center justify-center gap-4","children":[["$","h1",null,{"className":"text-2xl","children":"404"}],["$","p",null,{"className":"text-sm","children":"This diagram does not exist."}],["$","a",null,{"href":"/","className":"btn btn-primary btn-sm","children":"Back to editor"}]]}],[]]}]}]]}]]}]}]
1b:["$","$1","0",{"children":"Storage tiering contains the cost of history. New backups land on fast tiered storage for quick recovery of recent data, and cold history rolls to cheaper object or archive tiers according to policy. Compression and encryption are applied before the chunks leave the backup engine — compression reduces bandwidth and space, encryption ensures the backup store cannot be read if it is exfiltrated, and both run before the bytes hit the network so the store never sees plaintext. The efficiency gains are substantial in practice: with daily incrementals and monthly fulls, a year of history typically costs a small multiple of one full backup's size."}]
1c:["$","$1","0",{"children":"The third layer is the restore drill. Periodic tests restore the oldest retained backup, a mid-history point, and a PITR target into a sandbox, then report success or failure per target. If a backup is found corrupt, the pipeline reacts automatically: it raises an alert, quarantines the corrupt data, and if possible reconstructs the affected chunks from adjacent snapshots or by re-capturing from the primary. Every verification result is recorded in the backup catalog, so the catalog becomes a report card proving the stated retention policy is real — and it is the first thing an auditor or an operator checks during an incident."}]
20:null
1a:true
f:300
f:C
19:0
11:"$undefined"
13:"$undefined"
16:"$undefined"
18:"$undefined"
e:"$undefined"
