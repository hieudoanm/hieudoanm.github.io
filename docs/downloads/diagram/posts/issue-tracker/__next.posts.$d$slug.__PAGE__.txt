1:"$Sreact.fragment"
2:I[83070,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js"],""]
f:X
14:X
14:C
0:{"buildId":"YtoKbkX7y76MytYfCubSL","data":[{"rsc":["$","$1","c",{"children":[["$","div",null,{"className":"h-screen overflow-y-auto","children":["$","main",null,{"className":"mx-auto max-w-3xl px-4 py-10","children":[["$","p",null,{"className":"mb-6 opacity-60","children":["$","$L2",null,{"className":"link","href":"/posts/","children":"← All posts"}]}],["$","article",null,{"children":[["$","header",null,{"className":"mb-8","children":[["$","h1",null,{"className":"text-3xl font-bold","children":"Jira — Issue Tracker"}],["$","p",null,{"className":"text-base-content/70 mt-2","children":"Issues, sprints, boards, workflows, search, notifications."}],["$","p",null,{"className":"text-base-content/50 mt-3 text-sm","children":["Hieu Doan"," · ",["$","span",null,{"className":"capitalize","children":"hard"}]," ·"," ",["$","span",null,{"className":"capitalize","children":"productivity"}]]}],["$","div",null,{"className":"mt-3 flex flex-wrap gap-1.5","children":[["$","span","notification",{"className":"badge badge-outline badge-sm","children":"notification"}],["$","span","search",{"className":"badge badge-outline badge-sm","children":"search"}]]}]]}],["$","section",null,{"children":[["$","h2",null,{"className":"mb-2 text-xl font-semibold","children":"Interview Questions"}],["$","ul",null,{"className":"list-disc space-y-1 pl-6","children":[["$","li","Design an issue tracking system",{"children":"Design an issue tracking system"}],["$","li","How do you model issues, projects, and workflows?",{"children":"How do you model issues, projects, and workflows?"}],["$","li","How do you power board and backlog queries?",{"children":"How do you power board and backlog queries?"}],["$","li","How do you support real-time collaboration?",{"children":"How do you support real-time collaboration?"}],["$","li","How do you scale search across many issues?",{"children":"How do you scale search across many issues?"}]]}]]}],["$","section",null,{"className":"mt-8","children":[["$","h2",null,{"className":"mb-4 text-xl font-semibold","children":"Answers"}],["$","div",null,{"className":"space-y-6","children":[["$","div","Design an issue tracking system",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"Design an issue tracking system"}],[["$","p","An issue tracker such as Jira organizes software teams around a single artifact: the issue. Every bug, task, story, or epic is an issue belonging to a project and passing through a workflow of states such as To Do, In Progress, In Review, and Done. The system therefore has a small set of core concepts: the Issue Service owns issue records and their fields, the Project Service links issues to projects and enforces project-level settings, the Workflow Engine validates state transitions, and the Sprint Service groups issues into time-boxed iterations. All of these sit behind a stateless API gateway so the client only ever talks to one entry point.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"An issue tracker such as Jira organizes software teams around a single artifact: the issue. Every bug, task, story, or epic is an issue belonging to a project and passing through a workflow of states such as To Do, In Progress, In Review, and Done. The system therefore has a small set of core concepts: the Issue Service owns issue records and their fields, the Project Service links issues to projects and enforces project-level settings, the Workflow Engine validates state transitions, and the Sprint Service groups issues into time-boxed iterations. All of these sit behind a stateless API gateway so the client only ever talks to one entry point."}]]}],["$","p","The write path is where correctness matters. Creating an issue validates required fields, enforces permissions, assigns a stable key, and writes to the Issues DB, a sharded relational store split by project or tenant. Transitions are guarded by the workflow so an issue cannot move to a state the project does not allow, and side effects such as reassignment, comment, or notification are published as events after the transaction commits. Search indexing is driven off those same events, so the search index is eventually consistent with the operational store but never blocks the write. This event-driven split is the backbone of the whole design.",{"className":"mb-2 leading-relaxed","children":["$L3"]}],"$L4"]]}],"$L5","$L6","$L7","$L8"]}]]}],"$L9"]}],"$La"]}]}],["$Lb","$Lc"],"$Ld"]}],"isPartial":"$@e","staleTime":"$f","varyParams":null},{"rsc":"$L10","isPartial":"$@11","staleTime":"$f","varyParams":null},{"rsc":"$L12","isPartial":"$@13","staleTime":"$f","varyParams":"$14"},{"rsc":"$L15","isPartial":"$@16","staleTime":"$f","varyParams":"$14"},{"rsc":"$L17","isPartial":"$@18","staleTime":"$f","varyParams":null}],"isUpgradeableISRFallback":false,"a":"$@19","rootVaryParams":null,"needsRuntimeRequest":"$@1a"}
1f:I[94844,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js"],"default"]
20:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"OutletBoundary"]
21:"$Sreact.suspense"
23:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"ViewportBoundary"]
24:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"MetadataBoundary"]
25:I[93226,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default"]
26:I[15750,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default"]
27:I[93226,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"LoadingBoundaryProvider"]
28:I[46791,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/2td1d3x1ljv-f.js"],"default"]
:HL["/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","style"]
:HL["/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","style"]
3:["$","$1","0",{"children":"The write path is where correctness matters. Creating an issue validates required fields, enforces permissions, assigns a stable key, and writes to the Issues DB, a sharded relational store split by project or tenant. Transitions are guarded by the workflow so an issue cannot move to a state the project does not allow, and side effects such as reassignment, comment, or notification are published as events after the transaction commits. Search indexing is driven off those same events, so the search index is eventually consistent with the operational store but never blocks the write. This event-driven split is the backbone of the whole design."}]
4:["$","p","Reads dominate the workload. Boards and backlogs are filtered, sorted views of potentially thousands of issues, and dashboards aggregate across many projects. The fast path caches board state and materialized lists, while the search index powers free-text and JQL-style queries. Realtime collaboration is handled by a push layer that fans out change events over WebSockets to every client watching a board or issue, so a transition made by one developer appears on every teammate's screen in near real time. The result is a system where writes are transactional and auditable while reads are eventually consistent and fast.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Reads dominate the workload. Boards and backlogs are filtered, sorted views of potentially thousands of issues, and dashboards aggregate across many projects. The fast path caches board state and materialized lists, while the search index powers free-text and JQL-style queries. Realtime collaboration is handled by a push layer that fans out change events over WebSockets to every client watching a board or issue, so a transition made by one developer appears on every teammate's screen in near real time. The result is a system where writes are transactional and auditable while reads are eventually consistent and fast."}]]}]
5:["$","div","How do you model issues, projects, and workflows?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you model issues, projects, and workflows?"}],[["$","p","The issue is a rich document on top of a relational core. Every issue has a stable identifier such as PROJECT-123, a type, a summary, a description, a reporter, an assignee, and a status; teams then add custom fields such as story points, sprint, priority, and severity. The Project Service scopes these fields, because each project defines which issue types and fields are available, and it owns configuration such as default assignees and templates. A workflow defines the allowed statuses and the transitions between them, optionally with conditions, validators, and post-functions that run when a transition fires. This makes the workflow a small domain-specific state machine executed by the Workflow Engine.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"The issue is a rich document on top of a relational core. Every issue has a stable identifier such as PROJECT-123, a type, a summary, a description, a reporter, an assignee, and a status; teams then add custom fields such as story points, sprint, priority, and severity. The Project Service scopes these fields, because each project defines which issue types and fields are available, and it owns configuration such as default assignees and templates. A workflow defines the allowed statuses and the transitions between them, optionally with conditions, validators, and post-functions that run when a transition fires. This makes the workflow a small domain-specific state machine executed by the Workflow Engine."}]]}],["$","p","Modeling choices matter at scale. Storing all fields on one wide row becomes unwieldy once teams customize heavily, so the canonical design keeps core columns typed and moves custom fields into an extension table or a JSONB column, with indexes on the fields teams filter by. Status is stored both as the current value on the issue and as an append-only history of transitions, so a team can answer questions like how long an issue spent in review. Sprints are modeled as a group plus a membership table, because an issue can move between sprints, and boards are derived projections: a board is just a saved filter over projects, assignees, and statuses rather than its own storage.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Modeling choices matter at scale. Storing all fields on one wide row becomes unwieldy once teams customize heavily, so the canonical design keeps core columns typed and moves custom fields into an extension table or a JSONB column, with indexes on the fields teams filter by. Status is stored both as the current value on the issue and as an append-only history of transitions, so a team can answer questions like how long an issue spent in review. Sprints are modeled as a group plus a membership table, because an issue can move between sprints, and boards are derived projections: a board is just a saved filter over projects, assignees, and statuses rather than its own storage."}]]}],["$","p","The workflow is versioned. When a team edits a workflow, the engine must keep old transitions valid for issues already in flight, so each transition record points to the workflow version that defined it. Approval gates and parallel statuses complicate the state machine further; some teams run multiple concurrent states, such as a code-review state and a QA state at once. The pragmatic answer is to model the workflow as a directed graph with explicit nodes and edges in a table rather than hard-coded statuses, so arbitrary graphs can be loaded and validated at runtime without deploying new code.",{"className":"mb-2 leading-relaxed","children":["$L1b"]}]]]}]
6:["$","div","How do you power board and backlog queries?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you power board and backlog queries?"}],[["$","p","Boards and backlogs are read-heavy views over issues: a board shows the current sprint filtered by status columns, while the backlog shows unstarted, unscheduled issues sorted by priority. Computing these by scanning the issues table on every request does not scale past a few thousand issues per project. The pragmatic approach is to maintain materialized projections. A background worker consumes issue change events and rebuilds board and backlog views into a projection store, so reading a board is a single keyed lookup rather than a multi-join query. The projection is keyed by project and sprint, with columns for the exact statuses and sort orders a board needs.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Boards and backlogs are read-heavy views over issues: a board shows the current sprint filtered by status columns, while the backlog shows unstarted, unscheduled issues sorted by priority. Computing these by scanning the issues table on every request does not scale past a few thousand issues per project. The pragmatic approach is to maintain materialized projections. A background worker consumes issue change events and rebuilds board and backlog views into a projection store, so reading a board is a single keyed lookup rather than a multi-join query. The projection is keyed by project and sprint, with columns for the exact statuses and sort orders a board needs."}]]}],["$","p","Queries still need flexibility. Teams filter by assignee, priority, label, epic, and custom fields, and they expect results in under a hundred milliseconds. Two complementary engines cover this: an OLTP index for structured filters and a search index for free text and complex JQL. The structured path can be served from projections with bitmap-style indexes on filter columns, while anything touching text, comments, or history goes to the search engine. Keeping both consistent requires an outbox pattern, where the issue transaction publishes events that both the projection builder and the search indexer consume; failures are retried by comparing a cursor of event IDs.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Queries still need flexibility. Teams filter by assignee, priority, label, epic, and custom fields, and they expect results in under a hundred milliseconds. Two complementary engines cover this: an OLTP index for structured filters and a search index for free text and complex JQL. The structured path can be served from projections with bitmap-style indexes on filter columns, while anything touching text, comments, or history goes to the search engine. Keeping both consistent requires an outbox pattern, where the issue transaction publishes events that both the projection builder and the search indexer consume; failures are retried by comparing a cursor of event IDs."}]]}],["$","p","Caching sits in front of both engines. Board definitions and rendered column layouts are cached by project, and frequently opened backlogs are cached with a short TTL, invalidated on the next change event for that project. Because a single issue change can affect many boards, invalidation is coarse: the event carries the project and sprint keys, and the cache drops every affected entry rather than trying to diff views precisely. This over-invalidation is cheap compared with the cost of serving a stale board, and it keeps the projection builder idempotent and simple to reason about.",{"className":"mb-2 leading-relaxed","children":["$L1c"]}]]]}]
7:["$","div","How do you support real-time collaboration?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you support real-time collaboration?"}],[["$","p","Real-time collaboration in an issue tracker means every participant sees changes without refreshing: a comment appears, a status flips, an assignee changes, a drag-and-drop reorders the board. The architecture is a publish-subscribe layer on top of the event stream. When the issue service commits a transaction, it publishes a domain event describing the change and the affected resources. A realtime service subscribes to that stream, enriches the event with the payload needed to render it, and pushes it over WebSockets to every client that currently has the board or issue open. Clients filter incoming messages by their subscribed keys, so a client watching board A does not render events for board B.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Real-time collaboration in an issue tracker means every participant sees changes without refreshing: a comment appears, a status flips, an assignee changes, a drag-and-drop reorders the board. The architecture is a publish-subscribe layer on top of the event stream. When the issue service commits a transaction, it publishes a domain event describing the change and the affected resources. A realtime service subscribes to that stream, enriches the event with the payload needed to render it, and pushes it over WebSockets to every client that currently has the board or issue open. Clients filter incoming messages by their subscribed keys, so a client watching board A does not render events for board B."}]]}],["$","p","Connection management is the scaling problem. Each WebSocket is a long-lived connection, and a big tenant can have tens of thousands open at once. The realtime service is therefore stateless for routing, with connection state held in a shared store and a consistent hash so a client's session is sticky to one node; a fan-out layer groups connections by subscribed topic so a single event is not duplicated per connection. Heartbeats, reconnect with sequence-based catch-up, and delivery receipts guard against flaky mobile connections. On reconnect, the client fetches the latest state via the API and reconciles it against the missed push events.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Connection management is the scaling problem. Each WebSocket is a long-lived connection, and a big tenant can have tens of thousands open at once. The realtime service is therefore stateless for routing, with connection state held in a shared store and a consistent hash so a client's session is sticky to one node; a fan-out layer groups connections by subscribed topic so a single event is not duplicated per connection. Heartbeats, reconnect with sequence-based catch-up, and delivery receipts guard against flaky mobile connections. On reconnect, the client fetches the latest state via the API and reconciles it against the missed push events."}]]}],["$","p","Ordering and conflicts are handled at the source of truth, not the push layer. Events carry a monotonically increasing sequence, and clients that reconnect replay missed ranges from the realtime buffer. Drag-and-drop reordering is the classic race: two people dragging different cards at once. The system resolves this with optimistic UI plus server-side serialization, where the drag operation is a transaction that recomputes the order and the losing client receives a corrected snapshot. This keeps the server authoritative while preserving the feel of instant, shared interaction.",{"className":"mb-2 leading-relaxed","children":["$L1d"]}]]]}]
8:["$","div","How do you scale search across many issues?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you scale search across many issues?"}],[["$","p","Search in Jira means both free text over summaries and descriptions and structured JQL over fields like status, assignee, and sprint. Free text is served by a search engine such as Lucene or Elasticsearch. Documents are the issues, containing summary, description, comments, and key fields, and they are indexed incrementally from the change-event stream so indexing never sits on the write path. Sharding is by project or a range of keys, because almost every search is scoped to one project or a small set of projects; this keeps queries within a few shards and makes the index hot for the common case.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Search in Jira means both free text over summaries and descriptions and structured JQL over fields like status, assignee, and sprint. Free text is served by a search engine such as Lucene or Elasticsearch. Documents are the issues, containing summary, description, comments, and key fields, and they are indexed incrementally from the change-event stream so indexing never sits on the write path. Sharding is by project or a range of keys, because almost every search is scoped to one project or a small set of projects; this keeps queries within a few shards and makes the index hot for the common case."}]]}],["$","p","Structured queries are a different beast. JQL can join across projects, filter by custom fields, and combine with boolean logic. Translating JQL to a single query language is fragile, so the platform translates it into a query plan: the search engine handles the text portion and facets, while the relational store or a columnar index handles the structured filters, and the results are intersected. To keep this fast, documents are enriched with the field values that filters commonly use, so the search engine itself can satisfy many JQL filters without hitting the database. Fields the platform does not know about are handled by a schema registry that maps them to index attributes.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Structured queries are a different beast. JQL can join across projects, filter by custom fields, and combine with boolean logic. Translating JQL to a single query language is fragile, so the platform translates it into a query plan: the search engine handles the text portion and facets, while the relational store or a columnar index handles the structured filters, and the results are intersected. To keep this fast, documents are enriched with the field values that filters commonly use, so the search engine itself can satisfy many JQL filters without hitting the database. Fields the platform does not know about are handled by a schema registry that maps them to index attributes."}]]}],["$","p","Relevance and freshness pull in opposite directions. Bulk reindexing keeps the index coherent after migrations, while continuous incremental indexing keeps it current; the design uses both, with a nightly rebuild for defragmentation and correction. Per-tenant indexes isolate noisy tenants and let the system size shards by data volume rather than by global count. Finally, permission filtering is a major cost: every result must be checked against project and issue-level permissions before display. The index stores the ACL groups each issue is visible to, so permission checks become a set intersection during query rather than a post-query join that would destroy latency.",{"className":"mb-2 leading-relaxed","children":["$L1e"]}]]]}]
9:["$","details",null,{"className":"mt-8","children":[["$","summary",null,{"className":"cursor-pointer font-medium","children":"Diagram source"}],["$","pre",null,{"className":"bg-base-200 mt-2 overflow-x-auto rounded-lg p-4 font-mono text-xs","children":"title: Issue Tracker\nnode dev: Developer [round, icon=browser]\nnode app: Tracker App [icon=browser]\nnode gateway: API Gateway [icon=server]\nnode issue: Issue Service [icon=compute]\nnode project: Project Service [icon=compute]\nnode workflow: Workflow Engine [icon=compute]\nnode sprint: Sprint Service [icon=compute]\nnode search: Search Index [icon=search]\nnode realtime: Realtime Updates [icon=sync]\nnode notify: Notifications [icon=message]\nnode db: Issues DB [cylinder, icon=database]\n\nedge dev -> app: create issue\nedge app -> gateway: submit\nedge gateway -> issue: validate\nedge issue -> project: link\nedge issue -> workflow: transition\nedge workflow -> sprint: assign\nedge issue -> search: index\nedge realtime -> app: push updates\nedge search -> app: results\nedge issue -> notify: mentions\nedge issue -> db: store"}]]}]
a:["$","div",null,{"className":"mt-8","children":["$","$L1f",null,{"text":"title: Issue Tracker\nnode dev: Developer [round, icon=browser]\nnode app: Tracker App [icon=browser]\nnode gateway: API Gateway [icon=server]\nnode issue: Issue Service [icon=compute]\nnode project: Project Service [icon=compute]\nnode workflow: Workflow Engine [icon=compute]\nnode sprint: Sprint Service [icon=compute]\nnode search: Search Index [icon=search]\nnode realtime: Realtime Updates [icon=sync]\nnode notify: Notifications [icon=message]\nnode db: Issues DB [cylinder, icon=database]\n\nedge dev -> app: create issue\nedge app -> gateway: submit\nedge gateway -> issue: validate\nedge issue -> project: link\nedge issue -> workflow: transition\nedge workflow -> sprint: assign\nedge issue -> search: index\nedge realtime -> app: push updates\nedge search -> app: results\nedge issue -> notify: mentions\nedge issue -> db: store","name":"Jira — Issue Tracker"}]}]
b:["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","async":true}]
c:["$","script","script-1",{"src":"/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js","async":true}]
d:["$","$L20",null,{"children":["$","$21",null,{"name":"Next.MetadataOutlet","children":"$@22"}]}]
10:["$","$1","h",{"children":[null,["$","$L23",null,{"children":[["$","meta","0",{"charSet":"utf-8"}],["$","meta","1",{"name":"viewport","content":"width=device-width, initial-scale=1"}]]}],["$","div",null,{"hidden":true,"children":["$","$L24",null,{"children":["$","$21",null,{"name":"Next.Metadata","children":[["$","title","0",{"children":"Jira — Issue Tracker — Diagram Examples"}],["$","meta","1",{"name":"description","content":"Issues, sprints, boards, workflows, search, notifications."}],["$","link","2",{"rel":"manifest","href":"/manifest.json"}],["$","meta","3",{"name":"mobile-web-app-capable","content":"yes"}],["$","meta","4",{"name":"apple-mobile-web-app-title","content":"Diagram"}],["$","meta","5",{"name":"apple-mobile-web-app-status-bar-style","content":"black-translucent"}]]}]}]}],["$","meta",null,{"name":"next-size-adjust","content":""}]]}]
12:["$","$1","c",{"children":[null,["$","$L25",null,{"parallelRouterKey":"children","template":["$","$L26",null,{}]}]]}]
15:["$","$1","c",{"children":[null,["$","$L25",null,{"parallelRouterKey":"children","template":["$","$L26",null,{}]}]]}]
17:["$","$L27",null,{"loading":[["$","div","l",{"className":"flex h-screen items-center justify-center","children":["$","span",null,{"className":"loading loading-spinner loading-lg text-primary"}]}],[],null],"children":["$","$1","c",{"children":[[["$","link","0",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","precedence":"next"}],["$","link","1",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","precedence":"next"}],["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","async":true}]],["$","html",null,{"lang":"en","data-theme":"diagram","className":"inter_537b2856-module__DOofUa__variable jetbrains_mono_da46d059-module__t9WmUG__variable","children":[["$","head",null,{"children":["$","link",null,{"rel":"apple-touch-icon","href":"/icons/icon-192.png"}]}],["$","body",null,{"className":"h-screen overflow-hidden","children":["$","$L25",null,{"parallelRouterKey":"children","error":"$28","errorStyles":[],"errorScripts":[["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/2td1d3x1ljv-f.js","async":true}]],"template":["$","$L26",null,{}],"notFound":[["$","div",null,{"className":"flex h-screen flex-col items-center justify-center gap-4","children":[["$","h1",null,{"className":"text-2xl","children":"404"}],["$","p",null,{"className":"text-sm","children":"This diagram does not exist."}],["$","a",null,{"href":"/","className":"btn btn-primary btn-sm","children":"Back to editor"}]]}],[]]}]}]]}]]}]}]
1b:["$","$1","0",{"children":"The workflow is versioned. When a team edits a workflow, the engine must keep old transitions valid for issues already in flight, so each transition record points to the workflow version that defined it. Approval gates and parallel statuses complicate the state machine further; some teams run multiple concurrent states, such as a code-review state and a QA state at once. The pragmatic answer is to model the workflow as a directed graph with explicit nodes and edges in a table rather than hard-coded statuses, so arbitrary graphs can be loaded and validated at runtime without deploying new code."}]
1c:["$","$1","0",{"children":"Caching sits in front of both engines. Board definitions and rendered column layouts are cached by project, and frequently opened backlogs are cached with a short TTL, invalidated on the next change event for that project. Because a single issue change can affect many boards, invalidation is coarse: the event carries the project and sprint keys, and the cache drops every affected entry rather than trying to diff views precisely. This over-invalidation is cheap compared with the cost of serving a stale board, and it keeps the projection builder idempotent and simple to reason about."}]
1d:["$","$1","0",{"children":"Ordering and conflicts are handled at the source of truth, not the push layer. Events carry a monotonically increasing sequence, and clients that reconnect replay missed ranges from the realtime buffer. Drag-and-drop reordering is the classic race: two people dragging different cards at once. The system resolves this with optimistic UI plus server-side serialization, where the drag operation is a transaction that recomputes the order and the losing client receives a corrected snapshot. This keeps the server authoritative while preserving the feel of instant, shared interaction."}]
1e:["$","$1","0",{"children":"Relevance and freshness pull in opposite directions. Bulk reindexing keeps the index coherent after migrations, while continuous incremental indexing keeps it current; the design uses both, with a nightly rebuild for defragmentation and correction. Per-tenant indexes isolate noisy tenants and let the system size shards by data volume rather than by global count. Finally, permission filtering is a major cost: every result must be checked against project and issue-level permissions before display. The index stores the ACL groups each issue is visible to, so permission checks become a set intersection during query rather than a post-query join that would destroy latency."}]
22:null
1a:true
f:300
f:C
19:0
11:"$undefined"
13:"$undefined"
16:"$undefined"
18:"$undefined"
e:"$undefined"
