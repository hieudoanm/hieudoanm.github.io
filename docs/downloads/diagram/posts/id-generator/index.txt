1:I[93226,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"LoadingBoundaryProvider"]
2:"$Sreact.fragment"
3:I[93226,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default"]
4:I[46791,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/2td1d3x1ljv-f.js"],"default"]
5:I[15750,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default"]
7:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"OutletBoundary"]
8:"$Sreact.suspense"
b:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"ViewportBoundary"]
d:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"MetadataBoundary"]
f:I[56193,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default",1]
:HL["/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","style"]
:HL["/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","style"]
:HL["/downloads/diagram/_next/static/media/70bc3e132a0a741e-s.p.3t6q91iet4nsy.woff2","font",{"crossOrigin":"","type":"font/woff2"}]
:HL["/downloads/diagram/_next/static/media/83afe278b6a6bb3c-s.p.2bn3s6zvc0dyp.woff2","font",{"crossOrigin":"","type":"font/woff2"}]
a:X
0:{"P":null,"c":["","posts","id-generator",""],"q":"","i":false,"f":[[["",{"children":["posts",{"children":[["slug","id-generator","d",[]],{"children":["__PAGE__",{},"$undefined","$undefined",4608]},"$undefined","$undefined",4608]},"$undefined","$undefined",4608]},"$undefined","$undefined",4628],[["$","$L1",null,{"loading":[["$","div","l",{"className":"flex h-screen items-center justify-center","children":["$","span",null,{"className":"loading loading-spinner loading-lg text-primary"}]}],[],null],"children":["$","$2","c",{"children":[[["$","link","0",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","precedence":"next","crossOrigin":"$undefined","nonce":"$undefined"}],["$","link","1",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","precedence":"next","crossOrigin":"$undefined","nonce":"$undefined"}],["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","async":true,"nonce":"$undefined"}]],["$","html",null,{"lang":"en","data-theme":"diagram","className":"inter_537b2856-module__DOofUa__variable jetbrains_mono_da46d059-module__t9WmUG__variable","children":[["$","head",null,{"children":["$","link",null,{"rel":"apple-touch-icon","href":"/icons/icon-192.png"}]}],["$","body",null,{"className":"h-screen overflow-hidden","children":["$","$L3",null,{"parallelRouterKey":"children","error":"$4","errorStyles":[],"errorScripts":[["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/2td1d3x1ljv-f.js","async":true}]],"template":["$","$L5",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":[["$","div",null,{"className":"flex h-screen flex-col items-center justify-center gap-4","children":[["$","h1",null,{"className":"text-2xl","children":"404"}],["$","p",null,{"className":"text-sm","children":"This diagram does not exist."}],["$","a",null,{"href":"/","className":"btn btn-primary btn-sm","children":"Back to editor"}]]}],[]],"forbidden":"$undefined","unauthorized":"$undefined"}]}]]}]]}]}],{"children":[["$","$2","c",{"children":[null,["$","$L3",null,{"parallelRouterKey":"children","error":"$undefined","errorStyles":"$undefined","errorScripts":"$undefined","template":["$","$L5",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":"$undefined","forbidden":"$undefined","unauthorized":"$undefined"}]]}],{"children":[["$","$2","c",{"children":[null,["$","$L3",null,{"parallelRouterKey":"children","error":"$undefined","errorStyles":"$undefined","errorScripts":"$undefined","template":["$","$L5",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":"$undefined","forbidden":"$undefined","unauthorized":"$undefined"}]]}],{"children":[["$","$2","c",{"children":["$L6",[["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","async":true,"nonce":"$undefined"}],["$","script","script-1",{"src":"/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js","async":true,"nonce":"$undefined"}]],["$","$L7",null,{"children":["$","$8",null,{"name":"Next.MetadataOutlet","children":"$@9"}]}]]}],{},null,false,null]},null,false,"$a"]},null,false,"$a"]},null,false,null],["$","$2","h",{"children":[null,["$","$Lb",null,{"children":"$Lc"}],["$","div",null,{"hidden":true,"children":["$","$Ld",null,{"children":["$","$8",null,{"name":"Next.Metadata","children":"$Le"}]}]}],["$","meta",null,{"name":"next-size-adjust","content":""}]]}],false]],"m":"$undefined","G":["$f",[["$","link","0",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","precedence":"next","crossOrigin":"$undefined","nonce":"$undefined"}],["$","link","1",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","precedence":"next","crossOrigin":"$undefined","nonce":"$undefined"}]]],"S":true,"h":null,"r":"$undefined","s":"$undefined","a":"$undefined","l":"$undefined","p":"$undefined","d":"$undefined","b":"kwMAhgW1ckibYawstx_6-"}
10:I[83070,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js"],""]
a:C
6:["$","div",null,{"className":"h-screen overflow-y-auto","children":["$","main",null,{"className":"mx-auto max-w-3xl px-4 py-10","children":[["$","p",null,{"className":"mb-6 opacity-60","children":["$","$L10",null,{"className":"link","href":"/posts/","children":"← All posts"}]}],["$","article",null,{"children":[["$","header",null,{"className":"mb-8","children":[["$","h1",null,{"className":"text-3xl font-bold","children":"Distributed ID Generator"}],["$","p",null,{"className":"text-base-content/70 mt-2","children":"Snowflake IDs, uniqueness at scale, ordering, allocation."}],["$","p",null,{"className":"text-base-content/50 mt-3 text-sm","children":["Hieu Doan"," · ",["$","span",null,{"className":"capitalize","children":"medium"}]," ·"," ",["$","span",null,{"className":"capitalize","children":"infrastructure"}]]}],["$","div",null,{"className":"mt-3 flex flex-wrap gap-1.5","children":[["$","span","distributed",{"className":"badge badge-outline badge-sm","children":"distributed"}]]}]]}],["$","section",null,{"children":[["$","h2",null,{"className":"mb-2 text-xl font-semibold","children":"Interview Questions"}],["$","ul",null,{"className":"list-disc space-y-1 pl-6","children":[["$","li","Design a distributed unique ID generator",{"children":"Design a distributed unique ID generator"}],["$","li","Why are database auto-increment IDs not enough?",{"children":"Why are database auto-increment IDs not enough?"}],["$","li","How does a Snowflake-style ID work?",{"children":"How does a Snowflake-style ID work?"}],["$","li","How do you allocate ID ranges without a central bottleneck?",{"children":"How do you allocate ID ranges without a central bottleneck?"}],["$","li","How do you ensure IDs are k-ordered by time?",{"children":"How do you ensure IDs are k-ordered by time?"}]]}]]}],["$","section",null,{"className":"mt-8","children":[["$","h2",null,{"className":"mb-4 text-xl font-semibold","children":"Answers"}],["$","div",null,{"className":"space-y-6","children":[["$","div","Design a distributed unique ID generator",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"Design a distributed unique ID generator"}],[["$","p","An ID generator hands out globally unique identifiers to many clients at high throughput. The main approaches are Snowflake-style (timestamp + worker + sequence packed into a 64-bit integer), database sequences with batch allocation, and UUIDs. For 64-bit, roughly time-ordered IDs, the design is a fleet of allocator/Snowflake workers behind an API gateway: a client calls `next id`, a worker composes its timestamp, worker ID, and per-millisecond sequence, and returns the value — the hot path is pure local arithmetic, no coordination.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"An ID generator hands out globally unique identifiers to many clients at high throughput. The main approaches are Snowflake-style (timestamp + worker + sequence packed into a 64-bit integer), database sequences with batch allocation, and UUIDs. For 64-bit, roughly time-ordered IDs, the design is a fleet of allocator/Snowflake workers behind an API gateway: a client calls "}],["$","code","1",{"className":"bg-base-300 rounded px-1 font-mono text-sm","children":"next id"}],["$","$2","2",{"children":", a worker composes its timestamp, worker ID, and per-millisecond sequence, and returns the value — the hot path is pure local arithmetic, no coordination."}]]}],["$","p","Data model: the ID is the only output; internally the system keeps worker identity (registered in the ID DB or a config store), per-worker sequence state, and metadata rows for allocated ranges. A monitor collects per-worker stats — generation rate, sequence overflow, clock drift — and alerts on anomalies. Failure handling: a worker crash loses at most an in-memory sequence value, and because worker IDs are disjoint, uniqueness holds across restarts and across the fleet; a worker that comes back with the same ID resumes safely as long as clocks are correct. Scaling is horizontal and linear: add workers, each with its own ID space, no shared counter to contend for.",{"className":"mb-2 leading-relaxed","children":["$L11"]}],"$L12"]]}],"$L13","$L14","$L15","$L16"]}]]}],"$L17"]}],"$L18"]}]}]
1f:I[94844,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js"],"default"]
11:["$","$2","0",{"children":"Data model: the ID is the only output; internally the system keeps worker identity (registered in the ID DB or a config store), per-worker sequence state, and metadata rows for allocated ranges. A monitor collects per-worker stats — generation rate, sequence overflow, clock drift — and alerts on anomalies. Failure handling: a worker crash loses at most an in-memory sequence value, and because worker IDs are disjoint, uniqueness holds across restarts and across the fleet; a worker that comes back with the same ID resumes safely as long as clocks are correct. Scaling is horizontal and linear: add workers, each with its own ID space, no shared counter to contend for."}]
12:["$","p","The core trade-off is coordination versus guarantees. Coordination-free Snowflake is fast (millions of IDs/sec) but assumes clock sanity and unique worker IDs; duplicate risk appears only when those invariants break. Batch allocation against a database is simpler and safer but pays a DB round trip per range, and a crash skips leftover IDs (gaps are fine). UUIDs (v4) need no coordination at all but are 128 bits, unorderable, and index-hostile, which matters for clustered indexes. The choice depends on whether you need sortability, compactness, or simplicity — most systems end up with a Snowflake variant plus a batch-allocated fallback for correctness-critical ranges.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"The core trade-off is coordination versus guarantees. Coordination-free Snowflake is fast (millions of IDs/sec) but assumes clock sanity and unique worker IDs; duplicate risk appears only when those invariants break. Batch allocation against a database is simpler and safer but pays a DB round trip per range, and a crash skips leftover IDs (gaps are fine). UUIDs (v4) need no coordination at all but are 128 bits, unorderable, and index-hostile, which matters for clustered indexes. The choice depends on whether you need sortability, compactness, or simplicity — most systems end up with a Snowflake variant plus a batch-allocated fallback for correctness-critical ranges."}]]}]
13:["$","div","Why are database auto-increment IDs not enough?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"Why are database auto-increment IDs not enough?"}],[["$","p","A single auto-increment column is a single writer: every insert serializes on the sequence, taking a lock and updating the counter, so throughput is capped at what one database node can sustain — and it's the same counter for every table and every caller, so it becomes a hard bottleneck during spikes. Worse, it's a hot spot you cannot shard: split your write load across databases and either they share one sequence (defeating the point) or they each get their own, which collides.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"A single auto-increment column is a single writer: every insert serializes on the sequence, taking a lock and updating the counter, so throughput is capped at what one database node can sustain — and it's the same counter for every table and every caller, so it becomes a hard bottleneck during spikes. Worse, it's a hot spot you cannot shard: split your write load across databases and either they share one sequence (defeating the point) or they each get their own, which collides."}]]}],["$","p","Second, auto-increment carries no temporal information. An ID like `1001423` tells you almost nothing about when the row was created, which you need for time-ordered display, partitioning by creation time, data retention, and debugging. Third, it leaks information: the rate of change of the counter reveals write volume, a privacy and security concern. Fourth, sharding breaks auto-increment outright — two shards both numbering rows `1..N` collide, forcing awkward composite keys or cross-shard coordination. And the sequential guarantee is weaker than it looks: rollbacks, failed inserts, and deletes leave gaps anyway, so the ID sequence is already full of holes.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"Second, auto-increment carries no temporal information. An ID like "}],["$","code","1",{"className":"bg-base-300 rounded px-1 font-mono text-sm","children":"1001423"}],["$","$2","2",{"children":" tells you almost nothing about when the row was created, which you need for time-ordered display, partitioning by creation time, data retention, and debugging. Third, it leaks information: the rate of change of the counter reveals write volume, a privacy and security concern. Fourth, sharding breaks auto-increment outright — two shards both numbering rows "}],["$","code","3",{"className":"bg-base-300 rounded px-1 font-mono text-sm","children":"1..N"}],["$","$2","4",{"children":" collide, forcing awkward composite keys or cross-shard coordination. And the sequential guarantee is weaker than it looks: rollbacks, failed inserts, and deletes leave gaps anyway, so the ID sequence is already full of holes."}]]}],["$","p","Distributed generators replace the single sequence. A Snowflake-style worker computes IDs locally from timestamp + worker ID + sequence: no shared state, so throughput scales with worker count, and IDs are sortable by time. Batch allocation keeps the database but inverts the cost model: one `UPDATE ... WHERE last_id = X` reserves a range of, say, 1000 IDs, and the allocator serves them locally — one DB call per thousand IDs instead of one per ID. Both approaches preserve uniqueness across many writers, which is the property single-node auto-increment fundamentally cannot give you.",{"className":"mb-2 leading-relaxed","children":["$L19","$L1a","$L1b"]}]]]}]
14:["$","div","How does a Snowflake-style ID work?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How does a Snowflake-style ID work?"}],[["$","p","Twitter's Snowflake packs 64 bits: 1 unused sign bit, 41 bits of millisecond timestamp (covers ~69 years from a chosen epoch, e.g. 2010), 10 bits of worker/machine ID (1024 distinct workers), and 12 bits of a per-millisecond sequence (4096 IDs per millisecond per worker). To generate an ID, a worker reads the current millisecond, increments its sequence if the millisecond matches the last one, or resets the sequence and records the new timestamp; when the sequence saturates, it waits for the next millisecond rather than overflowing.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"Twitter's Snowflake packs 64 bits: 1 unused sign bit, 41 bits of millisecond timestamp (covers ~69 years from a chosen epoch, e.g. 2010), 10 bits of worker/machine ID (1024 distinct workers), and 12 bits of a per-millisecond sequence (4096 IDs per millisecond per worker). To generate an ID, a worker reads the current millisecond, increments its sequence if the millisecond matches the last one, or resets the sequence and records the new timestamp; when the sequence saturates, it waits for the next millisecond rather than overflowing."}]]}],["$","p","Uniqueness rests on three disjointness invariants: a worker never reuses an ID within the same epoch (its sequence strictly increases), workers have unique IDs (no two workers share a timestamp + worker field), and the timestamp only moves forward. The weak point is the clock: if a worker's wall clock jumps backward, it can reissue a sequence it already handed out. Mitigations: remember the last timestamp and wait until real time catches up; if drift is severe, fail fast or take a fresh worker ID; and refuse to start a worker whose clock is behind the cluster's metadata.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"Uniqueness rests on three disjointness invariants: a worker never reuses an ID within the same epoch (its sequence strictly increases), workers have unique IDs (no two workers share a timestamp + worker field), and the timestamp only moves forward. The weak point is the clock: if a worker's wall clock jumps backward, it can reissue a sequence it already handed out. Mitigations: remember the last timestamp and wait until real time catches up; if drift is severe, fail fast or take a fresh worker ID; and refuse to start a worker whose clock is behind the cluster's metadata."}]]}],["$","p","Capacity math: 1024 workers × 4096 IDs/ms = ~4.2M IDs/sec sustained, which is why Snowflake variants are the default for high-throughput systems. Variants rebalance the fields — e.g., ShardingSphere's snowflake, Baidu UID-Generator, or adding a discriminator/business-type field to the worker bits — trading worker count or sequence width for more throughput or embeddable metadata. The trade-off: the ID is compact, sortable, and coordination-free, but it is not strictly monotonic across workers (IDs from the same millisecond interleave), and its order value is only as good as the clock. For strictly ordered IDs you need coordination; for most systems, k-ordering is enough.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"Capacity math: 1024 workers × 4096 IDs/ms = ~4.2M IDs/sec sustained, which is why Snowflake variants are the default for high-throughput systems. Variants rebalance the fields — e.g., ShardingSphere's snowflake, Baidu UID-Generator, or adding a discriminator/business-type field to the worker bits — trading worker count or sequence width for more throughput or embeddable metadata. The trade-off: the ID is compact, sortable, and coordination-free, but it is not strictly monotonic across workers (IDs from the same millisecond interleave), and its order value is only as good as the clock. For strictly ordered IDs you need coordination; for most systems, k-ordering is enough."}]]}]]]}]
15:["$","div","How do you allocate ID ranges without a central bottleneck?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you allocate ID ranges without a central bottleneck?"}],[["$","p","Batch allocation removes the database from the hot path. Instead of asking for one ID at a time, each allocator server asks the ID DB once for a range — an atomic `UPDATE ... WHERE last_id = X` reserves, say, `[1000000, 1000500)`, and the server then serves IDs from that range locally, from memory, until exhausted. Because the reservation is atomic and each range is unique, disjoint ranges guarantee uniqueness across the fleet with zero per-ID coordination. The math: one DB round trip per 500 IDs instead of per ID is roughly a 500x reduction in sequence contention, which is how a modest database supports millions of IDs per second.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"Batch allocation removes the database from the hot path. Instead of asking for one ID at a time, each allocator server asks the ID DB once for a range — an atomic "}],["$","code","1",{"className":"bg-base-300 rounded px-1 font-mono text-sm","children":"UPDATE ... WHERE last_id = X"}],["$","$2","2",{"children":" reserves, say, "}],["$","code","3",{"className":"bg-base-300 rounded px-1 font-mono text-sm","children":"[1000000, 1000500)"}],["$","$2","4",{"children":", and the server then serves IDs from that range locally, from memory, until exhausted. Because the reservation is atomic and each range is unique, disjoint ranges guarantee uniqueness across the fleet with zero per-ID coordination. The math: one DB round trip per 500 IDs instead of per ID is roughly a 500x reduction in sequence contention, which is how a modest database supports millions of IDs per second."}]]}],["$","p","Each server holds its current position in a range cache. When it drains a range, it fetches the next; the fetch is retried with backoff if the DB is down, but in the meantime the server keeps serving from its cached range — so a DB outage degrades, not stops, ID issuance. If a server crashes mid-range, the leftover IDs are skipped and never reused; that's acceptable, since ID gaps don't affect correctness (IDs just need uniqueness, not density). A monitor tracks range drain rates and cache headroom so servers prefetch before exhausting.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"Each server holds its current position in a range cache. When it drains a range, it fetches the next; the fetch is retried with backoff if the DB is down, but in the meantime the server keeps serving from its cached range — so a DB outage degrades, not stops, ID issuance. If a server crashes mid-range, the leftover IDs are skipped and never reused; that's acceptable, since ID gaps don't affect correctness (IDs just need uniqueness, not density). A monitor tracks range drain rates and cache headroom so servers prefetch before exhausting."}]]}],["$","p","Range size is the tuning knob. Large ranges (e.g., 100k) minimize DB calls and maximize resilience, at the cost of wider gaps and more wasted IDs when servers churn; small ranges give tight, dense IDs but more DB round trips. A common refinement is segment/step allocation: multiple servers take non-adjacent ranges (`+step`) so any server can serve a fresh range without waiting for a neighbor, at the cost of interleaved (non-contiguous) ID order. Ordering note: within one allocator, ranges are issued in increasing order, so IDs are k-ordered per allocator; across allocators they interleave, which is fine for most consumers and can be fixed at read time.",{"className":"mb-2 leading-relaxed","children":["$L1c","$L1d","$L1e"]}]]]}]
16:["$","div","How do you ensure IDs are k-ordered by time?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you ensure IDs are k-ordered by time?"}],[["$","p","k-ordered means IDs created around the same time are numerically close — not strictly monotonic, but approximately time-ordered. Snowflake achieves this because the high-order bits are a millisecond timestamp: IDs from different workers in the same millisecond can interleave (worker A's `...0005` versus worker B's `...1005` at the same instant), but any ID is within k of IDs generated in the same millisecond, where k is bounded by the worker and sequence fields. Within a single worker, order is exact; globally it is k-ordered.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"k-ordered means IDs created around the same time are numerically close — not strictly monotonic, but approximately time-ordered. Snowflake achieves this because the high-order bits are a millisecond timestamp: IDs from different workers in the same millisecond can interleave (worker A's "}],["$","code","1",{"className":"bg-base-300 rounded px-1 font-mono text-sm","children":"...0005"}],["$","$2","2",{"children":" versus worker B's "}],["$","code","3",{"className":"bg-base-300 rounded px-1 font-mono text-sm","children":"...1005"}],["$","$2","4",{"children":" at the same instant), but any ID is within k of IDs generated in the same millisecond, where k is bounded by the worker and sequence fields. Within a single worker, order is exact; globally it is k-ordered."}]]}],["$","p","Preserve the property by never reusing worker IDs within the same epoch, keeping the worker ID in the middle bits so the timestamp dominates the numeric value, and detecting clock jumps so a worker cannot backdate into territory another worker already issued. Batch allocation is k-ordered per allocator: ranges are handed out in increasing order, so IDs from a newer range are numerically larger than an older one, but two allocators draining overlapping time windows can interleave. If you need strict global ordering you must reintroduce a coordination point (and its bottleneck); otherwise accept k-ordering and sort at the consumer.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"Preserve the property by never reusing worker IDs within the same epoch, keeping the worker ID in the middle bits so the timestamp dominates the numeric value, and detecting clock jumps so a worker cannot backdate into territory another worker already issued. Batch allocation is k-ordered per allocator: ranges are handed out in increasing order, so IDs from a newer range are numerically larger than an older one, but two allocators draining overlapping time windows can interleave. If you need strict global ordering you must reintroduce a coordination point (and its bottleneck); otherwise accept k-ordering and sort at the consumer."}]]}]]]}]
17:["$","details",null,{"className":"mt-8","children":[["$","summary",null,{"className":"cursor-pointer font-medium","children":"Diagram source"}],["$","pre",null,{"className":"bg-base-200 mt-2 overflow-x-auto rounded-lg p-4 font-mono text-xs","children":"title: ID Generator\nnode client: Client [round, icon=browser]\nnode api: API Gateway [icon=server]\nnode allocator: Allocator Service [icon=server]\nnode snowflake: Snowflake Worker [icon=compute]\nnode segment: Segment Queue [icon=queue]\nnode db: ID DB [cylinder, icon=database]\nnode cache: Range Cache [cylinder, icon=cache]\nnode monitor: Monitor [icon=worker]\n\nedge client -> api: next id\nedge api -> allocator: request\nedge allocator -> snowflake: generate\nedge allocator -> segment: batch\nedge allocator -> db: reserve range\nedge allocator -> cache: cached range\nedge api -> client: id\nedge allocator -> monitor: stats"}]]}]
18:["$","div",null,{"className":"mt-8","children":["$","$L1f",null,{"text":"title: ID Generator\nnode client: Client [round, icon=browser]\nnode api: API Gateway [icon=server]\nnode allocator: Allocator Service [icon=server]\nnode snowflake: Snowflake Worker [icon=compute]\nnode segment: Segment Queue [icon=queue]\nnode db: ID DB [cylinder, icon=database]\nnode cache: Range Cache [cylinder, icon=cache]\nnode monitor: Monitor [icon=worker]\n\nedge client -> api: next id\nedge api -> allocator: request\nedge allocator -> snowflake: generate\nedge allocator -> segment: batch\nedge allocator -> db: reserve range\nedge allocator -> cache: cached range\nedge api -> client: id\nedge allocator -> monitor: stats","name":"Distributed ID Generator"}]}]
19:["$","$2","0",{"children":"Distributed generators replace the single sequence. A Snowflake-style worker computes IDs locally from timestamp + worker ID + sequence: no shared state, so throughput scales with worker count, and IDs are sortable by time. Batch allocation keeps the database but inverts the cost model: one "}]
1a:["$","code","1",{"className":"bg-base-300 rounded px-1 font-mono text-sm","children":"UPDATE ... WHERE last_id = X"}]
1b:["$","$2","2",{"children":" reserves a range of, say, 1000 IDs, and the allocator serves them locally — one DB call per thousand IDs instead of one per ID. Both approaches preserve uniqueness across many writers, which is the property single-node auto-increment fundamentally cannot give you."}]
1c:["$","$2","0",{"children":"Range size is the tuning knob. Large ranges (e.g., 100k) minimize DB calls and maximize resilience, at the cost of wider gaps and more wasted IDs when servers churn; small ranges give tight, dense IDs but more DB round trips. A common refinement is segment/step allocation: multiple servers take non-adjacent ranges ("}]
1d:["$","code","1",{"className":"bg-base-300 rounded px-1 font-mono text-sm","children":"+step"}]
1e:["$","$2","2",{"children":") so any server can serve a fresh range without waiting for a neighbor, at the cost of interleaved (non-contiguous) ID order. Ordering note: within one allocator, ranges are issued in increasing order, so IDs are k-ordered per allocator; across allocators they interleave, which is fine for most consumers and can be fixed at read time."}]
c:[["$","meta","0",{"charSet":"utf-8"}],["$","meta","1",{"name":"viewport","content":"width=device-width, initial-scale=1"}]]
9:null
e:[["$","title","0",{"children":"Distributed ID Generator — Diagram Examples"}],["$","meta","1",{"name":"description","content":"Snowflake IDs, uniqueness at scale, ordering, allocation."}],["$","link","2",{"rel":"manifest","href":"/manifest.json","crossOrigin":"$undefined"}],["$","meta","3",{"name":"mobile-web-app-capable","content":"yes"}],["$","meta","4",{"name":"apple-mobile-web-app-title","content":"Diagram"}],["$","meta","5",{"name":"apple-mobile-web-app-status-bar-style","content":"black-translucent"}]]
