1:"$Sreact.fragment"
2:I[83070,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js"],""]
e:X
13:X
13:C
0:{"buildId":"kwMAhgW1ckibYawstx_6-","data":[{"rsc":["$","$1","c",{"children":[["$","div",null,{"className":"h-screen overflow-y-auto","children":["$","main",null,{"className":"mx-auto max-w-3xl px-4 py-10","children":[["$","p",null,{"className":"mb-6 opacity-60","children":["$","$L2",null,{"className":"link","href":"/posts/","children":"← All posts"}]}],["$","article",null,{"children":[["$","header",null,{"className":"mb-8","children":[["$","h1",null,{"className":"text-3xl font-bold","children":"Distributed File System"}],["$","p",null,{"className":"text-base-content/70 mt-2","children":"Namespaces, chunk storage, replication, recovery."}],["$","p",null,{"className":"text-base-content/50 mt-3 text-sm","children":["Hieu Doan"," · ",["$","span",null,{"className":"capitalize","children":"medium"}]," ·"," ",["$","span",null,{"className":"capitalize","children":"infrastructure"}]]}],["$","div",null,{"className":"mt-3 flex flex-wrap gap-1.5","children":[["$","span","database",{"className":"badge badge-outline badge-sm","children":"database"}],["$","span","distributed",{"className":"badge badge-outline badge-sm","children":"distributed"}],["$","span","storage",{"className":"badge badge-outline badge-sm","children":"storage"}]]}]]}],["$","section",null,{"children":[["$","h2",null,{"className":"mb-2 text-xl font-semibold","children":"Interview Questions"}],["$","ul",null,{"className":"list-disc space-y-1 pl-6","children":[["$","li","Design a distributed file system",{"children":"Design a distributed file system"}],["$","li","How do you split files into chunks?",{"children":"How do you split files into chunks?"}],["$","li","How do you replicate chunks for durability?",{"children":"How do you replicate chunks for durability?"}],["$","li","How do you locate chunks quickly?",{"children":"How do you locate chunks quickly?"}],["$","li","How do you recover from node failures?",{"children":"How do you recover from node failures?"}]]}]]}],["$","section",null,{"className":"mt-8","children":[["$","h2",null,{"className":"mb-4 text-xl font-semibold","children":"Answers"}],["$","div",null,{"className":"space-y-6","children":[["$","div","Design a distributed file system",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"Design a distributed file system"}],[["$","p","A distributed file system stores files across many commodity machines while presenting a single namespace. The architecture splits metadata from data: a NameNode manages the namespace — the directory tree, file names, permissions, and for each file the map from logical ranges to physical chunks — while a fleet of chunk servers holds the actual data. A file is broken into fixed-size chunks, each chunk stored on several chunk servers for redundancy, and each chunk server reports its health and inventory to the NameNode. The gateway handles uploads and reads: the chunk splitter divides incoming files, and the location service translates a read request into the list of chunk servers that hold the needed ranges.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"A distributed file system stores files across many commodity machines while presenting a single namespace. The architecture splits metadata from data: a NameNode manages the namespace — the directory tree, file names, permissions, and for each file the map from logical ranges to physical chunks — while a fleet of chunk servers holds the actual data. A file is broken into fixed-size chunks, each chunk stored on several chunk servers for redundancy, and each chunk server reports its health and inventory to the NameNode. The gateway handles uploads and reads: the chunk splitter divides incoming files, and the location service translates a read request into the list of chunk servers that hold the needed ranges."}]]}],["$","p","The two planes have completely different scale profiles. Metadata is small — bytes per file — so the NameNode can hold the whole namespace in memory and answer every lookup in microseconds; data is huge, so chunk servers are sized by storage and bandwidth and are expected to fail constantly. This asymmetry is the point of the design: the metadata plane stays fast and consistent while the data plane scales out and self-heals. The file system also needs durable guarantees around atomic renames, open semantics, and snapshot or append operations, and it must tolerate concurrent readers and writers. The hard problems are chunk location, replication, and recovery — the NameNode must always know where every chunk lives and must always be able to rebuild a chunk that was lost. Availability and durability guarantees are published per file class.",{"className":"mb-2 leading-relaxed","children":["$L3"]}]]]}],"$L4","$L5","$L6","$L7"]}]]}],"$L8"]}],"$L9"]}]}],["$La","$Lb"],"$Lc"]}],"isPartial":"$@d","staleTime":"$e","varyParams":null},{"rsc":"$Lf","isPartial":"$@10","staleTime":"$e","varyParams":null},{"rsc":"$L11","isPartial":"$@12","staleTime":"$e","varyParams":"$13"},{"rsc":"$L14","isPartial":"$@15","staleTime":"$e","varyParams":"$13"},{"rsc":"$L16","isPartial":"$@17","staleTime":"$e","varyParams":null}],"isUpgradeableISRFallback":false,"a":"$@18","rootVaryParams":null,"needsRuntimeRequest":"$@19"}
1a:I[94844,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js"],"default"]
1b:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"OutletBoundary"]
1c:"$Sreact.suspense"
1e:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"ViewportBoundary"]
1f:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"MetadataBoundary"]
20:I[93226,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default"]
21:I[15750,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default"]
22:I[93226,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"LoadingBoundaryProvider"]
23:I[46791,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/2td1d3x1ljv-f.js"],"default"]
:HL["/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","style"]
:HL["/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","style"]
3:["$","$1","0",{"children":"The two planes have completely different scale profiles. Metadata is small — bytes per file — so the NameNode can hold the whole namespace in memory and answer every lookup in microseconds; data is huge, so chunk servers are sized by storage and bandwidth and are expected to fail constantly. This asymmetry is the point of the design: the metadata plane stays fast and consistent while the data plane scales out and self-heals. The file system also needs durable guarantees around atomic renames, open semantics, and snapshot or append operations, and it must tolerate concurrent readers and writers. The hard problems are chunk location, replication, and recovery — the NameNode must always know where every chunk lives and must always be able to rebuild a chunk that was lost. Availability and durability guarantees are published per file class."}]
4:["$","div","How do you split files into chunks?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you split files into chunks?"}],[["$","p","Files are split into fixed-size chunks, typically 64 MB or 128 MB, chosen to balance overhead against parallelism. A large chunk means few metadata entries — the NameNode tracks one mapping per chunk rather than per kilobyte — and it amortizes the per-chunk bookkeeping, but it reduces parallelism for small files and inflates the cost of a partial read. The splitter handles files of any size: a small file becomes one chunk, while a multi-terabyte file becomes tens of thousands. Each chunk gets a globally unique ID, and the NameNode records, per file, the ordered list of chunk IDs plus each chunk's version. Because chunks are immutable once written and replaced by new versions on rewrite, the mapping stays simple and the read path never needs locks on data.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Files are split into fixed-size chunks, typically 64 MB or 128 MB, chosen to balance overhead against parallelism. A large chunk means few metadata entries — the NameNode tracks one mapping per chunk rather than per kilobyte — and it amortizes the per-chunk bookkeeping, but it reduces parallelism for small files and inflates the cost of a partial read. The splitter handles files of any size: a small file becomes one chunk, while a multi-terabyte file becomes tens of thousands. Each chunk gets a globally unique ID, and the NameNode records, per file, the ordered list of chunk IDs plus each chunk's version. Because chunks are immutable once written and replaced by new versions on rewrite, the mapping stays simple and the read path never needs locks on data."}]]}],["$","p","Splitting also needs to deal with appends and partial writes. An append is handled as a new chunk or an extended chunk with a version bump, and the NameNode's mapping is updated transactionally so a reader either sees the old length or the new one, never a torn state. The chunk boundary interacts with read efficiency: clients read chunk-aligned ranges so the location service can return exactly the servers for the ranges requested, and the file system supports a sequential read pattern where the client requests the next chunk before finishing the current one, hiding network latency. Chunk size is also a replication and recovery unit — the smaller the chunk, the faster a lost chunk can be re-replicated from its peers, so the choice of size directly bounds the recovery time budget.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Splitting also needs to deal with appends and partial writes. An append is handled as a new chunk or an extended chunk with a version bump, and the NameNode's mapping is updated transactionally so a reader either sees the old length or the new one, never a torn state. The chunk boundary interacts with read efficiency: clients read chunk-aligned ranges so the location service can return exactly the servers for the ranges requested, and the file system supports a sequential read pattern where the client requests the next chunk before finishing the current one, hiding network latency. Chunk size is also a replication and recovery unit — the smaller the chunk, the faster a lost chunk can be re-replicated from its peers, so the choice of size directly bounds the recovery time budget."}]]}]]]}]
5:["$","div","How do you replicate chunks for durability?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you replicate chunks for durability?"}],[["$","p","Every chunk is stored on multiple chunk servers, by default three, so the loss of one or even two servers does not lose data. The replication policy is chosen when the chunk is created: the system picks servers on different racks or failure domains, keeps the load balanced across servers, and prefers to place the replicas near clients that write or read the chunk most. When a client writes a chunk, one server is chosen as the pipeline leader; it receives the data and forwards it to the next replica in a chain, so the bandwidth cost of replicating three copies is roughly one copy, not three, and the client receives a single acknowledgment when all copies are durable.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Every chunk is stored on multiple chunk servers, by default three, so the loss of one or even two servers does not lose data. The replication policy is chosen when the chunk is created: the system picks servers on different racks or failure domains, keeps the load balanced across servers, and prefers to place the replicas near clients that write or read the chunk most. When a client writes a chunk, one server is chosen as the pipeline leader; it receives the data and forwards it to the next replica in a chain, so the bandwidth cost of replicating three copies is roughly one copy, not three, and the client receives a single acknowledgment when all copies are durable."}]]}],["$","p","The replication system is proactive, not reactive. The NameNode continuously reconciles its view of the world: each chunk server reports its chunk inventory on heartbeat, and the NameNode compares it against the desired state. A chunk that is under-replicated — because a server died or a new replica was requested — is added to a replication queue and rebuilt from an existing replica. Re-replication is rate-limited so that recovering from a failed server does not itself overload the cluster, and the system prioritizes chunks that are close to losing their last replica. This makes durability a closed loop: a write is durable when the pipeline finishes, and it stays durable because the NameNode notices any drift from the target replication factor and fixes it continuously.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"The replication system is proactive, not reactive. The NameNode continuously reconciles its view of the world: each chunk server reports its chunk inventory on heartbeat, and the NameNode compares it against the desired state. A chunk that is under-replicated — because a server died or a new replica was requested — is added to a replication queue and rebuilt from an existing replica. Re-replication is rate-limited so that recovering from a failed server does not itself overload the cluster, and the system prioritizes chunks that are close to losing their last replica. This makes durability a closed loop: a write is durable when the pipeline finishes, and it stays durable because the NameNode notices any drift from the target replication factor and fixes it continuously."}]]}]]]}]
6:["$","div","How do you locate chunks quickly?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you locate chunks quickly?"}],[["$","p","Chunk location is a pure metadata problem, and the design keeps it entirely off the data path. The NameNode holds the complete mapping in memory: for each file, the ordered chunk list; for each chunk, its replicas and their locations. A read begins at the gateway, which asks the location service for the chunk list of the requested byte range. The location service consults the NameNode, which answers in microseconds with the ordered list of chunk IDs and, for each chunk, the ranked list of chunk servers holding a replica. Ranking prefers a server in the same rack or zone as the client and one with spare capacity, so the client can fetch the nearest replica with minimal network crossing.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Chunk location is a pure metadata problem, and the design keeps it entirely off the data path. The NameNode holds the complete mapping in memory: for each file, the ordered chunk list; for each chunk, its replicas and their locations. A read begins at the gateway, which asks the location service for the chunk list of the requested byte range. The location service consults the NameNode, which answers in microseconds with the ordered list of chunk IDs and, for each chunk, the ranked list of chunk servers holding a replica. Ranking prefers a server in the same rack or zone as the client and one with spare capacity, so the client can fetch the nearest replica with minimal network crossing."}]]}],["$","p","The trick is that the location answer must be correct even while the system is changing. Chunk servers come and go, and replicas move, so the NameNode is the single source of truth and every mutation — server join, server death, replica creation, chunk deletion — updates the mapping transactionally. Clients cache chunk-to-server mappings briefly and retry through the gateway on staleness, which keeps the hot path fast without making the client part of the consistency machinery. Because the metadata store is small relative to the data, the NameNode can also replicate its state for availability and checkpoint it to a durable metadata database, so a NameNode failure means the mapping is restored quickly and the data plane continues to serve from the chunk servers.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"The trick is that the location answer must be correct even while the system is changing. Chunk servers come and go, and replicas move, so the NameNode is the single source of truth and every mutation — server join, server death, replica creation, chunk deletion — updates the mapping transactionally. Clients cache chunk-to-server mappings briefly and retry through the gateway on staleness, which keeps the hot path fast without making the client part of the consistency machinery. Because the metadata store is small relative to the data, the NameNode can also replicate its state for availability and checkpoint it to a durable metadata database, so a NameNode failure means the mapping is restored quickly and the data plane continues to serve from the chunk servers."}]]}]]]}]
7:["$","div","How do you recover from node failures?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you recover from node failures?"}],[["$","p","Failure is the default state of a distributed file system, so recovery is designed in rather than bolted on. Chunk servers send periodic heartbeats; when the NameNode stops hearing from one, it marks the server lost and schedules its chunks for re-replication. Chunk inventory on the heartbeats also catches silent corruption — each chunk stores a checksum, and reads or background scanners that find a mismatch trigger a rewrite from a healthy replica. The rebalancer and the NameNode prioritize repairs: chunks that are missing one of three replicas are re-replicated first, and the recovery rate is throttled so rebuilding a dead server does not saturate the network or starve live traffic.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Failure is the default state of a distributed file system, so recovery is designed in rather than bolted on. Chunk servers send periodic heartbeats; when the NameNode stops hearing from one, it marks the server lost and schedules its chunks for re-replication. Chunk inventory on the heartbeats also catches silent corruption — each chunk stores a checksum, and reads or background scanners that find a mismatch trigger a rewrite from a healthy replica. The rebalancer and the NameNode prioritize repairs: chunks that are missing one of three replicas are re-replicated first, and the recovery rate is throttled so rebuilding a dead server does not saturate the network or starve live traffic."}]]}],["$","p","The NameNode itself must also survive. Its metadata is checkpointed and journaled, and the system runs a standby NameNode that watches the primary's log, so a primary failure fails over to a standby in seconds rather than requiring a long metadata rebuild. If the metadata store is entirely lost, the system can reconstruct the namespace from the chunk servers' self-reported inventories, with the data plane intact — a last-resort recovery that restores the system to a consistent, if slightly stale, state. Replica placement is what bounds recovery: because chunks were spread across racks and servers, losing one server at a time is survivable, and the system deliberately avoids scheduling multiple replicas of a chunk on one machine or rack. The design goal is a system where a node dies, its data is repaired automatically, and nothing that depends on the file system notices anything beyond a few seconds of degraded parallelism.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"The NameNode itself must also survive. Its metadata is checkpointed and journaled, and the system runs a standby NameNode that watches the primary's log, so a primary failure fails over to a standby in seconds rather than requiring a long metadata rebuild. If the metadata store is entirely lost, the system can reconstruct the namespace from the chunk servers' self-reported inventories, with the data plane intact — a last-resort recovery that restores the system to a consistent, if slightly stale, state. Replica placement is what bounds recovery: because chunks were spread across racks and servers, losing one server at a time is survivable, and the system deliberately avoids scheduling multiple replicas of a chunk on one machine or rack. The design goal is a system where a node dies, its data is repaired automatically, and nothing that depends on the file system notices anything beyond a few seconds of degraded parallelism."}]]}]]]}]
8:["$","details",null,{"className":"mt-8","children":[["$","summary",null,{"className":"cursor-pointer font-medium","children":"Diagram source"}],["$","pre",null,{"className":"bg-base-200 mt-2 overflow-x-auto rounded-lg p-4 font-mono text-xs","children":"title: Distributed File System\nnode client: Client [round, icon=browser]\nnode app: File App [icon=browser]\nnode gateway: API Gateway [icon=server]\nnode name: NameNode [icon=compute]\nnode meta: Metadata Store [cylinder, icon=database]\nnode chunk: Chunk Servers [icon=server]\nnode split: Chunk Splitter [icon=compute]\nnode replicate: Replication [icon=sync]\nnode locate: Location Service [icon=search]\nnode heal: Rebalancer [icon=worker]\nnode db: File Metadata DB [cylinder, icon=database]\n\nedge client -> app: write file\nedge app -> gateway: upload\nedge gateway -> split: chunks\nedge split -> chunk: store\nedge chunk -> replicate: copy\nedge client -> app: read\nedge app -> gateway: request\nedge gateway -> locate: chunk list\nedge locate -> meta: lookup\nedge locate -> chunk: fetch\nedge heal -> chunk: repair"}]]}]
9:["$","div",null,{"className":"mt-8","children":["$","$L1a",null,{"text":"title: Distributed File System\nnode client: Client [round, icon=browser]\nnode app: File App [icon=browser]\nnode gateway: API Gateway [icon=server]\nnode name: NameNode [icon=compute]\nnode meta: Metadata Store [cylinder, icon=database]\nnode chunk: Chunk Servers [icon=server]\nnode split: Chunk Splitter [icon=compute]\nnode replicate: Replication [icon=sync]\nnode locate: Location Service [icon=search]\nnode heal: Rebalancer [icon=worker]\nnode db: File Metadata DB [cylinder, icon=database]\n\nedge client -> app: write file\nedge app -> gateway: upload\nedge gateway -> split: chunks\nedge split -> chunk: store\nedge chunk -> replicate: copy\nedge client -> app: read\nedge app -> gateway: request\nedge gateway -> locate: chunk list\nedge locate -> meta: lookup\nedge locate -> chunk: fetch\nedge heal -> chunk: repair","name":"Distributed File System"}]}]
a:["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","async":true}]
b:["$","script","script-1",{"src":"/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js","async":true}]
c:["$","$L1b",null,{"children":["$","$1c",null,{"name":"Next.MetadataOutlet","children":"$@1d"}]}]
f:["$","$1","h",{"children":[null,["$","$L1e",null,{"children":[["$","meta","0",{"charSet":"utf-8"}],["$","meta","1",{"name":"viewport","content":"width=device-width, initial-scale=1"}]]}],["$","div",null,{"hidden":true,"children":["$","$L1f",null,{"children":["$","$1c",null,{"name":"Next.Metadata","children":[["$","title","0",{"children":"Distributed File System — Diagram Examples"}],["$","meta","1",{"name":"description","content":"Namespaces, chunk storage, replication, recovery."}],["$","link","2",{"rel":"manifest","href":"/manifest.json"}],["$","meta","3",{"name":"mobile-web-app-capable","content":"yes"}],["$","meta","4",{"name":"apple-mobile-web-app-title","content":"Diagram"}],["$","meta","5",{"name":"apple-mobile-web-app-status-bar-style","content":"black-translucent"}]]}]}]}],["$","meta",null,{"name":"next-size-adjust","content":""}]]}]
11:["$","$1","c",{"children":[null,["$","$L20",null,{"parallelRouterKey":"children","template":["$","$L21",null,{}]}]]}]
14:["$","$1","c",{"children":[null,["$","$L20",null,{"parallelRouterKey":"children","template":["$","$L21",null,{}]}]]}]
16:["$","$L22",null,{"loading":[["$","div","l",{"className":"flex h-screen items-center justify-center","children":["$","span",null,{"className":"loading loading-spinner loading-lg text-primary"}]}],[],null],"children":["$","$1","c",{"children":[[["$","link","0",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","precedence":"next"}],["$","link","1",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","precedence":"next"}],["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","async":true}]],["$","html",null,{"lang":"en","data-theme":"diagram","className":"inter_537b2856-module__DOofUa__variable jetbrains_mono_da46d059-module__t9WmUG__variable","children":[["$","head",null,{"children":["$","link",null,{"rel":"apple-touch-icon","href":"/icons/icon-192.png"}]}],["$","body",null,{"className":"h-screen overflow-hidden","children":["$","$L20",null,{"parallelRouterKey":"children","error":"$23","errorStyles":[],"errorScripts":[["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/2td1d3x1ljv-f.js","async":true}]],"template":["$","$L21",null,{}],"notFound":[["$","div",null,{"className":"flex h-screen flex-col items-center justify-center gap-4","children":[["$","h1",null,{"className":"text-2xl","children":"404"}],["$","p",null,{"className":"text-sm","children":"This diagram does not exist."}],["$","a",null,{"href":"/","className":"btn btn-primary btn-sm","children":"Back to editor"}]]}],[]]}]}]]}]]}]}]
1d:null
19:true
e:300
e:C
18:0
10:"$undefined"
12:"$undefined"
15:"$undefined"
17:"$undefined"
d:"$undefined"
