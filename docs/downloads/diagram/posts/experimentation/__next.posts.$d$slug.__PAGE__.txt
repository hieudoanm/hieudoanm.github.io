1:"$Sreact.fragment"
2:I[83070,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js"],""]
3:T418,On the analysis path, an event collector ingests exposure and outcome events, and a metrics pipeline aggregates them into per-variant summaries. The analysis engine runs statistical tests, writes conclusions plus guardrail metrics to a results store, and alerts when an experiment crosses stop or restart thresholds. The separation of serving from analysis is deliberate: a bad experiment can be killed instantly by flipping the flag, while analysis of the collected data continues independently. No shared mutable state couples the paths, so serving latency, analysis latency, and experiment management each scale and fail on their own. The data model anchors both paths: exposures and outcomes are keyed by (experiment_id, variant, user_id, timestamp), and the results store keeps per-variant summaries plus references to the raw events so any published conclusion can be audited. Both paths are horizontally scalable and independently deployable, which keeps the experiment backlog small even as the product team runs dozens of concurrent tests.f:X
14:X
14:C
0:{"buildId":"kwMAhgW1ckibYawstx_6-","data":[{"rsc":["$","$1","c",{"children":[["$","div",null,{"className":"h-screen overflow-y-auto","children":["$","main",null,{"className":"mx-auto max-w-3xl px-4 py-10","children":[["$","p",null,{"className":"mb-6 opacity-60","children":["$","$L2",null,{"className":"link","href":"/posts/","children":"← All posts"}]}],["$","article",null,{"children":[["$","header",null,{"className":"mb-8","children":[["$","h1",null,{"className":"text-3xl font-bold","children":"Experimentation — A/B Testing"}],["$","p",null,{"className":"text-base-content/70 mt-2","children":"Variant assignment, statistical testing, metrics, guardrails."}],["$","p",null,{"className":"text-base-content/50 mt-3 text-sm","children":["Hieu Doan"," · ",["$","span",null,{"className":"capitalize","children":"medium"}]," ·"," ",["$","span",null,{"className":"capitalize","children":"infrastructure"}]]}],["$","div",null,{"className":"mt-3 flex flex-wrap gap-1.5","children":[["$","span","experimentation",{"className":"badge badge-outline badge-sm","children":"experimentation"}],["$","span","time-series",{"className":"badge badge-outline badge-sm","children":"time-series"}]]}]]}],["$","section",null,{"children":[["$","h2",null,{"className":"mb-2 text-xl font-semibold","children":"Interview Questions"}],["$","ul",null,{"className":"list-disc space-y-1 pl-6","children":[["$","li","Design an A/B testing platform",{"children":"Design an A/B testing platform"}],["$","li","How do you assign users to variants consistently?",{"children":"How do you assign users to variants consistently?"}],["$","li","How do you ensure statistical validity across metrics?",{"children":"How do you ensure statistical validity across metrics?"}],["$","li","How do you run experiments at scale without pollution?",{"children":"How do you run experiments at scale without pollution?"}],["$","li","How do you detect and stop bad experiments early?",{"children":"How do you detect and stop bad experiments early?"}]]}]]}],["$","section",null,{"className":"mt-8","children":[["$","h2",null,{"className":"mb-4 text-xl font-semibold","children":"Answers"}],["$","div",null,{"className":"space-y-6","children":[["$","div","Design an A/B testing platform",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"Design an A/B testing platform"}],[["$","p","The platform has two halves that must stay decoupled: a real-time decision path that tells each client which variant to show, and an offline analysis path that proves whether the variant caused an effect. On the decision path, the app SDK calls a flag API, which asks an assignment service to bucket the user into a variant. Assignment is deterministic — the same user always receives the same variant — so results stay stable across sessions and devices. Variant configuration and traffic splits come from an allocation store that the experiment UI persists to, and an event collector records which variant each user saw.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"The platform has two halves that must stay decoupled: a real-time decision path that tells each client which variant to show, and an offline analysis path that proves whether the variant caused an effect. On the decision path, the app SDK calls a flag API, which asks an assignment service to bucket the user into a variant. Assignment is deterministic — the same user always receives the same variant — so results stay stable across sessions and devices. Variant configuration and traffic splits come from an allocation store that the experiment UI persists to, and an event collector records which variant each user saw."}]]}],["$","p","$3",{"className":"mb-2 leading-relaxed","children":["$L4"]}]]]}],"$L5","$L6","$L7","$L8"]}]]}],"$L9"]}],"$La"]}]}],["$Lb","$Lc"],"$Ld"]}],"isPartial":"$@e","staleTime":"$f","varyParams":null},{"rsc":"$L10","isPartial":"$@11","staleTime":"$f","varyParams":null},{"rsc":"$L12","isPartial":"$@13","staleTime":"$f","varyParams":"$14"},{"rsc":"$L15","isPartial":"$@16","staleTime":"$f","varyParams":"$14"},{"rsc":"$L17","isPartial":"$@18","staleTime":"$f","varyParams":null}],"isUpgradeableISRFallback":false,"a":"$@19","rootVaryParams":null,"needsRuntimeRequest":"$@1a"}
1e:I[94844,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js"],"default"]
1f:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"OutletBoundary"]
20:"$Sreact.suspense"
22:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"ViewportBoundary"]
23:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"MetadataBoundary"]
24:I[93226,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default"]
25:I[15750,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default"]
26:I[93226,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"LoadingBoundaryProvider"]
27:I[46791,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/2td1d3x1ljv-f.js"],"default"]
:HL["/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","style"]
:HL["/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","style"]
1b:T418,On the analysis path, an event collector ingests exposure and outcome events, and a metrics pipeline aggregates them into per-variant summaries. The analysis engine runs statistical tests, writes conclusions plus guardrail metrics to a results store, and alerts when an experiment crosses stop or restart thresholds. The separation of serving from analysis is deliberate: a bad experiment can be killed instantly by flipping the flag, while analysis of the collected data continues independently. No shared mutable state couples the paths, so serving latency, analysis latency, and experiment management each scale and fail on their own. The data model anchors both paths: exposures and outcomes are keyed by (experiment_id, variant, user_id, timestamp), and the results store keeps per-variant summaries plus references to the raw events so any published conclusion can be audited. Both paths are horizontally scalable and independently deployable, which keeps the experiment backlog small even as the product team runs dozens of concurrent tests.4:["$","$1","0",{"children":"$1b"}]
5:["$","div","How do you assign users to variants consistently?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you assign users to variants consistently?"}],[["$","p","Assignment is a pure function of a stable key — typically the user ID or a per-experiment salt-mixed identifier — so any server computes the same answer without a round trip or a shared database. The standard scheme hashes the key (e.g., `SHA256(user_id || salt)`), takes a 64-bit slice, and maps that value into a bucket range. Each experiment declares a traffic split, and every variant owns an interval of the hash space; the user's hash lands in exactly one interval, which is their variant. The per-experiment salt prevents the same user from always landing in the same group and makes assignments unpredictable to anyone gaming the system.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Assignment is a pure function of a stable key — typically the user ID or a per-experiment salt-mixed identifier — so any server computes the same answer without a round trip or a shared database. The standard scheme hashes the key (e.g., "}],["$","code","1",{"className":"bg-base-300 rounded px-1 font-mono text-sm","children":"SHA256(user_id || salt)"}],["$","$1","2",{"children":"), takes a 64-bit slice, and maps that value into a bucket range. Each experiment declares a traffic split, and every variant owns an interval of the hash space; the user's hash lands in exactly one interval, which is their variant. The per-experiment salt prevents the same user from always landing in the same group and makes assignments unpredictable to anyone gaming the system."}]]}],["$","p","Persistence is optional but valuable: the allocation store can record the assignment so analysis can join exposures to outcomes even if the user later switches devices. When a split changes mid-experiment, the system must decide whether to re-bucket or to freeze existing users — freezing (reusing the stored assignment) avoids contaminating users who already saw the old variant, which is why most mature platforms freeze on split changes. The SDK caches the result, and the flag API can serve a coarse-grained bucket from a local cache, so assignment costs microseconds. Consistency comes from the deterministic hash function itself, not from locking, which is what lets assignment scale to millions of users per second. One subtlety: the hash function and salt are versioned, so changing them requires a coordinated re-randomization rather than silently reassigning existing users mid-experiment.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Persistence is optional but valuable: the allocation store can record the assignment so analysis can join exposures to outcomes even if the user later switches devices. When a split changes mid-experiment, the system must decide whether to re-bucket or to freeze existing users — freezing (reusing the stored assignment) avoids contaminating users who already saw the old variant, which is why most mature platforms freeze on split changes. The SDK caches the result, and the flag API can serve a coarse-grained bucket from a local cache, so assignment costs microseconds. Consistency comes from the deterministic hash function itself, not from locking, which is what lets assignment scale to millions of users per second. One subtlety: the hash function and salt are versioned, so changing them requires a coordinated re-randomization rather than silently reassigning existing users mid-experiment."}]]}]]]}]
6:["$","div","How do you ensure statistical validity across metrics?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you ensure statistical validity across metrics?"}],[["$","p","Validity starts before data is collected. Every experiment pre-registers a primary metric and a set of guardrail metrics, and the platform computes the sample size needed to detect the minimum meaningful effect (MDE) at the chosen power and significance level. Testing then runs on a fixed horizon, or with sequential testing that bounds error as data streams in — either way the platform prevents the classic mistake of peeking at the p-value repeatedly and declaring victory at the first dip. Because an experiment usually measures many metrics, per-metric adjustments (Bonferroni or a false-discovery-rate control) keep the overall false-positive rate bounded.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Validity starts before data is collected. Every experiment pre-registers a primary metric and a set of guardrail metrics, and the platform computes the sample size needed to detect the minimum meaningful effect (MDE) at the chosen power and significance level. Testing then runs on a fixed horizon, or with sequential testing that bounds error as data streams in — either way the platform prevents the classic mistake of peeking at the p-value repeatedly and declaring victory at the first dip. Because an experiment usually measures many metrics, per-metric adjustments (Bonferroni or a false-discovery-rate control) keep the overall false-positive rate bounded."}]]}],["$","p","The statistics must match the metric's shape. Simple averages use a t-test, ordinal outcomes use Mann–Whitney, and ratio metrics like revenue-per-user need the delta method or bootstrap to build valid confidence intervals. Variance reduction keeps experiments fast and cheap: CUPED regresses the outcome on the pre-experiment value of the same metric, shrinking noise without biasing the estimate, which cuts the required sample size substantially. The engine also checks assignment balance — if bucketing is skewed by geography or device, it can re-run with stratified randomization — and guardrail metrics are analyzed with the same rigor as the primary, so a conversion win is never hiding a latency or revenue regression. The engine also reports confidence intervals, not just p-values, so stakeholders see the magnitude of an effect and its uncertainty, and it flags when the executed design deviates from the pre-registered test, forcing a documented decision rather than silent drift.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"The statistics must match the metric's shape. Simple averages use a t-test, ordinal outcomes use Mann–Whitney, and ratio metrics like revenue-per-user need the delta method or bootstrap to build valid confidence intervals. Variance reduction keeps experiments fast and cheap: CUPED regresses the outcome on the pre-experiment value of the same metric, shrinking noise without biasing the estimate, which cuts the required sample size substantially. The engine also checks assignment balance — if bucketing is skewed by geography or device, it can re-run with stratified randomization — and guardrail metrics are analyzed with the same rigor as the primary, so a conversion win is never hiding a latency or revenue regression. The engine also reports confidence intervals, not just p-values, so stakeholders see the magnitude of an effect and its uncertainty, and it flags when the executed design deviates from the pre-registered test, forcing a documented decision rather than silent drift."}]]}]]]}]
1c:T49b,Some interference cannot be bucketed away. For experiments where one user's variant affects another — marketplaces, social features, network effects — individual randomization is statistically invalid, so the platform supports cluster randomization by geographic or network partition and analyzes with cluster-robust estimators. Novelty and learning effects are handled by ramp schedules: traffic is ramped gradually and a washout period is excluded from analysis so early user behavior does not contaminate the steady-state estimate. Finally, the platform monitors "pollution" operationally — exposures served from stale flag config, variant allocations that do not sum to 100%, and unusual correlation across concurrent experiments all trip alerts before they corrupt a conclusion. Interference checks are also explicit: the platform can cross-tabulate a subset of users across overlapping experiments and compare per-cell effects, quarantining the later experiment when a conflict is confirmed. This combination — orthogonal layers plus explicit conflict resolution — is what lets the platform scale to many concurrent experiments without the results drifting apart.1d:T49b,Some interference cannot be bucketed away. For experiments where one user's variant affects another — marketplaces, social features, network effects — individual randomization is statistically invalid, so the platform supports cluster randomization by geographic or network partition and analyzes with cluster-robust estimators. Novelty and learning effects are handled by ramp schedules: traffic is ramped gradually and a washout period is excluded from analysis so early user behavior does not contaminate the steady-state estimate. Finally, the platform monitors "pollution" operationally — exposures served from stale flag config, variant allocations that do not sum to 100%, and unusual correlation across concurrent experiments all trip alerts before they corrupt a conclusion. Interference checks are also explicit: the platform can cross-tabulate a subset of users across overlapping experiments and compare per-cell effects, quarantining the later experiment when a conflict is confirmed. This combination — orthogonal layers plus explicit conflict resolution — is what lets the platform scale to many concurrent experiments without the results drifting apart.7:["$","div","How do you run experiments at scale without pollution?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you run experiments at scale without pollution?"}],[["$","p","Experiments interfere when the same user is assigned to two experiments that touch the same metric. The dominant fix is multi-layer, orthogonal bucketing: each layer (UI, ranking, pricing) uses its own independent hash function, so a user's assignment in one layer is statistically independent of their assignment in another. Because orthogonal layers use independent salts over the same user base, hundreds of experiments can run concurrently without correlated exposure. On top of that, the platform maintains a metric-exclusivity map that blocks two experiments from modifying the same metric on the same population — a guard against interference that orthogonality alone cannot provide.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Experiments interfere when the same user is assigned to two experiments that touch the same metric. The dominant fix is multi-layer, orthogonal bucketing: each layer (UI, ranking, pricing) uses its own independent hash function, so a user's assignment in one layer is statistically independent of their assignment in another. Because orthogonal layers use independent salts over the same user base, hundreds of experiments can run concurrently without correlated exposure. On top of that, the platform maintains a metric-exclusivity map that blocks two experiments from modifying the same metric on the same population — a guard against interference that orthogonality alone cannot provide."}]]}],["$","p","$1c",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"$1d"}]]}]]]}]
8:["$","div","How do you detect and stop bad experiments early?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you detect and stop bad experiments early?"}],[["$","p","Detection runs on two loops with very different speeds. The operational loop is nearly instant: the platform watches exposure volumes, variant-to-audience leakage, and event-pipeline error rates, and fires alerts when a variant stops being served, starts being served to users it should not reach, or the data pipeline drops events. Because serving is decoupled from analysis, a bad experiment is killable in seconds — the UI flips the allocation to 100% control or removes the flag, and the change propagates through the cached flag store long before any statistical test could react. These infrastructure alerts catch breakage even when the point estimate still looks fine.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"Detection runs on two loops with very different speeds. The operational loop is nearly instant: the platform watches exposure volumes, variant-to-audience leakage, and event-pipeline error rates, and fires alerts when a variant stops being served, starts being served to users it should not reach, or the data pipeline drops events. Because serving is decoupled from analysis, a bad experiment is killable in seconds — the UI flips the allocation to 100% control or removes the flag, and the change propagates through the cached flag store long before any statistical test could react. These infrastructure alerts catch breakage even when the point estimate still looks fine."}]]}],["$","p","The statistical loop is fast but principled: the analysis engine evaluates the pre-registered sequential test after each data batch and stops for either significance or futility. A sequential design keeps both error types bounded, so a large regression in a guardrail metric stops the experiment within days instead of at the pre-computed end. For experiments that are merely unpromising rather than harmful, a futility boundary halts them early and frees traffic for more promising tests. Every stop writes a post-mortem record — exposure counts, observed effects, stop reason — into the results store, so no bad experiment disappears silently and the platform learns a reusable signal about what went wrong. Finally, all detection is log-based and replayable: the same event stream that drives the real-time alerts feeds the post-hoc analysis, so a near-miss that never tripped a threshold can still be diagnosed after the fact.",{"className":"mb-2 leading-relaxed","children":[["$","$1","0",{"children":"The statistical loop is fast but principled: the analysis engine evaluates the pre-registered sequential test after each data batch and stops for either significance or futility. A sequential design keeps both error types bounded, so a large regression in a guardrail metric stops the experiment within days instead of at the pre-computed end. For experiments that are merely unpromising rather than harmful, a futility boundary halts them early and frees traffic for more promising tests. Every stop writes a post-mortem record — exposure counts, observed effects, stop reason — into the results store, so no bad experiment disappears silently and the platform learns a reusable signal about what went wrong. Finally, all detection is log-based and replayable: the same event stream that drives the real-time alerts feeds the post-hoc analysis, so a near-miss that never tripped a threshold can still be diagnosed after the fact."}]]}]]]}]
9:["$","details",null,{"className":"mt-8","children":[["$","summary",null,{"className":"cursor-pointer font-medium","children":"Diagram source"}],["$","pre",null,{"className":"bg-base-200 mt-2 overflow-x-auto rounded-lg p-4 font-mono text-xs","children":"title: Experimentation\nnode app: App SDK [round, icon=browser]\nnode api: Flag API [icon=server]\nnode assign: Assignment Service [icon=compute]\nnode alloc: Allocation Store [cylinder, icon=cache]\nnode event: Event Collector [icon=queue]\nnode metrics: Metrics Pipeline [icon=worker]\nnode analyze: Analysis Engine [icon=search]\nnode results: Results Store [cylinder, icon=database]\nnode ui: Experiment UI [icon=browser]\nnode notify: Alerts [icon=message]\n\nedge app -> api: get variants\nedge api -> assign: bucket\nedge assign -> alloc: lookup\nedge app -> event: log\nedge event -> metrics: aggregate\nedge metrics -> analyze: test\nedge analyze -> results: save\nedge ui -> api: create\nedge api -> alloc: persist\nedge analyze -> notify: alert"}]]}]
a:["$","div",null,{"className":"mt-8","children":["$","$L1e",null,{"text":"title: Experimentation\nnode app: App SDK [round, icon=browser]\nnode api: Flag API [icon=server]\nnode assign: Assignment Service [icon=compute]\nnode alloc: Allocation Store [cylinder, icon=cache]\nnode event: Event Collector [icon=queue]\nnode metrics: Metrics Pipeline [icon=worker]\nnode analyze: Analysis Engine [icon=search]\nnode results: Results Store [cylinder, icon=database]\nnode ui: Experiment UI [icon=browser]\nnode notify: Alerts [icon=message]\n\nedge app -> api: get variants\nedge api -> assign: bucket\nedge assign -> alloc: lookup\nedge app -> event: log\nedge event -> metrics: aggregate\nedge metrics -> analyze: test\nedge analyze -> results: save\nedge ui -> api: create\nedge api -> alloc: persist\nedge analyze -> notify: alert","name":"Experimentation — A/B Testing"}]}]
b:["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","async":true}]
c:["$","script","script-1",{"src":"/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js","async":true}]
d:["$","$L1f",null,{"children":["$","$20",null,{"name":"Next.MetadataOutlet","children":"$@21"}]}]
10:["$","$1","h",{"children":[null,["$","$L22",null,{"children":[["$","meta","0",{"charSet":"utf-8"}],["$","meta","1",{"name":"viewport","content":"width=device-width, initial-scale=1"}]]}],["$","div",null,{"hidden":true,"children":["$","$L23",null,{"children":["$","$20",null,{"name":"Next.Metadata","children":[["$","title","0",{"children":"Experimentation — A/B Testing — Diagram Examples"}],["$","meta","1",{"name":"description","content":"Variant assignment, statistical testing, metrics, guardrails."}],["$","link","2",{"rel":"manifest","href":"/manifest.json"}],["$","meta","3",{"name":"mobile-web-app-capable","content":"yes"}],["$","meta","4",{"name":"apple-mobile-web-app-title","content":"Diagram"}],["$","meta","5",{"name":"apple-mobile-web-app-status-bar-style","content":"black-translucent"}]]}]}]}],["$","meta",null,{"name":"next-size-adjust","content":""}]]}]
12:["$","$1","c",{"children":[null,["$","$L24",null,{"parallelRouterKey":"children","template":["$","$L25",null,{}]}]]}]
15:["$","$1","c",{"children":[null,["$","$L24",null,{"parallelRouterKey":"children","template":["$","$L25",null,{}]}]]}]
17:["$","$L26",null,{"loading":[["$","div","l",{"className":"flex h-screen items-center justify-center","children":["$","span",null,{"className":"loading loading-spinner loading-lg text-primary"}]}],[],null],"children":["$","$1","c",{"children":[[["$","link","0",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","precedence":"next"}],["$","link","1",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","precedence":"next"}],["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","async":true}]],["$","html",null,{"lang":"en","data-theme":"diagram","className":"inter_537b2856-module__DOofUa__variable jetbrains_mono_da46d059-module__t9WmUG__variable","children":[["$","head",null,{"children":["$","link",null,{"rel":"apple-touch-icon","href":"/icons/icon-192.png"}]}],["$","body",null,{"className":"h-screen overflow-hidden","children":["$","$L24",null,{"parallelRouterKey":"children","error":"$27","errorStyles":[],"errorScripts":[["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/2td1d3x1ljv-f.js","async":true}]],"template":["$","$L25",null,{}],"notFound":[["$","div",null,{"className":"flex h-screen flex-col items-center justify-center gap-4","children":[["$","h1",null,{"className":"text-2xl","children":"404"}],["$","p",null,{"className":"text-sm","children":"This diagram does not exist."}],["$","a",null,{"href":"/","className":"btn btn-primary btn-sm","children":"Back to editor"}]]}],[]]}]}]]}]]}]}]
21:null
1a:true
f:300
f:C
19:0
11:"$undefined"
13:"$undefined"
16:"$undefined"
18:"$undefined"
e:"$undefined"
