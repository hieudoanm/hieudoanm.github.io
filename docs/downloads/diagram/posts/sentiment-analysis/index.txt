1:I[93226,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"LoadingBoundaryProvider"]
2:"$Sreact.fragment"
3:I[93226,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default"]
4:I[46791,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/2td1d3x1ljv-f.js"],"default"]
5:I[15750,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default"]
7:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"OutletBoundary"]
8:"$Sreact.suspense"
b:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"ViewportBoundary"]
d:I[17982,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"MetadataBoundary"]
f:I[56193,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js"],"default",1]
:HL["/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","style"]
:HL["/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","style"]
:HL["/downloads/diagram/_next/static/media/70bc3e132a0a741e-s.p.3t6q91iet4nsy.woff2","font",{"crossOrigin":"","type":"font/woff2"}]
:HL["/downloads/diagram/_next/static/media/83afe278b6a6bb3c-s.p.2bn3s6zvc0dyp.woff2","font",{"crossOrigin":"","type":"font/woff2"}]
a:X
0:{"P":null,"c":["","posts","sentiment-analysis",""],"q":"","i":false,"f":[[["",{"children":["posts",{"children":[["slug","sentiment-analysis","d",[]],{"children":["__PAGE__",{},"$undefined","$undefined",4608]},"$undefined","$undefined",4608]},"$undefined","$undefined",4608]},"$undefined","$undefined",4628],[["$","$L1",null,{"loading":[["$","div","l",{"className":"flex h-screen items-center justify-center","children":["$","span",null,{"className":"loading loading-spinner loading-lg text-primary"}]}],[],null],"children":["$","$2","c",{"children":[[["$","link","0",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","precedence":"next","crossOrigin":"$undefined","nonce":"$undefined"}],["$","link","1",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","precedence":"next","crossOrigin":"$undefined","nonce":"$undefined"}],["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","async":true,"nonce":"$undefined"}]],["$","html",null,{"lang":"en","data-theme":"diagram","className":"inter_537b2856-module__DOofUa__variable jetbrains_mono_da46d059-module__t9WmUG__variable","children":[["$","head",null,{"children":["$","link",null,{"rel":"apple-touch-icon","href":"/icons/icon-192.png"}]}],["$","body",null,{"className":"h-screen overflow-hidden","children":["$","$L3",null,{"parallelRouterKey":"children","error":"$4","errorStyles":[],"errorScripts":[["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/2td1d3x1ljv-f.js","async":true}]],"template":["$","$L5",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":[["$","div",null,{"className":"flex h-screen flex-col items-center justify-center gap-4","children":[["$","h1",null,{"className":"text-2xl","children":"404"}],["$","p",null,{"className":"text-sm","children":"This diagram does not exist."}],["$","a",null,{"href":"/","className":"btn btn-primary btn-sm","children":"Back to editor"}]]}],[]],"forbidden":"$undefined","unauthorized":"$undefined"}]}]]}]]}]}],{"children":[["$","$2","c",{"children":[null,["$","$L3",null,{"parallelRouterKey":"children","error":"$undefined","errorStyles":"$undefined","errorScripts":"$undefined","template":["$","$L5",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":"$undefined","forbidden":"$undefined","unauthorized":"$undefined"}]]}],{"children":[["$","$2","c",{"children":[null,["$","$L3",null,{"parallelRouterKey":"children","error":"$undefined","errorStyles":"$undefined","errorScripts":"$undefined","template":["$","$L5",null,{}],"templateStyles":"$undefined","templateScripts":"$undefined","notFound":"$undefined","forbidden":"$undefined","unauthorized":"$undefined"}]]}],{"children":[["$","$2","c",{"children":["$L6",[["$","script","script-0",{"src":"/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","async":true,"nonce":"$undefined"}],["$","script","script-1",{"src":"/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js","async":true,"nonce":"$undefined"}]],["$","$L7",null,{"children":["$","$8",null,{"name":"Next.MetadataOutlet","children":"$@9"}]}]]}],{},null,false,null]},null,false,"$a"]},null,false,"$a"]},null,false,null],["$","$2","h",{"children":[null,["$","$Lb",null,{"children":"$Lc"}],["$","div",null,{"hidden":true,"children":["$","$Ld",null,{"children":["$","$8",null,{"name":"Next.Metadata","children":"$Le"}]}]}],["$","meta",null,{"name":"next-size-adjust","content":""}]]}],false]],"m":"$undefined","G":["$f",[["$","link","0",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/3up92td8ytigi.css","precedence":"next","crossOrigin":"$undefined","nonce":"$undefined"}],["$","link","1",{"rel":"stylesheet","href":"/downloads/diagram/_next/static/chunks/1zf5djuwsvj55.css","precedence":"next","crossOrigin":"$undefined","nonce":"$undefined"}]]],"S":true,"h":null,"r":"$undefined","s":"$undefined","a":"$undefined","l":"$undefined","p":"$undefined","d":"$undefined","b":"YtoKbkX7y76MytYfCubSL"}
10:I[83070,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js"],""]
a:C
6:["$","div",null,{"className":"h-screen overflow-y-auto","children":["$","main",null,{"className":"mx-auto max-w-3xl px-4 py-10","children":[["$","p",null,{"className":"mb-6 opacity-60","children":["$","$L10",null,{"className":"link","href":"/posts/","children":"← All posts"}]}],["$","article",null,{"children":[["$","header",null,{"className":"mb-8","children":[["$","h1",null,{"className":"text-3xl font-bold","children":"Sentiment Analysis"}],["$","p",null,{"className":"text-base-content/70 mt-2","children":"Text scoring, aggregation, dashboards, streaming input."}],["$","p",null,{"className":"text-base-content/50 mt-3 text-sm","children":["Hieu Doan"," · ",["$","span",null,{"className":"capitalize","children":"hard"}]," ·"," ",["$","span",null,{"className":"capitalize","children":"ai"}]]}],["$","div",null,{"className":"mt-3 flex flex-wrap gap-1.5","children":[["$","span","analytics",{"className":"badge badge-outline badge-sm","children":"analytics"}],["$","span","ml",{"className":"badge badge-outline badge-sm","children":"ml"}],["$","span","realtime",{"className":"badge badge-outline badge-sm","children":"realtime"}]]}]]}],["$","section",null,{"children":[["$","h2",null,{"className":"mb-2 text-xl font-semibold","children":"Interview Questions"}],["$","ul",null,{"className":"list-disc space-y-1 pl-6","children":[["$","li","Design a sentiment analysis service",{"children":"Design a sentiment analysis service"}],["$","li","How do you classify sentiment at scale?",{"children":"How do you classify sentiment at scale?"}],["$","li","How do you aggregate sentiment over time?",{"children":"How do you aggregate sentiment over time?"}],["$","li","How do you handle streaming data sources?",{"children":"How do you handle streaming data sources?"}],["$","li","How do you evaluate and improve accuracy?",{"children":"How do you evaluate and improve accuracy?"}]]}]]}],["$","section",null,{"className":"mt-8","children":[["$","h2",null,{"className":"mb-4 text-xl font-semibold","children":"Answers"}],["$","div",null,{"className":"space-y-6","children":[["$","div","Design a sentiment analysis service",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"Design a sentiment analysis service"}],[["$","p","A sentiment analysis service takes text from many sources, classifies each piece as positive, negative, or neutral, and presents the results as time-series trends and dashboards. Data Sources stream text continuously, and the Dashboard App is the read side that renders aggregated results. The flow is decoupled by an Event Queue: an Ingestion Worker buffers incoming text, enqueues it, and a pool of classifier workers consume the queue and score each message with the Sentiment Model. Each scored message flows to the Aggregation Engine, which buckets results by time and dimension and writes aggregates into a Time Series Store. The Dashboard Service queries that store for the Dashboard App, and the Model Monitor watches the classifier for drift while a Scores DB retains the raw scored records.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"A sentiment analysis service takes text from many sources, classifies each piece as positive, negative, or neutral, and presents the results as time-series trends and dashboards. Data Sources stream text continuously, and the Dashboard App is the read side that renders aggregated results. The flow is decoupled by an Event Queue: an Ingestion Worker buffers incoming text, enqueues it, and a pool of classifier workers consume the queue and score each message with the Sentiment Model. Each scored message flows to the Aggregation Engine, which buckets results by time and dimension and writes aggregates into a Time Series Store. The Dashboard Service queries that store for the Dashboard App, and the Model Monitor watches the classifier for drift while a Scores DB retains the raw scored records."}]]}],"$L11","$L12"]]}],"$L13","$L14","$L15","$L16"]}]]}],"$L17"]}],"$L18"]}]}]
1c:I[94844,["/downloads/diagram/_next/static/chunks/3b87zvv_q40u7.js","/downloads/diagram/_next/static/chunks/0usrn1voy2s95.js","/downloads/diagram/_next/static/chunks/1dw64o3a44yv9.js"],"default"]
11:["$","p","The queue is the backbone of the design because it decouples the write rate from the read and processing rate. Sources produce text in bursts, and consumers cannot be allowed to fall behind permanently or to be overwhelmed. The queue absorbs the burst, lets the consumer fleet scale independently of the producers, and gives the pipeline replay semantics when a consumer fails. Buffering also lets the ingestion layer normalize formats, so every source, whether a tweet, a review, or a survey response, becomes a uniform event before classification.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"The queue is the backbone of the design because it decouples the write rate from the read and processing rate. Sources produce text in bursts, and consumers cannot be allowed to fall behind permanently or to be overwhelmed. The queue absorbs the burst, lets the consumer fleet scale independently of the producers, and gives the pipeline replay semantics when a consumer fails. Buffering also lets the ingestion layer normalize formats, so every source, whether a tweet, a review, or a survey response, becomes a uniform event before classification."}]]}]
12:["$","p","The system must handle two distinct traffic shapes. Streaming sources arrive in real time and drive the live dashboards, while batch sources, such as historical reviews, arrive in large chunks and backfill the aggregates. Both paths converge on the same classification and aggregation stages, which keeps the machinery simple. The design goal is that a newly ingested message changes the dashboard within seconds, while the pipeline remains robust to producer outages, consumer failures, and wildly variable message volumes.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"The system must handle two distinct traffic shapes. Streaming sources arrive in real time and drive the live dashboards, while batch sources, such as historical reviews, arrive in large chunks and backfill the aggregates. Both paths converge on the same classification and aggregation stages, which keeps the machinery simple. The design goal is that a newly ingested message changes the dashboard within seconds, while the pipeline remains robust to producer outages, consumer failures, and wildly variable message volumes."}]]}]
13:["$","div","How do you classify sentiment at scale?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you classify sentiment at scale?"}],[["$","p","Classification is a machine learning problem served as a stateless scoring service. Each message is tokenized, normalized, and converted into the feature representation the Sentiment Model expects, typically an embedding vector. The model predicts a probability distribution over sentiment classes, and the service emits the most likely class with a confidence score. Neutral is treated as a real class rather than the absence of signal, because in product reviews and social posts neutral is the majority of the traffic, and miscounting it corrupts every downstream trend. The confidence score is carried through the pipeline so downstream aggregation can weight or filter low-confidence predictions.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"Classification is a machine learning problem served as a stateless scoring service. Each message is tokenized, normalized, and converted into the feature representation the Sentiment Model expects, typically an embedding vector. The model predicts a probability distribution over sentiment classes, and the service emits the most likely class with a confidence score. Neutral is treated as a real class rather than the absence of signal, because in product reviews and social posts neutral is the majority of the traffic, and miscounting it corrupts every downstream trend. The confidence score is carried through the pipeline so downstream aggregation can weight or filter low-confidence predictions."}]]}],["$","p","Throughput is governed by model inference cost, so the serving tier is built for batching. Consumers collect messages into micro-batches, run the model on the batch in one pass on a GPU or an optimized CPU kernel, and emit one result per message. Batch inference is often an order of magnitude cheaper than per-message inference, so the queue size and consumer count are tuned to maximize batch fill without exceeding latency targets. The model service is horizontally scaled, and because inference is stateless, any message can be scored by any instance.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"Throughput is governed by model inference cost, so the serving tier is built for batching. Consumers collect messages into micro-batches, run the model on the batch in one pass on a GPU or an optimized CPU kernel, and emit one result per message. Batch inference is often an order of magnitude cheaper than per-message inference, so the queue size and consumer count are tuned to maximize batch fill without exceeding latency targets. The model service is horizontally scaled, and because inference is stateless, any message can be scored by any instance."}]]}],["$","p","Scale also means defending the model from pathological input. Long documents are truncated or chunked with overlapping windows, non-text payloads are rejected at ingestion, and adversarial or non-linguistic input is handled by a fallback path that yields neutral with low confidence. The Scores DB records every classification with its input hash, timestamp, and source, so the team can reproduce the scoring decision later. This raw store is also the foundation for monitoring, since the distribution of predicted classes and confidences is the first signal that the model has started misbehaving.",{"className":"mb-2 leading-relaxed","children":["$L19"]}]]]}]
14:["$","div","How do you aggregate sentiment over time?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you aggregate sentiment over time?"}],[["$","p","Raw classifications are individual events, but the product value is the trend. The Aggregation Engine consumes the scored stream and folds each message into pre-aggregated buckets keyed by time window, source, and dimension such as product or topic. Buckets are typically one-minute or five-minute tumbling windows, and each bucket stores the count per sentiment class plus a rolling positivity score, which can be a weighted average of the class labels or the mean of the confidence-weighted score. Writing aggregates instead of raw events into the Time Series Store keeps the dashboard queries cheap and bounded, since the number of buckets is tiny compared to the number of messages.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"Raw classifications are individual events, but the product value is the trend. The Aggregation Engine consumes the scored stream and folds each message into pre-aggregated buckets keyed by time window, source, and dimension such as product or topic. Buckets are typically one-minute or five-minute tumbling windows, and each bucket stores the count per sentiment class plus a rolling positivity score, which can be a weighted average of the class labels or the mean of the confidence-weighted score. Writing aggregates instead of raw events into the Time Series Store keeps the dashboard queries cheap and bounded, since the number of buckets is tiny compared to the number of messages."}]]}],["$","p","Aggregation must handle two failure modes: late events and out-of-order arrival. A message that was queued during a slow consumer burst may arrive after its window closed, so the engine uses watermark-based windowing and explicitly re-aggregates late events into their correct bucket or into a correction bucket that the dashboard merges. Without this, the tails of the distribution are silently undercounted and trends drift. The bucket store is also designed for idempotent writes, so a replayed batch of messages updates counters in a way that is exactly computable regardless of duplication.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"Aggregation must handle two failure modes: late events and out-of-order arrival. A message that was queued during a slow consumer burst may arrive after its window closed, so the engine uses watermark-based windowing and explicitly re-aggregates late events into their correct bucket or into a correction bucket that the dashboard merges. Without this, the tails of the distribution are silently undercounted and trends drift. The bucket store is also designed for idempotent writes, so a replayed batch of messages updates counters in a way that is exactly computable regardless of duplication."}]]}],["$","p","The Time Series Store serves the dashboard directly and is read-heavy, so it is optimized for range scans and downsampling. The Dashboard Service rolls fine-grained buckets into hourly and daily series, so the same store serves both a live minute-by-minute view and a six-month trend with predictable latency. Aggregates are stored as compact rows rather than raw events, and the retention policy keeps raw events in the Scores DB for a shorter window while aggregates persist much longer, balancing cost against the ability to recompute history if the aggregation logic changes.",{"className":"mb-2 leading-relaxed","children":["$L1a"]}]]]}]
15:["$","div","How do you handle streaming data sources?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you handle streaming data sources?"}],[["$","p","Streaming is the dominant input pattern, and the design treats the ingestion tier as a throughput funnel. Each Data Source connects through a stable ingestion endpoint, and the Ingestion Worker validates, normalizes, and enriches the message before enqueueing it. Normalization matters because sources differ wildly: a tweet has emoji and truncation, a review has star ratings alongside text, and a chat message has slang. The worker also applies backpressure to producers, because a fast source must slow down or buffer rather than overwhelm the queue, and it tags each message with an arrival timestamp and source ID that flow through every downstream stage.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"Streaming is the dominant input pattern, and the design treats the ingestion tier as a throughput funnel. Each Data Source connects through a stable ingestion endpoint, and the Ingestion Worker validates, normalizes, and enriches the message before enqueueing it. Normalization matters because sources differ wildly: a tweet has emoji and truncation, a review has star ratings alongside text, and a chat message has slang. The worker also applies backpressure to producers, because a fast source must slow down or buffer rather than overwhelm the queue, and it tags each message with an arrival timestamp and source ID that flow through every downstream stage."}]]}],["$","p","The Event Queue gives the pipeline its resilience properties. Producers and consumers are fully decoupled, so a source outage or a classifier outage does not take the other down. Consumers track their position in the queue and commit offsets only after a message is durably scored and written, which provides at-least-once delivery. That choice forces idempotency downstream: aggregation is designed so that a duplicated message updates a count exactly once. The queue also enables parallel consumption, with partitions per source or hash so that ordering within a source is preserved when it matters.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"The Event Queue gives the pipeline its resilience properties. Producers and consumers are fully decoupled, so a source outage or a classifier outage does not take the other down. Consumers track their position in the queue and commit offsets only after a message is durably scored and written, which provides at-least-once delivery. That choice forces idempotency downstream: aggregation is designed so that a duplicated message updates a count exactly once. The queue also enables parallel consumption, with partitions per source or hash so that ordering within a source is preserved when it matters."}]]}],["$","p","Streaming brings steady-state operation and monitoring concerns that batch systems do not have. The consumer group lag is the primary health metric, because sustained lag means the dashboard is showing the past. Autoscaling of consumers is driven by lag rather than CPU, adding capacity when the backlog grows and shedding it when it shrinks. Replays are a deliberate feature, so when classification logic is fixed, the pipeline can rewind the queue position and reprocess a window of history, giving the team a cheap way to correct a bad stretch of predictions.",{"className":"mb-2 leading-relaxed","children":["$L1b"]}]]]}]
16:["$","div","How do you evaluate and improve accuracy?",{"children":[["$","h3",null,{"className":"mb-2 font-semibold","children":"How do you evaluate and improve accuracy?"}],[["$","p","Accuracy is a pipeline property, not just a model property, so evaluation starts with labeled ground truth. The team maintains a labeled test set of messages spanning the sources and domains the product serves, and every candidate model or threshold change is scored against it before deployment. Precision, recall, and F1 are measured per class, and the acceptance bar is per-domain, because a model that nails product reviews may fail on sarcastic social posts. The same evaluation harness gates rule changes and model updates, so the system cannot silently regress.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"Accuracy is a pipeline property, not just a model property, so evaluation starts with labeled ground truth. The team maintains a labeled test set of messages spanning the sources and domains the product serves, and every candidate model or threshold change is scored against it before deployment. Precision, recall, and F1 are measured per class, and the acceptance bar is per-domain, because a model that nails product reviews may fail on sarcastic social posts. The same evaluation harness gates rule changes and model updates, so the system cannot silently regress."}]]}],["$","p","The Model Monitor provides the runtime half of evaluation. It tracks the distribution of predicted classes, confidence scores, and vocabulary over time, and flags drift when the input distribution moves away from what the model was trained on. A classic failure is domain shift, where a new product launch or a new slang term changes the language without changing the labels. The monitor also samples scored messages for human review, producing continuously refreshed ground truth that feeds the evaluation set and detects accuracy decay that distribution monitoring alone cannot see.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"The Model Monitor provides the runtime half of evaluation. It tracks the distribution of predicted classes, confidence scores, and vocabulary over time, and flags drift when the input distribution moves away from what the model was trained on. A classic failure is domain shift, where a new product launch or a new slang term changes the language without changing the labels. The monitor also samples scored messages for human review, producing continuously refreshed ground truth that feeds the evaluation set and detects accuracy decay that distribution monitoring alone cannot see."}]]}],["$","p","Improvement closes the loop back into training. Sampled misclassifications from the monitor are labeled and added to the training set, and the model is retrained on a schedule, with the new version validated on a held-out window and compared against the current version before promotion. Aggregated acceptance data from the dashboard, such as how trends compared to expectations, feeds back as weak labels. Every version is logged in the Scores DB, so a change in dashboard behavior can be traced to the exact model and input distribution that produced it.",{"className":"mb-2 leading-relaxed","children":[["$","$2","0",{"children":"Improvement closes the loop back into training. Sampled misclassifications from the monitor are labeled and added to the training set, and the model is retrained on a schedule, with the new version validated on a held-out window and compared against the current version before promotion. Aggregated acceptance data from the dashboard, such as how trends compared to expectations, feeds back as weak labels. Every version is logged in the Scores DB, so a change in dashboard behavior can be traced to the exact model and input distribution that produced it."}]]}]]]}]
17:["$","details",null,{"className":"mt-8","children":[["$","summary",null,{"className":"cursor-pointer font-medium","children":"Diagram source"}],["$","pre",null,{"className":"bg-base-200 mt-2 overflow-x-auto rounded-lg p-4 font-mono text-xs","children":"title: Sentiment Analysis\nnode source: Data Sources [icon=cloud]\nnode app: Dashboard App [icon=browser]\nnode gateway: API Gateway [icon=server]\nnode ingest: Ingestion Worker [icon=worker]\nnode queue: Event Queue [icon=queue]\nnode classify: Sentiment Model [icon=cloud]\nnode agg: Aggregation Engine [icon=compute]\nnode store: Time Series Store [cylinder, icon=database]\nnode dash: Dashboard Service [icon=search]\nnode monitor: Model Monitor [icon=cache]\nnode db: Scores DB [cylinder, icon=database]\n\nedge source -> app: stream\nedge app -> gateway: forward\nedge gateway -> ingest: buffer\nedge ingest -> queue: enqueue\nedge queue -> classify: score\nedge classify -> agg: aggregate\nedge agg -> store: buckets\nedge store -> dash: query\nedge dash -> app: render\nedge classify -> monitor: drift\nedge classify -> db: store"}]]}]
18:["$","div",null,{"className":"mt-8","children":["$","$L1c",null,{"text":"title: Sentiment Analysis\nnode source: Data Sources [icon=cloud]\nnode app: Dashboard App [icon=browser]\nnode gateway: API Gateway [icon=server]\nnode ingest: Ingestion Worker [icon=worker]\nnode queue: Event Queue [icon=queue]\nnode classify: Sentiment Model [icon=cloud]\nnode agg: Aggregation Engine [icon=compute]\nnode store: Time Series Store [cylinder, icon=database]\nnode dash: Dashboard Service [icon=search]\nnode monitor: Model Monitor [icon=cache]\nnode db: Scores DB [cylinder, icon=database]\n\nedge source -> app: stream\nedge app -> gateway: forward\nedge gateway -> ingest: buffer\nedge ingest -> queue: enqueue\nedge queue -> classify: score\nedge classify -> agg: aggregate\nedge agg -> store: buckets\nedge store -> dash: query\nedge dash -> app: render\nedge classify -> monitor: drift\nedge classify -> db: store","name":"Sentiment Analysis"}]}]
19:["$","$2","0",{"children":"Scale also means defending the model from pathological input. Long documents are truncated or chunked with overlapping windows, non-text payloads are rejected at ingestion, and adversarial or non-linguistic input is handled by a fallback path that yields neutral with low confidence. The Scores DB records every classification with its input hash, timestamp, and source, so the team can reproduce the scoring decision later. This raw store is also the foundation for monitoring, since the distribution of predicted classes and confidences is the first signal that the model has started misbehaving."}]
1a:["$","$2","0",{"children":"The Time Series Store serves the dashboard directly and is read-heavy, so it is optimized for range scans and downsampling. The Dashboard Service rolls fine-grained buckets into hourly and daily series, so the same store serves both a live minute-by-minute view and a six-month trend with predictable latency. Aggregates are stored as compact rows rather than raw events, and the retention policy keeps raw events in the Scores DB for a shorter window while aggregates persist much longer, balancing cost against the ability to recompute history if the aggregation logic changes."}]
1b:["$","$2","0",{"children":"Streaming brings steady-state operation and monitoring concerns that batch systems do not have. The consumer group lag is the primary health metric, because sustained lag means the dashboard is showing the past. Autoscaling of consumers is driven by lag rather than CPU, adding capacity when the backlog grows and shedding it when it shrinks. Replays are a deliberate feature, so when classification logic is fixed, the pipeline can rewind the queue position and reprocess a window of history, giving the team a cheap way to correct a bad stretch of predictions."}]
c:[["$","meta","0",{"charSet":"utf-8"}],["$","meta","1",{"name":"viewport","content":"width=device-width, initial-scale=1"}]]
9:null
e:[["$","title","0",{"children":"Sentiment Analysis — Diagram Examples"}],["$","meta","1",{"name":"description","content":"Text scoring, aggregation, dashboards, streaming input."}],["$","link","2",{"rel":"manifest","href":"/manifest.json","crossOrigin":"$undefined"}],["$","meta","3",{"name":"mobile-web-app-capable","content":"yes"}],["$","meta","4",{"name":"apple-mobile-web-app-title","content":"Diagram"}],["$","meta","5",{"name":"apple-mobile-web-app-status-bar-style","content":"black-translucent"}]]
